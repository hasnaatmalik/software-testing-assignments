"""
Validator Agent -- 3-Tier Validation Engine for Django Migration

Tier 1: Static Validation (per-file, deterministic)
  - Syntax check via ast.parse()
  - Django 5.x API compatibility checks
  - Required parameter enforcement (on_delete, etc.)

Tier 2: Cross-File Consistency (uses dependency graph)
  - Model → View field reference consistency
  - Model → Serializer field sync
  - Import chain validation
  - URL → View binding check

Tier 3: LLM Semantic Review (optional, AI-powered)
  - Logic preservation check
  - Pattern correctness review
  - Missing migration detection
"""
import ast
import re
import os
import sys
import tempfile
import traceback
try:
    import docker
except ImportError:
    docker = None
from typing import Dict, List, Any, Optional, Tuple


# ─── Django 2.x → 5.x Deprecation Database ──────────────────────────────────

DEPRECATED_IMPORTS = {
    # (old_module, old_name) → (new_module, new_name)
    ("django.conf.urls", "url"): ("django.urls", "re_path"),
    ("django.conf.urls", "include"): ("django.urls", "include"),
    ("django.utils.encoding", "force_text"): ("django.utils.encoding", "force_str"),
    ("django.utils.encoding", "smart_text"): ("django.utils.encoding", "smart_str"),
    ("django.utils.translation", "ugettext"): ("django.utils.translation", "gettext"),
    ("django.utils.translation", "ugettext_lazy"): ("django.utils.translation", "gettext_lazy"),
    ("django.utils.translation", "ungettext"): ("django.utils.translation", "ngettext"),
    ("django.utils.translation", "ungettext_lazy"): ("django.utils.translation", "ngettext_lazy"),
    ("django.utils.encoding", "python_2_unicode_compatible"): (None, None),  # Removed entirely
    ("django.utils.decorators", "ContextDecorator"): ("contextlib", "ContextDecorator"),
    ("django.utils.lru_cache", "lru_cache"): ("functools", "lru_cache"),
    ("django.contrib.postgres.fields", "JSONField"): ("django.db.models", "JSONField"),
}

REMOVED_SETTINGS = [
    "MIDDLEWARE_CLASSES",           # → MIDDLEWARE
    "DEFAULT_CONTENT_TYPE",        # Removed in Django 3.0
    "FILE_CHARSET",                # Removed in Django 3.1
    "PASSWORD_RESET_TIMEOUT_DAYS", # → PASSWORD_RESET_TIMEOUT (seconds)
]

FOREIGNKEY_FIELDS = ["ForeignKey", "OneToOneField"]

DJANGO5_REMOVED_FUNCTIONS = [
    "smart_text",
    "force_text",
    "ugettext",
    "ugettext_lazy",
    "ungettext",
    "ungettext_lazy",
    "python_2_unicode_compatible",
]
from typing import Annotated
from typing import Callable
from typing import ClassVar

MutantDict = Annotated[dict[str, Callable], "Mutant"] # type: ignore


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None): # type: ignore
    """Forward call to original or mutated function, depending on the environment"""
    import os # type: ignore
    mutant_under_test = os.environ['MUTANT_UNDER_TEST'] # type: ignore
    if mutant_under_test == 'fail': # type: ignore
        from mutmut.__main__ import MutmutProgrammaticFailException # type: ignore
        raise MutmutProgrammaticFailException('Failed programmatically')       # type: ignore
    elif mutant_under_test == 'stats': # type: ignore
        from mutmut.__main__ import record_trampoline_hit # type: ignore
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__) # type: ignore
        # (for class methods, orig is bound and thus does not need the explicit self argument)
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_' # type: ignore
    if not mutant_under_test.startswith(prefix): # type: ignore
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    mutant_name = mutant_under_test.rpartition('.')[-1] # type: ignore
    if self_arg is not None: # type: ignore
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs) # type: ignore
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs) # type: ignore
    return result # type: ignore


class ValidationIssue:
    """Represents a single validation issue found during checking."""
    
    def __init__(
        self,
        tier: int,
        severity: str,      # "error", "warning", "info"
        category: str,       # "syntax", "import", "api_compat", "cross_file", "semantic"
        message: str,
        file_path: str,
        line_number: Optional[int] = None,
        fix_hint: Optional[str] = None,
        auto_fixable: bool = False,
    ):
        args = [tier, severity, category, message, file_path, line_number, fix_hint, auto_fixable]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁValidationIssueǁ__init____mutmut_orig'), object.__getattribute__(self, 'xǁValidationIssueǁ__init____mutmut_mutants'), args, kwargs, self)
    
    def xǁValidationIssueǁ__init____mutmut_orig(
        self,
        tier: int,
        severity: str,      # "error", "warning", "info"
        category: str,       # "syntax", "import", "api_compat", "cross_file", "semantic"
        message: str,
        file_path: str,
        line_number: Optional[int] = None,
        fix_hint: Optional[str] = None,
        auto_fixable: bool = False,
    ):
        self.tier = tier
        self.severity = severity
        self.category = category
        self.message = message
        self.file_path = file_path
        self.line_number = line_number
        self.fix_hint = fix_hint
        self.auto_fixable = auto_fixable
    
    def xǁValidationIssueǁ__init____mutmut_1(
        self,
        tier: int,
        severity: str,      # "error", "warning", "info"
        category: str,       # "syntax", "import", "api_compat", "cross_file", "semantic"
        message: str,
        file_path: str,
        line_number: Optional[int] = None,
        fix_hint: Optional[str] = None,
        auto_fixable: bool = True,
    ):
        self.tier = tier
        self.severity = severity
        self.category = category
        self.message = message
        self.file_path = file_path
        self.line_number = line_number
        self.fix_hint = fix_hint
        self.auto_fixable = auto_fixable
    
    def xǁValidationIssueǁ__init____mutmut_2(
        self,
        tier: int,
        severity: str,      # "error", "warning", "info"
        category: str,       # "syntax", "import", "api_compat", "cross_file", "semantic"
        message: str,
        file_path: str,
        line_number: Optional[int] = None,
        fix_hint: Optional[str] = None,
        auto_fixable: bool = False,
    ):
        self.tier = None
        self.severity = severity
        self.category = category
        self.message = message
        self.file_path = file_path
        self.line_number = line_number
        self.fix_hint = fix_hint
        self.auto_fixable = auto_fixable
    
    def xǁValidationIssueǁ__init____mutmut_3(
        self,
        tier: int,
        severity: str,      # "error", "warning", "info"
        category: str,       # "syntax", "import", "api_compat", "cross_file", "semantic"
        message: str,
        file_path: str,
        line_number: Optional[int] = None,
        fix_hint: Optional[str] = None,
        auto_fixable: bool = False,
    ):
        self.tier = tier
        self.severity = None
        self.category = category
        self.message = message
        self.file_path = file_path
        self.line_number = line_number
        self.fix_hint = fix_hint
        self.auto_fixable = auto_fixable
    
    def xǁValidationIssueǁ__init____mutmut_4(
        self,
        tier: int,
        severity: str,      # "error", "warning", "info"
        category: str,       # "syntax", "import", "api_compat", "cross_file", "semantic"
        message: str,
        file_path: str,
        line_number: Optional[int] = None,
        fix_hint: Optional[str] = None,
        auto_fixable: bool = False,
    ):
        self.tier = tier
        self.severity = severity
        self.category = None
        self.message = message
        self.file_path = file_path
        self.line_number = line_number
        self.fix_hint = fix_hint
        self.auto_fixable = auto_fixable
    
    def xǁValidationIssueǁ__init____mutmut_5(
        self,
        tier: int,
        severity: str,      # "error", "warning", "info"
        category: str,       # "syntax", "import", "api_compat", "cross_file", "semantic"
        message: str,
        file_path: str,
        line_number: Optional[int] = None,
        fix_hint: Optional[str] = None,
        auto_fixable: bool = False,
    ):
        self.tier = tier
        self.severity = severity
        self.category = category
        self.message = None
        self.file_path = file_path
        self.line_number = line_number
        self.fix_hint = fix_hint
        self.auto_fixable = auto_fixable
    
    def xǁValidationIssueǁ__init____mutmut_6(
        self,
        tier: int,
        severity: str,      # "error", "warning", "info"
        category: str,       # "syntax", "import", "api_compat", "cross_file", "semantic"
        message: str,
        file_path: str,
        line_number: Optional[int] = None,
        fix_hint: Optional[str] = None,
        auto_fixable: bool = False,
    ):
        self.tier = tier
        self.severity = severity
        self.category = category
        self.message = message
        self.file_path = None
        self.line_number = line_number
        self.fix_hint = fix_hint
        self.auto_fixable = auto_fixable
    
    def xǁValidationIssueǁ__init____mutmut_7(
        self,
        tier: int,
        severity: str,      # "error", "warning", "info"
        category: str,       # "syntax", "import", "api_compat", "cross_file", "semantic"
        message: str,
        file_path: str,
        line_number: Optional[int] = None,
        fix_hint: Optional[str] = None,
        auto_fixable: bool = False,
    ):
        self.tier = tier
        self.severity = severity
        self.category = category
        self.message = message
        self.file_path = file_path
        self.line_number = None
        self.fix_hint = fix_hint
        self.auto_fixable = auto_fixable
    
    def xǁValidationIssueǁ__init____mutmut_8(
        self,
        tier: int,
        severity: str,      # "error", "warning", "info"
        category: str,       # "syntax", "import", "api_compat", "cross_file", "semantic"
        message: str,
        file_path: str,
        line_number: Optional[int] = None,
        fix_hint: Optional[str] = None,
        auto_fixable: bool = False,
    ):
        self.tier = tier
        self.severity = severity
        self.category = category
        self.message = message
        self.file_path = file_path
        self.line_number = line_number
        self.fix_hint = None
        self.auto_fixable = auto_fixable
    
    def xǁValidationIssueǁ__init____mutmut_9(
        self,
        tier: int,
        severity: str,      # "error", "warning", "info"
        category: str,       # "syntax", "import", "api_compat", "cross_file", "semantic"
        message: str,
        file_path: str,
        line_number: Optional[int] = None,
        fix_hint: Optional[str] = None,
        auto_fixable: bool = False,
    ):
        self.tier = tier
        self.severity = severity
        self.category = category
        self.message = message
        self.file_path = file_path
        self.line_number = line_number
        self.fix_hint = fix_hint
        self.auto_fixable = None
    
    xǁValidationIssueǁ__init____mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁValidationIssueǁ__init____mutmut_1': xǁValidationIssueǁ__init____mutmut_1, 
        'xǁValidationIssueǁ__init____mutmut_2': xǁValidationIssueǁ__init____mutmut_2, 
        'xǁValidationIssueǁ__init____mutmut_3': xǁValidationIssueǁ__init____mutmut_3, 
        'xǁValidationIssueǁ__init____mutmut_4': xǁValidationIssueǁ__init____mutmut_4, 
        'xǁValidationIssueǁ__init____mutmut_5': xǁValidationIssueǁ__init____mutmut_5, 
        'xǁValidationIssueǁ__init____mutmut_6': xǁValidationIssueǁ__init____mutmut_6, 
        'xǁValidationIssueǁ__init____mutmut_7': xǁValidationIssueǁ__init____mutmut_7, 
        'xǁValidationIssueǁ__init____mutmut_8': xǁValidationIssueǁ__init____mutmut_8, 
        'xǁValidationIssueǁ__init____mutmut_9': xǁValidationIssueǁ__init____mutmut_9
    }
    xǁValidationIssueǁ__init____mutmut_orig.__name__ = 'xǁValidationIssueǁ__init__'
    
    def to_dict(self) -> Dict[str, Any]:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁValidationIssueǁto_dict__mutmut_orig'), object.__getattribute__(self, 'xǁValidationIssueǁto_dict__mutmut_mutants'), args, kwargs, self)
    
    def xǁValidationIssueǁto_dict__mutmut_orig(self) -> Dict[str, Any]:
        return {
            "tier": self.tier,
            "severity": self.severity,
            "category": self.category,
            "message": self.message,
            "file_path": self.file_path,
            "line_number": self.line_number,
            "fix_hint": self.fix_hint,
            "auto_fixable": self.auto_fixable,
        }
    
    def xǁValidationIssueǁto_dict__mutmut_1(self) -> Dict[str, Any]:
        return {
            "XXtierXX": self.tier,
            "severity": self.severity,
            "category": self.category,
            "message": self.message,
            "file_path": self.file_path,
            "line_number": self.line_number,
            "fix_hint": self.fix_hint,
            "auto_fixable": self.auto_fixable,
        }
    
    def xǁValidationIssueǁto_dict__mutmut_2(self) -> Dict[str, Any]:
        return {
            "TIER": self.tier,
            "severity": self.severity,
            "category": self.category,
            "message": self.message,
            "file_path": self.file_path,
            "line_number": self.line_number,
            "fix_hint": self.fix_hint,
            "auto_fixable": self.auto_fixable,
        }
    
    def xǁValidationIssueǁto_dict__mutmut_3(self) -> Dict[str, Any]:
        return {
            "tier": self.tier,
            "XXseverityXX": self.severity,
            "category": self.category,
            "message": self.message,
            "file_path": self.file_path,
            "line_number": self.line_number,
            "fix_hint": self.fix_hint,
            "auto_fixable": self.auto_fixable,
        }
    
    def xǁValidationIssueǁto_dict__mutmut_4(self) -> Dict[str, Any]:
        return {
            "tier": self.tier,
            "SEVERITY": self.severity,
            "category": self.category,
            "message": self.message,
            "file_path": self.file_path,
            "line_number": self.line_number,
            "fix_hint": self.fix_hint,
            "auto_fixable": self.auto_fixable,
        }
    
    def xǁValidationIssueǁto_dict__mutmut_5(self) -> Dict[str, Any]:
        return {
            "tier": self.tier,
            "severity": self.severity,
            "XXcategoryXX": self.category,
            "message": self.message,
            "file_path": self.file_path,
            "line_number": self.line_number,
            "fix_hint": self.fix_hint,
            "auto_fixable": self.auto_fixable,
        }
    
    def xǁValidationIssueǁto_dict__mutmut_6(self) -> Dict[str, Any]:
        return {
            "tier": self.tier,
            "severity": self.severity,
            "CATEGORY": self.category,
            "message": self.message,
            "file_path": self.file_path,
            "line_number": self.line_number,
            "fix_hint": self.fix_hint,
            "auto_fixable": self.auto_fixable,
        }
    
    def xǁValidationIssueǁto_dict__mutmut_7(self) -> Dict[str, Any]:
        return {
            "tier": self.tier,
            "severity": self.severity,
            "category": self.category,
            "XXmessageXX": self.message,
            "file_path": self.file_path,
            "line_number": self.line_number,
            "fix_hint": self.fix_hint,
            "auto_fixable": self.auto_fixable,
        }
    
    def xǁValidationIssueǁto_dict__mutmut_8(self) -> Dict[str, Any]:
        return {
            "tier": self.tier,
            "severity": self.severity,
            "category": self.category,
            "MESSAGE": self.message,
            "file_path": self.file_path,
            "line_number": self.line_number,
            "fix_hint": self.fix_hint,
            "auto_fixable": self.auto_fixable,
        }
    
    def xǁValidationIssueǁto_dict__mutmut_9(self) -> Dict[str, Any]:
        return {
            "tier": self.tier,
            "severity": self.severity,
            "category": self.category,
            "message": self.message,
            "XXfile_pathXX": self.file_path,
            "line_number": self.line_number,
            "fix_hint": self.fix_hint,
            "auto_fixable": self.auto_fixable,
        }
    
    def xǁValidationIssueǁto_dict__mutmut_10(self) -> Dict[str, Any]:
        return {
            "tier": self.tier,
            "severity": self.severity,
            "category": self.category,
            "message": self.message,
            "FILE_PATH": self.file_path,
            "line_number": self.line_number,
            "fix_hint": self.fix_hint,
            "auto_fixable": self.auto_fixable,
        }
    
    def xǁValidationIssueǁto_dict__mutmut_11(self) -> Dict[str, Any]:
        return {
            "tier": self.tier,
            "severity": self.severity,
            "category": self.category,
            "message": self.message,
            "file_path": self.file_path,
            "XXline_numberXX": self.line_number,
            "fix_hint": self.fix_hint,
            "auto_fixable": self.auto_fixable,
        }
    
    def xǁValidationIssueǁto_dict__mutmut_12(self) -> Dict[str, Any]:
        return {
            "tier": self.tier,
            "severity": self.severity,
            "category": self.category,
            "message": self.message,
            "file_path": self.file_path,
            "LINE_NUMBER": self.line_number,
            "fix_hint": self.fix_hint,
            "auto_fixable": self.auto_fixable,
        }
    
    def xǁValidationIssueǁto_dict__mutmut_13(self) -> Dict[str, Any]:
        return {
            "tier": self.tier,
            "severity": self.severity,
            "category": self.category,
            "message": self.message,
            "file_path": self.file_path,
            "line_number": self.line_number,
            "XXfix_hintXX": self.fix_hint,
            "auto_fixable": self.auto_fixable,
        }
    
    def xǁValidationIssueǁto_dict__mutmut_14(self) -> Dict[str, Any]:
        return {
            "tier": self.tier,
            "severity": self.severity,
            "category": self.category,
            "message": self.message,
            "file_path": self.file_path,
            "line_number": self.line_number,
            "FIX_HINT": self.fix_hint,
            "auto_fixable": self.auto_fixable,
        }
    
    def xǁValidationIssueǁto_dict__mutmut_15(self) -> Dict[str, Any]:
        return {
            "tier": self.tier,
            "severity": self.severity,
            "category": self.category,
            "message": self.message,
            "file_path": self.file_path,
            "line_number": self.line_number,
            "fix_hint": self.fix_hint,
            "XXauto_fixableXX": self.auto_fixable,
        }
    
    def xǁValidationIssueǁto_dict__mutmut_16(self) -> Dict[str, Any]:
        return {
            "tier": self.tier,
            "severity": self.severity,
            "category": self.category,
            "message": self.message,
            "file_path": self.file_path,
            "line_number": self.line_number,
            "fix_hint": self.fix_hint,
            "AUTO_FIXABLE": self.auto_fixable,
        }
    
    xǁValidationIssueǁto_dict__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁValidationIssueǁto_dict__mutmut_1': xǁValidationIssueǁto_dict__mutmut_1, 
        'xǁValidationIssueǁto_dict__mutmut_2': xǁValidationIssueǁto_dict__mutmut_2, 
        'xǁValidationIssueǁto_dict__mutmut_3': xǁValidationIssueǁto_dict__mutmut_3, 
        'xǁValidationIssueǁto_dict__mutmut_4': xǁValidationIssueǁto_dict__mutmut_4, 
        'xǁValidationIssueǁto_dict__mutmut_5': xǁValidationIssueǁto_dict__mutmut_5, 
        'xǁValidationIssueǁto_dict__mutmut_6': xǁValidationIssueǁto_dict__mutmut_6, 
        'xǁValidationIssueǁto_dict__mutmut_7': xǁValidationIssueǁto_dict__mutmut_7, 
        'xǁValidationIssueǁto_dict__mutmut_8': xǁValidationIssueǁto_dict__mutmut_8, 
        'xǁValidationIssueǁto_dict__mutmut_9': xǁValidationIssueǁto_dict__mutmut_9, 
        'xǁValidationIssueǁto_dict__mutmut_10': xǁValidationIssueǁto_dict__mutmut_10, 
        'xǁValidationIssueǁto_dict__mutmut_11': xǁValidationIssueǁto_dict__mutmut_11, 
        'xǁValidationIssueǁto_dict__mutmut_12': xǁValidationIssueǁto_dict__mutmut_12, 
        'xǁValidationIssueǁto_dict__mutmut_13': xǁValidationIssueǁto_dict__mutmut_13, 
        'xǁValidationIssueǁto_dict__mutmut_14': xǁValidationIssueǁto_dict__mutmut_14, 
        'xǁValidationIssueǁto_dict__mutmut_15': xǁValidationIssueǁto_dict__mutmut_15, 
        'xǁValidationIssueǁto_dict__mutmut_16': xǁValidationIssueǁto_dict__mutmut_16
    }
    xǁValidationIssueǁto_dict__mutmut_orig.__name__ = 'xǁValidationIssueǁto_dict'


class ValidationReport:
    """Complete validation report for a migration."""
    
    def __init__(self, file_path: str):
        args = [file_path]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁValidationReportǁ__init____mutmut_orig'), object.__getattribute__(self, 'xǁValidationReportǁ__init____mutmut_mutants'), args, kwargs, self)
    
    def xǁValidationReportǁ__init____mutmut_orig(self, file_path: str):
        self.file_path = file_path
        self.issues: List[ValidationIssue] = []
        self.passed = True
        self.tier1_passed = True
        self.tier2_passed = True
        self.tier3_passed = True
    
    def xǁValidationReportǁ__init____mutmut_1(self, file_path: str):
        self.file_path = None
        self.issues: List[ValidationIssue] = []
        self.passed = True
        self.tier1_passed = True
        self.tier2_passed = True
        self.tier3_passed = True
    
    def xǁValidationReportǁ__init____mutmut_2(self, file_path: str):
        self.file_path = file_path
        self.issues: List[ValidationIssue] = None
        self.passed = True
        self.tier1_passed = True
        self.tier2_passed = True
        self.tier3_passed = True
    
    def xǁValidationReportǁ__init____mutmut_3(self, file_path: str):
        self.file_path = file_path
        self.issues: List[ValidationIssue] = []
        self.passed = None
        self.tier1_passed = True
        self.tier2_passed = True
        self.tier3_passed = True
    
    def xǁValidationReportǁ__init____mutmut_4(self, file_path: str):
        self.file_path = file_path
        self.issues: List[ValidationIssue] = []
        self.passed = False
        self.tier1_passed = True
        self.tier2_passed = True
        self.tier3_passed = True
    
    def xǁValidationReportǁ__init____mutmut_5(self, file_path: str):
        self.file_path = file_path
        self.issues: List[ValidationIssue] = []
        self.passed = True
        self.tier1_passed = None
        self.tier2_passed = True
        self.tier3_passed = True
    
    def xǁValidationReportǁ__init____mutmut_6(self, file_path: str):
        self.file_path = file_path
        self.issues: List[ValidationIssue] = []
        self.passed = True
        self.tier1_passed = False
        self.tier2_passed = True
        self.tier3_passed = True
    
    def xǁValidationReportǁ__init____mutmut_7(self, file_path: str):
        self.file_path = file_path
        self.issues: List[ValidationIssue] = []
        self.passed = True
        self.tier1_passed = True
        self.tier2_passed = None
        self.tier3_passed = True
    
    def xǁValidationReportǁ__init____mutmut_8(self, file_path: str):
        self.file_path = file_path
        self.issues: List[ValidationIssue] = []
        self.passed = True
        self.tier1_passed = True
        self.tier2_passed = False
        self.tier3_passed = True
    
    def xǁValidationReportǁ__init____mutmut_9(self, file_path: str):
        self.file_path = file_path
        self.issues: List[ValidationIssue] = []
        self.passed = True
        self.tier1_passed = True
        self.tier2_passed = True
        self.tier3_passed = None
    
    def xǁValidationReportǁ__init____mutmut_10(self, file_path: str):
        self.file_path = file_path
        self.issues: List[ValidationIssue] = []
        self.passed = True
        self.tier1_passed = True
        self.tier2_passed = True
        self.tier3_passed = False
    
    xǁValidationReportǁ__init____mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁValidationReportǁ__init____mutmut_1': xǁValidationReportǁ__init____mutmut_1, 
        'xǁValidationReportǁ__init____mutmut_2': xǁValidationReportǁ__init____mutmut_2, 
        'xǁValidationReportǁ__init____mutmut_3': xǁValidationReportǁ__init____mutmut_3, 
        'xǁValidationReportǁ__init____mutmut_4': xǁValidationReportǁ__init____mutmut_4, 
        'xǁValidationReportǁ__init____mutmut_5': xǁValidationReportǁ__init____mutmut_5, 
        'xǁValidationReportǁ__init____mutmut_6': xǁValidationReportǁ__init____mutmut_6, 
        'xǁValidationReportǁ__init____mutmut_7': xǁValidationReportǁ__init____mutmut_7, 
        'xǁValidationReportǁ__init____mutmut_8': xǁValidationReportǁ__init____mutmut_8, 
        'xǁValidationReportǁ__init____mutmut_9': xǁValidationReportǁ__init____mutmut_9, 
        'xǁValidationReportǁ__init____mutmut_10': xǁValidationReportǁ__init____mutmut_10
    }
    xǁValidationReportǁ__init____mutmut_orig.__name__ = 'xǁValidationReportǁ__init__'
    
    def add_issue(self, issue: ValidationIssue):
        args = [issue]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁValidationReportǁadd_issue__mutmut_orig'), object.__getattribute__(self, 'xǁValidationReportǁadd_issue__mutmut_mutants'), args, kwargs, self)
    
    def xǁValidationReportǁadd_issue__mutmut_orig(self, issue: ValidationIssue):
        self.issues.append(issue)
        if issue.severity == "error":
            self.passed = False
            if issue.tier == 1:
                self.tier1_passed = False
            elif issue.tier == 2:
                self.tier2_passed = False
            elif issue.tier == 3:
                self.tier3_passed = False
    
    def xǁValidationReportǁadd_issue__mutmut_1(self, issue: ValidationIssue):
        self.issues.append(None)
        if issue.severity == "error":
            self.passed = False
            if issue.tier == 1:
                self.tier1_passed = False
            elif issue.tier == 2:
                self.tier2_passed = False
            elif issue.tier == 3:
                self.tier3_passed = False
    
    def xǁValidationReportǁadd_issue__mutmut_2(self, issue: ValidationIssue):
        self.issues.append(issue)
        if issue.severity != "error":
            self.passed = False
            if issue.tier == 1:
                self.tier1_passed = False
            elif issue.tier == 2:
                self.tier2_passed = False
            elif issue.tier == 3:
                self.tier3_passed = False
    
    def xǁValidationReportǁadd_issue__mutmut_3(self, issue: ValidationIssue):
        self.issues.append(issue)
        if issue.severity == "XXerrorXX":
            self.passed = False
            if issue.tier == 1:
                self.tier1_passed = False
            elif issue.tier == 2:
                self.tier2_passed = False
            elif issue.tier == 3:
                self.tier3_passed = False
    
    def xǁValidationReportǁadd_issue__mutmut_4(self, issue: ValidationIssue):
        self.issues.append(issue)
        if issue.severity == "ERROR":
            self.passed = False
            if issue.tier == 1:
                self.tier1_passed = False
            elif issue.tier == 2:
                self.tier2_passed = False
            elif issue.tier == 3:
                self.tier3_passed = False
    
    def xǁValidationReportǁadd_issue__mutmut_5(self, issue: ValidationIssue):
        self.issues.append(issue)
        if issue.severity == "error":
            self.passed = None
            if issue.tier == 1:
                self.tier1_passed = False
            elif issue.tier == 2:
                self.tier2_passed = False
            elif issue.tier == 3:
                self.tier3_passed = False
    
    def xǁValidationReportǁadd_issue__mutmut_6(self, issue: ValidationIssue):
        self.issues.append(issue)
        if issue.severity == "error":
            self.passed = True
            if issue.tier == 1:
                self.tier1_passed = False
            elif issue.tier == 2:
                self.tier2_passed = False
            elif issue.tier == 3:
                self.tier3_passed = False
    
    def xǁValidationReportǁadd_issue__mutmut_7(self, issue: ValidationIssue):
        self.issues.append(issue)
        if issue.severity == "error":
            self.passed = False
            if issue.tier != 1:
                self.tier1_passed = False
            elif issue.tier == 2:
                self.tier2_passed = False
            elif issue.tier == 3:
                self.tier3_passed = False
    
    def xǁValidationReportǁadd_issue__mutmut_8(self, issue: ValidationIssue):
        self.issues.append(issue)
        if issue.severity == "error":
            self.passed = False
            if issue.tier == 2:
                self.tier1_passed = False
            elif issue.tier == 2:
                self.tier2_passed = False
            elif issue.tier == 3:
                self.tier3_passed = False
    
    def xǁValidationReportǁadd_issue__mutmut_9(self, issue: ValidationIssue):
        self.issues.append(issue)
        if issue.severity == "error":
            self.passed = False
            if issue.tier == 1:
                self.tier1_passed = None
            elif issue.tier == 2:
                self.tier2_passed = False
            elif issue.tier == 3:
                self.tier3_passed = False
    
    def xǁValidationReportǁadd_issue__mutmut_10(self, issue: ValidationIssue):
        self.issues.append(issue)
        if issue.severity == "error":
            self.passed = False
            if issue.tier == 1:
                self.tier1_passed = True
            elif issue.tier == 2:
                self.tier2_passed = False
            elif issue.tier == 3:
                self.tier3_passed = False
    
    def xǁValidationReportǁadd_issue__mutmut_11(self, issue: ValidationIssue):
        self.issues.append(issue)
        if issue.severity == "error":
            self.passed = False
            if issue.tier == 1:
                self.tier1_passed = False
            elif issue.tier != 2:
                self.tier2_passed = False
            elif issue.tier == 3:
                self.tier3_passed = False
    
    def xǁValidationReportǁadd_issue__mutmut_12(self, issue: ValidationIssue):
        self.issues.append(issue)
        if issue.severity == "error":
            self.passed = False
            if issue.tier == 1:
                self.tier1_passed = False
            elif issue.tier == 3:
                self.tier2_passed = False
            elif issue.tier == 3:
                self.tier3_passed = False
    
    def xǁValidationReportǁadd_issue__mutmut_13(self, issue: ValidationIssue):
        self.issues.append(issue)
        if issue.severity == "error":
            self.passed = False
            if issue.tier == 1:
                self.tier1_passed = False
            elif issue.tier == 2:
                self.tier2_passed = None
            elif issue.tier == 3:
                self.tier3_passed = False
    
    def xǁValidationReportǁadd_issue__mutmut_14(self, issue: ValidationIssue):
        self.issues.append(issue)
        if issue.severity == "error":
            self.passed = False
            if issue.tier == 1:
                self.tier1_passed = False
            elif issue.tier == 2:
                self.tier2_passed = True
            elif issue.tier == 3:
                self.tier3_passed = False
    
    def xǁValidationReportǁadd_issue__mutmut_15(self, issue: ValidationIssue):
        self.issues.append(issue)
        if issue.severity == "error":
            self.passed = False
            if issue.tier == 1:
                self.tier1_passed = False
            elif issue.tier == 2:
                self.tier2_passed = False
            elif issue.tier != 3:
                self.tier3_passed = False
    
    def xǁValidationReportǁadd_issue__mutmut_16(self, issue: ValidationIssue):
        self.issues.append(issue)
        if issue.severity == "error":
            self.passed = False
            if issue.tier == 1:
                self.tier1_passed = False
            elif issue.tier == 2:
                self.tier2_passed = False
            elif issue.tier == 4:
                self.tier3_passed = False
    
    def xǁValidationReportǁadd_issue__mutmut_17(self, issue: ValidationIssue):
        self.issues.append(issue)
        if issue.severity == "error":
            self.passed = False
            if issue.tier == 1:
                self.tier1_passed = False
            elif issue.tier == 2:
                self.tier2_passed = False
            elif issue.tier == 3:
                self.tier3_passed = None
    
    def xǁValidationReportǁadd_issue__mutmut_18(self, issue: ValidationIssue):
        self.issues.append(issue)
        if issue.severity == "error":
            self.passed = False
            if issue.tier == 1:
                self.tier1_passed = False
            elif issue.tier == 2:
                self.tier2_passed = False
            elif issue.tier == 3:
                self.tier3_passed = True
    
    xǁValidationReportǁadd_issue__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁValidationReportǁadd_issue__mutmut_1': xǁValidationReportǁadd_issue__mutmut_1, 
        'xǁValidationReportǁadd_issue__mutmut_2': xǁValidationReportǁadd_issue__mutmut_2, 
        'xǁValidationReportǁadd_issue__mutmut_3': xǁValidationReportǁadd_issue__mutmut_3, 
        'xǁValidationReportǁadd_issue__mutmut_4': xǁValidationReportǁadd_issue__mutmut_4, 
        'xǁValidationReportǁadd_issue__mutmut_5': xǁValidationReportǁadd_issue__mutmut_5, 
        'xǁValidationReportǁadd_issue__mutmut_6': xǁValidationReportǁadd_issue__mutmut_6, 
        'xǁValidationReportǁadd_issue__mutmut_7': xǁValidationReportǁadd_issue__mutmut_7, 
        'xǁValidationReportǁadd_issue__mutmut_8': xǁValidationReportǁadd_issue__mutmut_8, 
        'xǁValidationReportǁadd_issue__mutmut_9': xǁValidationReportǁadd_issue__mutmut_9, 
        'xǁValidationReportǁadd_issue__mutmut_10': xǁValidationReportǁadd_issue__mutmut_10, 
        'xǁValidationReportǁadd_issue__mutmut_11': xǁValidationReportǁadd_issue__mutmut_11, 
        'xǁValidationReportǁadd_issue__mutmut_12': xǁValidationReportǁadd_issue__mutmut_12, 
        'xǁValidationReportǁadd_issue__mutmut_13': xǁValidationReportǁadd_issue__mutmut_13, 
        'xǁValidationReportǁadd_issue__mutmut_14': xǁValidationReportǁadd_issue__mutmut_14, 
        'xǁValidationReportǁadd_issue__mutmut_15': xǁValidationReportǁadd_issue__mutmut_15, 
        'xǁValidationReportǁadd_issue__mutmut_16': xǁValidationReportǁadd_issue__mutmut_16, 
        'xǁValidationReportǁadd_issue__mutmut_17': xǁValidationReportǁadd_issue__mutmut_17, 
        'xǁValidationReportǁadd_issue__mutmut_18': xǁValidationReportǁadd_issue__mutmut_18
    }
    xǁValidationReportǁadd_issue__mutmut_orig.__name__ = 'xǁValidationReportǁadd_issue'
    
    def to_dict(self) -> Dict[str, Any]:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁValidationReportǁto_dict__mutmut_orig'), object.__getattribute__(self, 'xǁValidationReportǁto_dict__mutmut_mutants'), args, kwargs, self)
    
    def xǁValidationReportǁto_dict__mutmut_orig(self) -> Dict[str, Any]:
        return {
            "file_path": self.file_path,
            "passed": self.passed,
            "tier1_passed": self.tier1_passed,
            "tier2_passed": self.tier2_passed,
            "tier3_passed": self.tier3_passed,
            "total_issues": len(self.issues),
            "error_count": sum(1 for i in self.issues if i.severity == "error"),
            "warning_count": sum(1 for i in self.issues if i.severity == "warning"),
            "issues": [i.to_dict() for i in self.issues],
        }
    
    def xǁValidationReportǁto_dict__mutmut_1(self) -> Dict[str, Any]:
        return {
            "XXfile_pathXX": self.file_path,
            "passed": self.passed,
            "tier1_passed": self.tier1_passed,
            "tier2_passed": self.tier2_passed,
            "tier3_passed": self.tier3_passed,
            "total_issues": len(self.issues),
            "error_count": sum(1 for i in self.issues if i.severity == "error"),
            "warning_count": sum(1 for i in self.issues if i.severity == "warning"),
            "issues": [i.to_dict() for i in self.issues],
        }
    
    def xǁValidationReportǁto_dict__mutmut_2(self) -> Dict[str, Any]:
        return {
            "FILE_PATH": self.file_path,
            "passed": self.passed,
            "tier1_passed": self.tier1_passed,
            "tier2_passed": self.tier2_passed,
            "tier3_passed": self.tier3_passed,
            "total_issues": len(self.issues),
            "error_count": sum(1 for i in self.issues if i.severity == "error"),
            "warning_count": sum(1 for i in self.issues if i.severity == "warning"),
            "issues": [i.to_dict() for i in self.issues],
        }
    
    def xǁValidationReportǁto_dict__mutmut_3(self) -> Dict[str, Any]:
        return {
            "file_path": self.file_path,
            "XXpassedXX": self.passed,
            "tier1_passed": self.tier1_passed,
            "tier2_passed": self.tier2_passed,
            "tier3_passed": self.tier3_passed,
            "total_issues": len(self.issues),
            "error_count": sum(1 for i in self.issues if i.severity == "error"),
            "warning_count": sum(1 for i in self.issues if i.severity == "warning"),
            "issues": [i.to_dict() for i in self.issues],
        }
    
    def xǁValidationReportǁto_dict__mutmut_4(self) -> Dict[str, Any]:
        return {
            "file_path": self.file_path,
            "PASSED": self.passed,
            "tier1_passed": self.tier1_passed,
            "tier2_passed": self.tier2_passed,
            "tier3_passed": self.tier3_passed,
            "total_issues": len(self.issues),
            "error_count": sum(1 for i in self.issues if i.severity == "error"),
            "warning_count": sum(1 for i in self.issues if i.severity == "warning"),
            "issues": [i.to_dict() for i in self.issues],
        }
    
    def xǁValidationReportǁto_dict__mutmut_5(self) -> Dict[str, Any]:
        return {
            "file_path": self.file_path,
            "passed": self.passed,
            "XXtier1_passedXX": self.tier1_passed,
            "tier2_passed": self.tier2_passed,
            "tier3_passed": self.tier3_passed,
            "total_issues": len(self.issues),
            "error_count": sum(1 for i in self.issues if i.severity == "error"),
            "warning_count": sum(1 for i in self.issues if i.severity == "warning"),
            "issues": [i.to_dict() for i in self.issues],
        }
    
    def xǁValidationReportǁto_dict__mutmut_6(self) -> Dict[str, Any]:
        return {
            "file_path": self.file_path,
            "passed": self.passed,
            "TIER1_PASSED": self.tier1_passed,
            "tier2_passed": self.tier2_passed,
            "tier3_passed": self.tier3_passed,
            "total_issues": len(self.issues),
            "error_count": sum(1 for i in self.issues if i.severity == "error"),
            "warning_count": sum(1 for i in self.issues if i.severity == "warning"),
            "issues": [i.to_dict() for i in self.issues],
        }
    
    def xǁValidationReportǁto_dict__mutmut_7(self) -> Dict[str, Any]:
        return {
            "file_path": self.file_path,
            "passed": self.passed,
            "tier1_passed": self.tier1_passed,
            "XXtier2_passedXX": self.tier2_passed,
            "tier3_passed": self.tier3_passed,
            "total_issues": len(self.issues),
            "error_count": sum(1 for i in self.issues if i.severity == "error"),
            "warning_count": sum(1 for i in self.issues if i.severity == "warning"),
            "issues": [i.to_dict() for i in self.issues],
        }
    
    def xǁValidationReportǁto_dict__mutmut_8(self) -> Dict[str, Any]:
        return {
            "file_path": self.file_path,
            "passed": self.passed,
            "tier1_passed": self.tier1_passed,
            "TIER2_PASSED": self.tier2_passed,
            "tier3_passed": self.tier3_passed,
            "total_issues": len(self.issues),
            "error_count": sum(1 for i in self.issues if i.severity == "error"),
            "warning_count": sum(1 for i in self.issues if i.severity == "warning"),
            "issues": [i.to_dict() for i in self.issues],
        }
    
    def xǁValidationReportǁto_dict__mutmut_9(self) -> Dict[str, Any]:
        return {
            "file_path": self.file_path,
            "passed": self.passed,
            "tier1_passed": self.tier1_passed,
            "tier2_passed": self.tier2_passed,
            "XXtier3_passedXX": self.tier3_passed,
            "total_issues": len(self.issues),
            "error_count": sum(1 for i in self.issues if i.severity == "error"),
            "warning_count": sum(1 for i in self.issues if i.severity == "warning"),
            "issues": [i.to_dict() for i in self.issues],
        }
    
    def xǁValidationReportǁto_dict__mutmut_10(self) -> Dict[str, Any]:
        return {
            "file_path": self.file_path,
            "passed": self.passed,
            "tier1_passed": self.tier1_passed,
            "tier2_passed": self.tier2_passed,
            "TIER3_PASSED": self.tier3_passed,
            "total_issues": len(self.issues),
            "error_count": sum(1 for i in self.issues if i.severity == "error"),
            "warning_count": sum(1 for i in self.issues if i.severity == "warning"),
            "issues": [i.to_dict() for i in self.issues],
        }
    
    def xǁValidationReportǁto_dict__mutmut_11(self) -> Dict[str, Any]:
        return {
            "file_path": self.file_path,
            "passed": self.passed,
            "tier1_passed": self.tier1_passed,
            "tier2_passed": self.tier2_passed,
            "tier3_passed": self.tier3_passed,
            "XXtotal_issuesXX": len(self.issues),
            "error_count": sum(1 for i in self.issues if i.severity == "error"),
            "warning_count": sum(1 for i in self.issues if i.severity == "warning"),
            "issues": [i.to_dict() for i in self.issues],
        }
    
    def xǁValidationReportǁto_dict__mutmut_12(self) -> Dict[str, Any]:
        return {
            "file_path": self.file_path,
            "passed": self.passed,
            "tier1_passed": self.tier1_passed,
            "tier2_passed": self.tier2_passed,
            "tier3_passed": self.tier3_passed,
            "TOTAL_ISSUES": len(self.issues),
            "error_count": sum(1 for i in self.issues if i.severity == "error"),
            "warning_count": sum(1 for i in self.issues if i.severity == "warning"),
            "issues": [i.to_dict() for i in self.issues],
        }
    
    def xǁValidationReportǁto_dict__mutmut_13(self) -> Dict[str, Any]:
        return {
            "file_path": self.file_path,
            "passed": self.passed,
            "tier1_passed": self.tier1_passed,
            "tier2_passed": self.tier2_passed,
            "tier3_passed": self.tier3_passed,
            "total_issues": len(self.issues),
            "XXerror_countXX": sum(1 for i in self.issues if i.severity == "error"),
            "warning_count": sum(1 for i in self.issues if i.severity == "warning"),
            "issues": [i.to_dict() for i in self.issues],
        }
    
    def xǁValidationReportǁto_dict__mutmut_14(self) -> Dict[str, Any]:
        return {
            "file_path": self.file_path,
            "passed": self.passed,
            "tier1_passed": self.tier1_passed,
            "tier2_passed": self.tier2_passed,
            "tier3_passed": self.tier3_passed,
            "total_issues": len(self.issues),
            "ERROR_COUNT": sum(1 for i in self.issues if i.severity == "error"),
            "warning_count": sum(1 for i in self.issues if i.severity == "warning"),
            "issues": [i.to_dict() for i in self.issues],
        }
    
    def xǁValidationReportǁto_dict__mutmut_15(self) -> Dict[str, Any]:
        return {
            "file_path": self.file_path,
            "passed": self.passed,
            "tier1_passed": self.tier1_passed,
            "tier2_passed": self.tier2_passed,
            "tier3_passed": self.tier3_passed,
            "total_issues": len(self.issues),
            "error_count": sum(None),
            "warning_count": sum(1 for i in self.issues if i.severity == "warning"),
            "issues": [i.to_dict() for i in self.issues],
        }
    
    def xǁValidationReportǁto_dict__mutmut_16(self) -> Dict[str, Any]:
        return {
            "file_path": self.file_path,
            "passed": self.passed,
            "tier1_passed": self.tier1_passed,
            "tier2_passed": self.tier2_passed,
            "tier3_passed": self.tier3_passed,
            "total_issues": len(self.issues),
            "error_count": sum(2 for i in self.issues if i.severity == "error"),
            "warning_count": sum(1 for i in self.issues if i.severity == "warning"),
            "issues": [i.to_dict() for i in self.issues],
        }
    
    def xǁValidationReportǁto_dict__mutmut_17(self) -> Dict[str, Any]:
        return {
            "file_path": self.file_path,
            "passed": self.passed,
            "tier1_passed": self.tier1_passed,
            "tier2_passed": self.tier2_passed,
            "tier3_passed": self.tier3_passed,
            "total_issues": len(self.issues),
            "error_count": sum(1 for i in self.issues if i.severity != "error"),
            "warning_count": sum(1 for i in self.issues if i.severity == "warning"),
            "issues": [i.to_dict() for i in self.issues],
        }
    
    def xǁValidationReportǁto_dict__mutmut_18(self) -> Dict[str, Any]:
        return {
            "file_path": self.file_path,
            "passed": self.passed,
            "tier1_passed": self.tier1_passed,
            "tier2_passed": self.tier2_passed,
            "tier3_passed": self.tier3_passed,
            "total_issues": len(self.issues),
            "error_count": sum(1 for i in self.issues if i.severity == "XXerrorXX"),
            "warning_count": sum(1 for i in self.issues if i.severity == "warning"),
            "issues": [i.to_dict() for i in self.issues],
        }
    
    def xǁValidationReportǁto_dict__mutmut_19(self) -> Dict[str, Any]:
        return {
            "file_path": self.file_path,
            "passed": self.passed,
            "tier1_passed": self.tier1_passed,
            "tier2_passed": self.tier2_passed,
            "tier3_passed": self.tier3_passed,
            "total_issues": len(self.issues),
            "error_count": sum(1 for i in self.issues if i.severity == "ERROR"),
            "warning_count": sum(1 for i in self.issues if i.severity == "warning"),
            "issues": [i.to_dict() for i in self.issues],
        }
    
    def xǁValidationReportǁto_dict__mutmut_20(self) -> Dict[str, Any]:
        return {
            "file_path": self.file_path,
            "passed": self.passed,
            "tier1_passed": self.tier1_passed,
            "tier2_passed": self.tier2_passed,
            "tier3_passed": self.tier3_passed,
            "total_issues": len(self.issues),
            "error_count": sum(1 for i in self.issues if i.severity == "error"),
            "XXwarning_countXX": sum(1 for i in self.issues if i.severity == "warning"),
            "issues": [i.to_dict() for i in self.issues],
        }
    
    def xǁValidationReportǁto_dict__mutmut_21(self) -> Dict[str, Any]:
        return {
            "file_path": self.file_path,
            "passed": self.passed,
            "tier1_passed": self.tier1_passed,
            "tier2_passed": self.tier2_passed,
            "tier3_passed": self.tier3_passed,
            "total_issues": len(self.issues),
            "error_count": sum(1 for i in self.issues if i.severity == "error"),
            "WARNING_COUNT": sum(1 for i in self.issues if i.severity == "warning"),
            "issues": [i.to_dict() for i in self.issues],
        }
    
    def xǁValidationReportǁto_dict__mutmut_22(self) -> Dict[str, Any]:
        return {
            "file_path": self.file_path,
            "passed": self.passed,
            "tier1_passed": self.tier1_passed,
            "tier2_passed": self.tier2_passed,
            "tier3_passed": self.tier3_passed,
            "total_issues": len(self.issues),
            "error_count": sum(1 for i in self.issues if i.severity == "error"),
            "warning_count": sum(None),
            "issues": [i.to_dict() for i in self.issues],
        }
    
    def xǁValidationReportǁto_dict__mutmut_23(self) -> Dict[str, Any]:
        return {
            "file_path": self.file_path,
            "passed": self.passed,
            "tier1_passed": self.tier1_passed,
            "tier2_passed": self.tier2_passed,
            "tier3_passed": self.tier3_passed,
            "total_issues": len(self.issues),
            "error_count": sum(1 for i in self.issues if i.severity == "error"),
            "warning_count": sum(2 for i in self.issues if i.severity == "warning"),
            "issues": [i.to_dict() for i in self.issues],
        }
    
    def xǁValidationReportǁto_dict__mutmut_24(self) -> Dict[str, Any]:
        return {
            "file_path": self.file_path,
            "passed": self.passed,
            "tier1_passed": self.tier1_passed,
            "tier2_passed": self.tier2_passed,
            "tier3_passed": self.tier3_passed,
            "total_issues": len(self.issues),
            "error_count": sum(1 for i in self.issues if i.severity == "error"),
            "warning_count": sum(1 for i in self.issues if i.severity != "warning"),
            "issues": [i.to_dict() for i in self.issues],
        }
    
    def xǁValidationReportǁto_dict__mutmut_25(self) -> Dict[str, Any]:
        return {
            "file_path": self.file_path,
            "passed": self.passed,
            "tier1_passed": self.tier1_passed,
            "tier2_passed": self.tier2_passed,
            "tier3_passed": self.tier3_passed,
            "total_issues": len(self.issues),
            "error_count": sum(1 for i in self.issues if i.severity == "error"),
            "warning_count": sum(1 for i in self.issues if i.severity == "XXwarningXX"),
            "issues": [i.to_dict() for i in self.issues],
        }
    
    def xǁValidationReportǁto_dict__mutmut_26(self) -> Dict[str, Any]:
        return {
            "file_path": self.file_path,
            "passed": self.passed,
            "tier1_passed": self.tier1_passed,
            "tier2_passed": self.tier2_passed,
            "tier3_passed": self.tier3_passed,
            "total_issues": len(self.issues),
            "error_count": sum(1 for i in self.issues if i.severity == "error"),
            "warning_count": sum(1 for i in self.issues if i.severity == "WARNING"),
            "issues": [i.to_dict() for i in self.issues],
        }
    
    def xǁValidationReportǁto_dict__mutmut_27(self) -> Dict[str, Any]:
        return {
            "file_path": self.file_path,
            "passed": self.passed,
            "tier1_passed": self.tier1_passed,
            "tier2_passed": self.tier2_passed,
            "tier3_passed": self.tier3_passed,
            "total_issues": len(self.issues),
            "error_count": sum(1 for i in self.issues if i.severity == "error"),
            "warning_count": sum(1 for i in self.issues if i.severity == "warning"),
            "XXissuesXX": [i.to_dict() for i in self.issues],
        }
    
    def xǁValidationReportǁto_dict__mutmut_28(self) -> Dict[str, Any]:
        return {
            "file_path": self.file_path,
            "passed": self.passed,
            "tier1_passed": self.tier1_passed,
            "tier2_passed": self.tier2_passed,
            "tier3_passed": self.tier3_passed,
            "total_issues": len(self.issues),
            "error_count": sum(1 for i in self.issues if i.severity == "error"),
            "warning_count": sum(1 for i in self.issues if i.severity == "warning"),
            "ISSUES": [i.to_dict() for i in self.issues],
        }
    
    xǁValidationReportǁto_dict__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁValidationReportǁto_dict__mutmut_1': xǁValidationReportǁto_dict__mutmut_1, 
        'xǁValidationReportǁto_dict__mutmut_2': xǁValidationReportǁto_dict__mutmut_2, 
        'xǁValidationReportǁto_dict__mutmut_3': xǁValidationReportǁto_dict__mutmut_3, 
        'xǁValidationReportǁto_dict__mutmut_4': xǁValidationReportǁto_dict__mutmut_4, 
        'xǁValidationReportǁto_dict__mutmut_5': xǁValidationReportǁto_dict__mutmut_5, 
        'xǁValidationReportǁto_dict__mutmut_6': xǁValidationReportǁto_dict__mutmut_6, 
        'xǁValidationReportǁto_dict__mutmut_7': xǁValidationReportǁto_dict__mutmut_7, 
        'xǁValidationReportǁto_dict__mutmut_8': xǁValidationReportǁto_dict__mutmut_8, 
        'xǁValidationReportǁto_dict__mutmut_9': xǁValidationReportǁto_dict__mutmut_9, 
        'xǁValidationReportǁto_dict__mutmut_10': xǁValidationReportǁto_dict__mutmut_10, 
        'xǁValidationReportǁto_dict__mutmut_11': xǁValidationReportǁto_dict__mutmut_11, 
        'xǁValidationReportǁto_dict__mutmut_12': xǁValidationReportǁto_dict__mutmut_12, 
        'xǁValidationReportǁto_dict__mutmut_13': xǁValidationReportǁto_dict__mutmut_13, 
        'xǁValidationReportǁto_dict__mutmut_14': xǁValidationReportǁto_dict__mutmut_14, 
        'xǁValidationReportǁto_dict__mutmut_15': xǁValidationReportǁto_dict__mutmut_15, 
        'xǁValidationReportǁto_dict__mutmut_16': xǁValidationReportǁto_dict__mutmut_16, 
        'xǁValidationReportǁto_dict__mutmut_17': xǁValidationReportǁto_dict__mutmut_17, 
        'xǁValidationReportǁto_dict__mutmut_18': xǁValidationReportǁto_dict__mutmut_18, 
        'xǁValidationReportǁto_dict__mutmut_19': xǁValidationReportǁto_dict__mutmut_19, 
        'xǁValidationReportǁto_dict__mutmut_20': xǁValidationReportǁto_dict__mutmut_20, 
        'xǁValidationReportǁto_dict__mutmut_21': xǁValidationReportǁto_dict__mutmut_21, 
        'xǁValidationReportǁto_dict__mutmut_22': xǁValidationReportǁto_dict__mutmut_22, 
        'xǁValidationReportǁto_dict__mutmut_23': xǁValidationReportǁto_dict__mutmut_23, 
        'xǁValidationReportǁto_dict__mutmut_24': xǁValidationReportǁto_dict__mutmut_24, 
        'xǁValidationReportǁto_dict__mutmut_25': xǁValidationReportǁto_dict__mutmut_25, 
        'xǁValidationReportǁto_dict__mutmut_26': xǁValidationReportǁto_dict__mutmut_26, 
        'xǁValidationReportǁto_dict__mutmut_27': xǁValidationReportǁto_dict__mutmut_27, 
        'xǁValidationReportǁto_dict__mutmut_28': xǁValidationReportǁto_dict__mutmut_28
    }
    xǁValidationReportǁto_dict__mutmut_orig.__name__ = 'xǁValidationReportǁto_dict'


class ValidatorAgent:
    """
    3-Tier Validation Engine for Django Migration.
    
    Usage:
        validator = ValidatorAgent()
        report = validator.validate_file(
            file_path="myapp/views.py",
            original_code="...",
            migrated_code="...",
            file_type="view",
            related_files={"myapp/models.py": "..."},
            dependency_graph={...}
        )
        
        if not report.passed:
            # Send issues back to Migration Agent for repair
            issues = report.to_dict()["issues"]
    """
    
    def __init__(self, enable_tier3: bool = False, llm_client=None):
        args = [enable_tier3, llm_client]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁValidatorAgentǁ__init____mutmut_orig'), object.__getattribute__(self, 'xǁValidatorAgentǁ__init____mutmut_mutants'), args, kwargs, self)
    
    def xǁValidatorAgentǁ__init____mutmut_orig(self, enable_tier3: bool = False, llm_client=None):
        """
        Args:
            enable_tier3: Enable LLM-powered semantic review (Tier 3)
            llm_client: LLM client for Tier 3 (Groq/Gemini instance)
        """
        self.enable_tier3 = enable_tier3
        self.llm_client = llm_client
        self.docker_client = None
        self.docker_error = None
        try:
            import docker
            self.docker_client = docker.from_env()
        except ImportError:
            self.docker_error = "Docker python module not installed (run `pip install docker`)"
        except Exception as e:
            self.docker_error = f"Error connecting to Docker daemon: {e}"
    
    def xǁValidatorAgentǁ__init____mutmut_1(self, enable_tier3: bool = True, llm_client=None):
        """
        Args:
            enable_tier3: Enable LLM-powered semantic review (Tier 3)
            llm_client: LLM client for Tier 3 (Groq/Gemini instance)
        """
        self.enable_tier3 = enable_tier3
        self.llm_client = llm_client
        self.docker_client = None
        self.docker_error = None
        try:
            import docker
            self.docker_client = docker.from_env()
        except ImportError:
            self.docker_error = "Docker python module not installed (run `pip install docker`)"
        except Exception as e:
            self.docker_error = f"Error connecting to Docker daemon: {e}"
    
    def xǁValidatorAgentǁ__init____mutmut_2(self, enable_tier3: bool = False, llm_client=None):
        """
        Args:
            enable_tier3: Enable LLM-powered semantic review (Tier 3)
            llm_client: LLM client for Tier 3 (Groq/Gemini instance)
        """
        self.enable_tier3 = None
        self.llm_client = llm_client
        self.docker_client = None
        self.docker_error = None
        try:
            import docker
            self.docker_client = docker.from_env()
        except ImportError:
            self.docker_error = "Docker python module not installed (run `pip install docker`)"
        except Exception as e:
            self.docker_error = f"Error connecting to Docker daemon: {e}"
    
    def xǁValidatorAgentǁ__init____mutmut_3(self, enable_tier3: bool = False, llm_client=None):
        """
        Args:
            enable_tier3: Enable LLM-powered semantic review (Tier 3)
            llm_client: LLM client for Tier 3 (Groq/Gemini instance)
        """
        self.enable_tier3 = enable_tier3
        self.llm_client = None
        self.docker_client = None
        self.docker_error = None
        try:
            import docker
            self.docker_client = docker.from_env()
        except ImportError:
            self.docker_error = "Docker python module not installed (run `pip install docker`)"
        except Exception as e:
            self.docker_error = f"Error connecting to Docker daemon: {e}"
    
    def xǁValidatorAgentǁ__init____mutmut_4(self, enable_tier3: bool = False, llm_client=None):
        """
        Args:
            enable_tier3: Enable LLM-powered semantic review (Tier 3)
            llm_client: LLM client for Tier 3 (Groq/Gemini instance)
        """
        self.enable_tier3 = enable_tier3
        self.llm_client = llm_client
        self.docker_client = ""
        self.docker_error = None
        try:
            import docker
            self.docker_client = docker.from_env()
        except ImportError:
            self.docker_error = "Docker python module not installed (run `pip install docker`)"
        except Exception as e:
            self.docker_error = f"Error connecting to Docker daemon: {e}"
    
    def xǁValidatorAgentǁ__init____mutmut_5(self, enable_tier3: bool = False, llm_client=None):
        """
        Args:
            enable_tier3: Enable LLM-powered semantic review (Tier 3)
            llm_client: LLM client for Tier 3 (Groq/Gemini instance)
        """
        self.enable_tier3 = enable_tier3
        self.llm_client = llm_client
        self.docker_client = None
        self.docker_error = ""
        try:
            import docker
            self.docker_client = docker.from_env()
        except ImportError:
            self.docker_error = "Docker python module not installed (run `pip install docker`)"
        except Exception as e:
            self.docker_error = f"Error connecting to Docker daemon: {e}"
    
    def xǁValidatorAgentǁ__init____mutmut_6(self, enable_tier3: bool = False, llm_client=None):
        """
        Args:
            enable_tier3: Enable LLM-powered semantic review (Tier 3)
            llm_client: LLM client for Tier 3 (Groq/Gemini instance)
        """
        self.enable_tier3 = enable_tier3
        self.llm_client = llm_client
        self.docker_client = None
        self.docker_error = None
        try:
            import docker
            self.docker_client = None
        except ImportError:
            self.docker_error = "Docker python module not installed (run `pip install docker`)"
        except Exception as e:
            self.docker_error = f"Error connecting to Docker daemon: {e}"
    
    def xǁValidatorAgentǁ__init____mutmut_7(self, enable_tier3: bool = False, llm_client=None):
        """
        Args:
            enable_tier3: Enable LLM-powered semantic review (Tier 3)
            llm_client: LLM client for Tier 3 (Groq/Gemini instance)
        """
        self.enable_tier3 = enable_tier3
        self.llm_client = llm_client
        self.docker_client = None
        self.docker_error = None
        try:
            import docker
            self.docker_client = docker.from_env()
        except ImportError:
            self.docker_error = None
        except Exception as e:
            self.docker_error = f"Error connecting to Docker daemon: {e}"
    
    def xǁValidatorAgentǁ__init____mutmut_8(self, enable_tier3: bool = False, llm_client=None):
        """
        Args:
            enable_tier3: Enable LLM-powered semantic review (Tier 3)
            llm_client: LLM client for Tier 3 (Groq/Gemini instance)
        """
        self.enable_tier3 = enable_tier3
        self.llm_client = llm_client
        self.docker_client = None
        self.docker_error = None
        try:
            import docker
            self.docker_client = docker.from_env()
        except ImportError:
            self.docker_error = "XXDocker python module not installed (run `pip install docker`)XX"
        except Exception as e:
            self.docker_error = f"Error connecting to Docker daemon: {e}"
    
    def xǁValidatorAgentǁ__init____mutmut_9(self, enable_tier3: bool = False, llm_client=None):
        """
        Args:
            enable_tier3: Enable LLM-powered semantic review (Tier 3)
            llm_client: LLM client for Tier 3 (Groq/Gemini instance)
        """
        self.enable_tier3 = enable_tier3
        self.llm_client = llm_client
        self.docker_client = None
        self.docker_error = None
        try:
            import docker
            self.docker_client = docker.from_env()
        except ImportError:
            self.docker_error = "docker python module not installed (run `pip install docker`)"
        except Exception as e:
            self.docker_error = f"Error connecting to Docker daemon: {e}"
    
    def xǁValidatorAgentǁ__init____mutmut_10(self, enable_tier3: bool = False, llm_client=None):
        """
        Args:
            enable_tier3: Enable LLM-powered semantic review (Tier 3)
            llm_client: LLM client for Tier 3 (Groq/Gemini instance)
        """
        self.enable_tier3 = enable_tier3
        self.llm_client = llm_client
        self.docker_client = None
        self.docker_error = None
        try:
            import docker
            self.docker_client = docker.from_env()
        except ImportError:
            self.docker_error = "DOCKER PYTHON MODULE NOT INSTALLED (RUN `PIP INSTALL DOCKER`)"
        except Exception as e:
            self.docker_error = f"Error connecting to Docker daemon: {e}"
    
    def xǁValidatorAgentǁ__init____mutmut_11(self, enable_tier3: bool = False, llm_client=None):
        """
        Args:
            enable_tier3: Enable LLM-powered semantic review (Tier 3)
            llm_client: LLM client for Tier 3 (Groq/Gemini instance)
        """
        self.enable_tier3 = enable_tier3
        self.llm_client = llm_client
        self.docker_client = None
        self.docker_error = None
        try:
            import docker
            self.docker_client = docker.from_env()
        except ImportError:
            self.docker_error = "Docker python module not installed (run `pip install docker`)"
        except Exception as e:
            self.docker_error = None
    
    xǁValidatorAgentǁ__init____mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁValidatorAgentǁ__init____mutmut_1': xǁValidatorAgentǁ__init____mutmut_1, 
        'xǁValidatorAgentǁ__init____mutmut_2': xǁValidatorAgentǁ__init____mutmut_2, 
        'xǁValidatorAgentǁ__init____mutmut_3': xǁValidatorAgentǁ__init____mutmut_3, 
        'xǁValidatorAgentǁ__init____mutmut_4': xǁValidatorAgentǁ__init____mutmut_4, 
        'xǁValidatorAgentǁ__init____mutmut_5': xǁValidatorAgentǁ__init____mutmut_5, 
        'xǁValidatorAgentǁ__init____mutmut_6': xǁValidatorAgentǁ__init____mutmut_6, 
        'xǁValidatorAgentǁ__init____mutmut_7': xǁValidatorAgentǁ__init____mutmut_7, 
        'xǁValidatorAgentǁ__init____mutmut_8': xǁValidatorAgentǁ__init____mutmut_8, 
        'xǁValidatorAgentǁ__init____mutmut_9': xǁValidatorAgentǁ__init____mutmut_9, 
        'xǁValidatorAgentǁ__init____mutmut_10': xǁValidatorAgentǁ__init____mutmut_10, 
        'xǁValidatorAgentǁ__init____mutmut_11': xǁValidatorAgentǁ__init____mutmut_11
    }
    xǁValidatorAgentǁ__init____mutmut_orig.__name__ = 'xǁValidatorAgentǁ__init__'
    
    def validate_file(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        args = [file_path, original_code, migrated_code, file_type, related_files, dependency_graph]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁValidatorAgentǁvalidate_file__mutmut_orig'), object.__getattribute__(self, 'xǁValidatorAgentǁvalidate_file__mutmut_mutants'), args, kwargs, self)
    
    def xǁValidatorAgentǁvalidate_file__mutmut_orig(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, migrated_code,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_1(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "XXunknownXX",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, migrated_code,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_2(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "UNKNOWN",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, migrated_code,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_3(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = None
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, migrated_code,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_4(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(None)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, migrated_code,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_5(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(None, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, migrated_code,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_6(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, None, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, migrated_code,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_7(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, None, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, migrated_code,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_8(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, None)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, migrated_code,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_9(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, migrated_code,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_10(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, migrated_code,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_11(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, migrated_code,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_12(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, )
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, migrated_code,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_13(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                None, file_path, original_code, migrated_code,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_14(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, None, original_code, migrated_code,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_15(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, None, migrated_code,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_16(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, None,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_17(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, migrated_code,
                None, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_18(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, migrated_code,
                file_type, None, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_19(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, migrated_code,
                file_type, related_files, None
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_20(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                file_path, original_code, migrated_code,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_21(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, original_code, migrated_code,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_22(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, migrated_code,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_23(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_24(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, migrated_code,
                related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_25(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, migrated_code,
                file_type, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_26(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, migrated_code,
                file_type, related_files, )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_27(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, migrated_code,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 or self.llm_client:
            self._run_tier3(
                report, file_path, original_code, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_28(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, migrated_code,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                None, file_path, original_code, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_29(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, migrated_code,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, None, original_code, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_30(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, migrated_code,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, None, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_31(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, migrated_code,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, None,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_32(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, migrated_code,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, migrated_code,
                None, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_33(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, migrated_code,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, migrated_code,
                file_type, None
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_34(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, migrated_code,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                file_path, original_code, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_35(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, migrated_code,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, original_code, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_36(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, migrated_code,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, migrated_code,
                file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_37(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, migrated_code,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, file_type, related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_38(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, migrated_code,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, migrated_code,
                related_files
            )
        
        return report
    
    def xǁValidatorAgentǁvalidate_file__mutmut_39(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str = "unknown",
        related_files: Optional[Dict[str, str]] = None,
        dependency_graph: Optional[Dict] = None,
    ) -> ValidationReport:
        """
        Run all validation tiers on a migrated file.
        
        Args:
            file_path: Path to the file
            original_code: Original Django 2.x code
            migrated_code: Migrated Django 5.x code
            file_type: Django file type (model, view, urls, etc.)
            related_files: Dict of {path: migrated_content} for related files
            dependency_graph: Full dependency graph from analyzer
            
        Returns:
            ValidationReport with all issues found
        """
        report = ValidationReport(file_path)
        
        # Tier 1: Static Validation
        self._run_tier1(report, file_path, migrated_code, file_type)
        
        # Tier 2: Cross-File Consistency
        if related_files:
            self._run_tier2(
                report, file_path, original_code, migrated_code,
                file_type, related_files, dependency_graph
            )
        
        # Tier 3: LLM Semantic Review (optional)
        if self.enable_tier3 and self.llm_client:
            self._run_tier3(
                report, file_path, original_code, migrated_code,
                file_type, )
        
        return report
    
    xǁValidatorAgentǁvalidate_file__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁValidatorAgentǁvalidate_file__mutmut_1': xǁValidatorAgentǁvalidate_file__mutmut_1, 
        'xǁValidatorAgentǁvalidate_file__mutmut_2': xǁValidatorAgentǁvalidate_file__mutmut_2, 
        'xǁValidatorAgentǁvalidate_file__mutmut_3': xǁValidatorAgentǁvalidate_file__mutmut_3, 
        'xǁValidatorAgentǁvalidate_file__mutmut_4': xǁValidatorAgentǁvalidate_file__mutmut_4, 
        'xǁValidatorAgentǁvalidate_file__mutmut_5': xǁValidatorAgentǁvalidate_file__mutmut_5, 
        'xǁValidatorAgentǁvalidate_file__mutmut_6': xǁValidatorAgentǁvalidate_file__mutmut_6, 
        'xǁValidatorAgentǁvalidate_file__mutmut_7': xǁValidatorAgentǁvalidate_file__mutmut_7, 
        'xǁValidatorAgentǁvalidate_file__mutmut_8': xǁValidatorAgentǁvalidate_file__mutmut_8, 
        'xǁValidatorAgentǁvalidate_file__mutmut_9': xǁValidatorAgentǁvalidate_file__mutmut_9, 
        'xǁValidatorAgentǁvalidate_file__mutmut_10': xǁValidatorAgentǁvalidate_file__mutmut_10, 
        'xǁValidatorAgentǁvalidate_file__mutmut_11': xǁValidatorAgentǁvalidate_file__mutmut_11, 
        'xǁValidatorAgentǁvalidate_file__mutmut_12': xǁValidatorAgentǁvalidate_file__mutmut_12, 
        'xǁValidatorAgentǁvalidate_file__mutmut_13': xǁValidatorAgentǁvalidate_file__mutmut_13, 
        'xǁValidatorAgentǁvalidate_file__mutmut_14': xǁValidatorAgentǁvalidate_file__mutmut_14, 
        'xǁValidatorAgentǁvalidate_file__mutmut_15': xǁValidatorAgentǁvalidate_file__mutmut_15, 
        'xǁValidatorAgentǁvalidate_file__mutmut_16': xǁValidatorAgentǁvalidate_file__mutmut_16, 
        'xǁValidatorAgentǁvalidate_file__mutmut_17': xǁValidatorAgentǁvalidate_file__mutmut_17, 
        'xǁValidatorAgentǁvalidate_file__mutmut_18': xǁValidatorAgentǁvalidate_file__mutmut_18, 
        'xǁValidatorAgentǁvalidate_file__mutmut_19': xǁValidatorAgentǁvalidate_file__mutmut_19, 
        'xǁValidatorAgentǁvalidate_file__mutmut_20': xǁValidatorAgentǁvalidate_file__mutmut_20, 
        'xǁValidatorAgentǁvalidate_file__mutmut_21': xǁValidatorAgentǁvalidate_file__mutmut_21, 
        'xǁValidatorAgentǁvalidate_file__mutmut_22': xǁValidatorAgentǁvalidate_file__mutmut_22, 
        'xǁValidatorAgentǁvalidate_file__mutmut_23': xǁValidatorAgentǁvalidate_file__mutmut_23, 
        'xǁValidatorAgentǁvalidate_file__mutmut_24': xǁValidatorAgentǁvalidate_file__mutmut_24, 
        'xǁValidatorAgentǁvalidate_file__mutmut_25': xǁValidatorAgentǁvalidate_file__mutmut_25, 
        'xǁValidatorAgentǁvalidate_file__mutmut_26': xǁValidatorAgentǁvalidate_file__mutmut_26, 
        'xǁValidatorAgentǁvalidate_file__mutmut_27': xǁValidatorAgentǁvalidate_file__mutmut_27, 
        'xǁValidatorAgentǁvalidate_file__mutmut_28': xǁValidatorAgentǁvalidate_file__mutmut_28, 
        'xǁValidatorAgentǁvalidate_file__mutmut_29': xǁValidatorAgentǁvalidate_file__mutmut_29, 
        'xǁValidatorAgentǁvalidate_file__mutmut_30': xǁValidatorAgentǁvalidate_file__mutmut_30, 
        'xǁValidatorAgentǁvalidate_file__mutmut_31': xǁValidatorAgentǁvalidate_file__mutmut_31, 
        'xǁValidatorAgentǁvalidate_file__mutmut_32': xǁValidatorAgentǁvalidate_file__mutmut_32, 
        'xǁValidatorAgentǁvalidate_file__mutmut_33': xǁValidatorAgentǁvalidate_file__mutmut_33, 
        'xǁValidatorAgentǁvalidate_file__mutmut_34': xǁValidatorAgentǁvalidate_file__mutmut_34, 
        'xǁValidatorAgentǁvalidate_file__mutmut_35': xǁValidatorAgentǁvalidate_file__mutmut_35, 
        'xǁValidatorAgentǁvalidate_file__mutmut_36': xǁValidatorAgentǁvalidate_file__mutmut_36, 
        'xǁValidatorAgentǁvalidate_file__mutmut_37': xǁValidatorAgentǁvalidate_file__mutmut_37, 
        'xǁValidatorAgentǁvalidate_file__mutmut_38': xǁValidatorAgentǁvalidate_file__mutmut_38, 
        'xǁValidatorAgentǁvalidate_file__mutmut_39': xǁValidatorAgentǁvalidate_file__mutmut_39
    }
    xǁValidatorAgentǁvalidate_file__mutmut_orig.__name__ = 'xǁValidatorAgentǁvalidate_file'
    
    def validate_batch(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        args = [migration_context, enable_tier3]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁValidatorAgentǁvalidate_batch__mutmut_orig'), object.__getattribute__(self, 'xǁValidatorAgentǁvalidate_batch__mutmut_mutants'), args, kwargs, self)
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_orig(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_1(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = True,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_2(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = None
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_3(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = None
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_4(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(None, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_5(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, None)
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_6(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get("")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_7(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, )
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_8(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "XXXX")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_9(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = None
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_10(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(None, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_11(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, None)
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_12(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get("unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_13(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, )
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_14(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "XXunknownXX")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_15(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "UNKNOWN")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_16(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = None
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_17(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(None)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_18(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = None
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_19(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=None,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_20(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=None,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_21(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=None,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_22(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=None,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_23(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=None,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_24(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=None,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_25(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_26(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_27(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_28(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_29(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_30(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_31(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = None
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_32(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(None, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_33(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, None)
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_34(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_35(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, )
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_36(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log(None)
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_37(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("XX[VALIDATOR] Running Tier 4 Containerized Sandbox...XX")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_38(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[validator] running tier 4 containerized sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_39(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] RUNNING TIER 4 CONTAINERIZED SANDBOX...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_40(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = None
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_41(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(None)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_42(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(None)
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_43(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = None
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_44(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = None
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_45(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target or target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_46(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target not in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_47(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(None)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_48(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = None
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_49(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(None)
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_50(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(None))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_51(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(None)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_52(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(None, report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_53(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, None)

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_54(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(report.to_dict())

        return reports
    
    def xǁValidatorAgentǁvalidate_batch__mutmut_55(
        self,
        migration_context,  # MigrationContext instance
        enable_tier3: bool = False,
    ) -> Dict[str, ValidationReport]:
        """Validate all migrated files in a batch using shared context."""
        reports = {}
        
        for file_path, migrated_code in migration_context.migrated_files.items():
            original_code = migration_context.original_files.get(file_path, "")
            file_type = migration_context.file_types.get(file_path, "unknown")
            related_files = migration_context.get_related_files(file_path)
            
            report = self.validate_file(
                file_path=file_path,
                original_code=original_code,
                migrated_code=migrated_code,
                file_type=file_type,
                related_files=related_files,
                dependency_graph=migration_context.dependency_graph,
            )
            
            reports[file_path] = report
            migration_context.add_validation_result(file_path, report.to_dict())
        
        # Tier 4: Containerized Runtime Testing
        if self.docker_client:
            migration_context.add_log("[VALIDATOR] Running Tier 4 Containerized Sandbox...")
            tier4_issues = self._run_tier4_docker(migration_context)
        else:
            migration_context.add_log(f"[VALIDATOR] Tier 4 Containerized Sandbox SKIPPED ({self.docker_error})")
            tier4_issues = []
            
        if tier4_issues:
            for issue in tier4_issues:
                target = issue.file_path
                if target and target in reports:
                    reports[target].add_issue(issue)
                elif reports:
                    # If we can't map it, attach to first available report
                    first = next(iter(reports.keys()))
                    reports[first].add_issue(issue)

            # Re-sync modified reports to context
            for path, report in reports.items():
                migration_context.add_validation_result(path, )

        return reports
    
    xǁValidatorAgentǁvalidate_batch__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁValidatorAgentǁvalidate_batch__mutmut_1': xǁValidatorAgentǁvalidate_batch__mutmut_1, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_2': xǁValidatorAgentǁvalidate_batch__mutmut_2, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_3': xǁValidatorAgentǁvalidate_batch__mutmut_3, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_4': xǁValidatorAgentǁvalidate_batch__mutmut_4, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_5': xǁValidatorAgentǁvalidate_batch__mutmut_5, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_6': xǁValidatorAgentǁvalidate_batch__mutmut_6, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_7': xǁValidatorAgentǁvalidate_batch__mutmut_7, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_8': xǁValidatorAgentǁvalidate_batch__mutmut_8, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_9': xǁValidatorAgentǁvalidate_batch__mutmut_9, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_10': xǁValidatorAgentǁvalidate_batch__mutmut_10, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_11': xǁValidatorAgentǁvalidate_batch__mutmut_11, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_12': xǁValidatorAgentǁvalidate_batch__mutmut_12, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_13': xǁValidatorAgentǁvalidate_batch__mutmut_13, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_14': xǁValidatorAgentǁvalidate_batch__mutmut_14, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_15': xǁValidatorAgentǁvalidate_batch__mutmut_15, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_16': xǁValidatorAgentǁvalidate_batch__mutmut_16, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_17': xǁValidatorAgentǁvalidate_batch__mutmut_17, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_18': xǁValidatorAgentǁvalidate_batch__mutmut_18, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_19': xǁValidatorAgentǁvalidate_batch__mutmut_19, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_20': xǁValidatorAgentǁvalidate_batch__mutmut_20, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_21': xǁValidatorAgentǁvalidate_batch__mutmut_21, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_22': xǁValidatorAgentǁvalidate_batch__mutmut_22, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_23': xǁValidatorAgentǁvalidate_batch__mutmut_23, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_24': xǁValidatorAgentǁvalidate_batch__mutmut_24, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_25': xǁValidatorAgentǁvalidate_batch__mutmut_25, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_26': xǁValidatorAgentǁvalidate_batch__mutmut_26, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_27': xǁValidatorAgentǁvalidate_batch__mutmut_27, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_28': xǁValidatorAgentǁvalidate_batch__mutmut_28, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_29': xǁValidatorAgentǁvalidate_batch__mutmut_29, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_30': xǁValidatorAgentǁvalidate_batch__mutmut_30, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_31': xǁValidatorAgentǁvalidate_batch__mutmut_31, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_32': xǁValidatorAgentǁvalidate_batch__mutmut_32, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_33': xǁValidatorAgentǁvalidate_batch__mutmut_33, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_34': xǁValidatorAgentǁvalidate_batch__mutmut_34, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_35': xǁValidatorAgentǁvalidate_batch__mutmut_35, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_36': xǁValidatorAgentǁvalidate_batch__mutmut_36, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_37': xǁValidatorAgentǁvalidate_batch__mutmut_37, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_38': xǁValidatorAgentǁvalidate_batch__mutmut_38, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_39': xǁValidatorAgentǁvalidate_batch__mutmut_39, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_40': xǁValidatorAgentǁvalidate_batch__mutmut_40, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_41': xǁValidatorAgentǁvalidate_batch__mutmut_41, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_42': xǁValidatorAgentǁvalidate_batch__mutmut_42, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_43': xǁValidatorAgentǁvalidate_batch__mutmut_43, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_44': xǁValidatorAgentǁvalidate_batch__mutmut_44, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_45': xǁValidatorAgentǁvalidate_batch__mutmut_45, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_46': xǁValidatorAgentǁvalidate_batch__mutmut_46, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_47': xǁValidatorAgentǁvalidate_batch__mutmut_47, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_48': xǁValidatorAgentǁvalidate_batch__mutmut_48, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_49': xǁValidatorAgentǁvalidate_batch__mutmut_49, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_50': xǁValidatorAgentǁvalidate_batch__mutmut_50, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_51': xǁValidatorAgentǁvalidate_batch__mutmut_51, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_52': xǁValidatorAgentǁvalidate_batch__mutmut_52, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_53': xǁValidatorAgentǁvalidate_batch__mutmut_53, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_54': xǁValidatorAgentǁvalidate_batch__mutmut_54, 
        'xǁValidatorAgentǁvalidate_batch__mutmut_55': xǁValidatorAgentǁvalidate_batch__mutmut_55
    }
    xǁValidatorAgentǁvalidate_batch__mutmut_orig.__name__ = 'xǁValidatorAgentǁvalidate_batch'
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def _run_tier1(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        args = [report, file_path, code, file_type]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁValidatorAgentǁ_run_tier1__mutmut_orig'), object.__getattribute__(self, 'xǁValidatorAgentǁ_run_tier1__mutmut_mutants'), args, kwargs, self)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_orig(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_1(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(None, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_2(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, None, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_3(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, None)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_4(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_5(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_6(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, )
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_7(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(None, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_8(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, None, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_9(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, None)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_10(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_11(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_12(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, )
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_13(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(None, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_14(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, None, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_15(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, None)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_16(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_17(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_18(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, )
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_19(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(None, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_20(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, None, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_21(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, None, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_22(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, None)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_23(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_24(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_25(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_26(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, )
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_27(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(None, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_28(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, None, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_29(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, None, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_30(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, None)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_31(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_32(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_33(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_34(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, )
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_35(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(None, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_36(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, None, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_37(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, None, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_38(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, None)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_39(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_40(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, code, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_41(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, file_type)
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_42(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, )
        self._check_url_patterns(report, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_43(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(None, file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_44(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, None, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_45(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, None, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_46(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, None)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_47(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(file_path, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_48(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, code, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_49(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, file_type)
    # ─── TIER 1: Static Validation ─────────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier1__mutmut_50(
        self,
        report: ValidationReport,
        file_path: str,
        code: str,
        file_type: str,
    ):
        """Run per-file static checks."""
        self._check_syntax(report, file_path, code)
        self._check_deprecated_imports(report, file_path, code)
        self._check_removed_apis(report, file_path, code)
        self._check_foreignkey_on_delete(report, file_path, code, file_type)
        self._check_middleware_pattern(report, file_path, code, file_type)
        self._check_removed_settings(report, file_path, code, file_type)
        self._check_url_patterns(report, file_path, code, )
    
    xǁValidatorAgentǁ_run_tier1__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁValidatorAgentǁ_run_tier1__mutmut_1': xǁValidatorAgentǁ_run_tier1__mutmut_1, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_2': xǁValidatorAgentǁ_run_tier1__mutmut_2, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_3': xǁValidatorAgentǁ_run_tier1__mutmut_3, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_4': xǁValidatorAgentǁ_run_tier1__mutmut_4, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_5': xǁValidatorAgentǁ_run_tier1__mutmut_5, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_6': xǁValidatorAgentǁ_run_tier1__mutmut_6, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_7': xǁValidatorAgentǁ_run_tier1__mutmut_7, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_8': xǁValidatorAgentǁ_run_tier1__mutmut_8, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_9': xǁValidatorAgentǁ_run_tier1__mutmut_9, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_10': xǁValidatorAgentǁ_run_tier1__mutmut_10, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_11': xǁValidatorAgentǁ_run_tier1__mutmut_11, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_12': xǁValidatorAgentǁ_run_tier1__mutmut_12, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_13': xǁValidatorAgentǁ_run_tier1__mutmut_13, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_14': xǁValidatorAgentǁ_run_tier1__mutmut_14, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_15': xǁValidatorAgentǁ_run_tier1__mutmut_15, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_16': xǁValidatorAgentǁ_run_tier1__mutmut_16, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_17': xǁValidatorAgentǁ_run_tier1__mutmut_17, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_18': xǁValidatorAgentǁ_run_tier1__mutmut_18, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_19': xǁValidatorAgentǁ_run_tier1__mutmut_19, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_20': xǁValidatorAgentǁ_run_tier1__mutmut_20, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_21': xǁValidatorAgentǁ_run_tier1__mutmut_21, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_22': xǁValidatorAgentǁ_run_tier1__mutmut_22, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_23': xǁValidatorAgentǁ_run_tier1__mutmut_23, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_24': xǁValidatorAgentǁ_run_tier1__mutmut_24, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_25': xǁValidatorAgentǁ_run_tier1__mutmut_25, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_26': xǁValidatorAgentǁ_run_tier1__mutmut_26, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_27': xǁValidatorAgentǁ_run_tier1__mutmut_27, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_28': xǁValidatorAgentǁ_run_tier1__mutmut_28, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_29': xǁValidatorAgentǁ_run_tier1__mutmut_29, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_30': xǁValidatorAgentǁ_run_tier1__mutmut_30, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_31': xǁValidatorAgentǁ_run_tier1__mutmut_31, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_32': xǁValidatorAgentǁ_run_tier1__mutmut_32, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_33': xǁValidatorAgentǁ_run_tier1__mutmut_33, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_34': xǁValidatorAgentǁ_run_tier1__mutmut_34, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_35': xǁValidatorAgentǁ_run_tier1__mutmut_35, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_36': xǁValidatorAgentǁ_run_tier1__mutmut_36, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_37': xǁValidatorAgentǁ_run_tier1__mutmut_37, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_38': xǁValidatorAgentǁ_run_tier1__mutmut_38, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_39': xǁValidatorAgentǁ_run_tier1__mutmut_39, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_40': xǁValidatorAgentǁ_run_tier1__mutmut_40, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_41': xǁValidatorAgentǁ_run_tier1__mutmut_41, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_42': xǁValidatorAgentǁ_run_tier1__mutmut_42, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_43': xǁValidatorAgentǁ_run_tier1__mutmut_43, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_44': xǁValidatorAgentǁ_run_tier1__mutmut_44, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_45': xǁValidatorAgentǁ_run_tier1__mutmut_45, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_46': xǁValidatorAgentǁ_run_tier1__mutmut_46, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_47': xǁValidatorAgentǁ_run_tier1__mutmut_47, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_48': xǁValidatorAgentǁ_run_tier1__mutmut_48, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_49': xǁValidatorAgentǁ_run_tier1__mutmut_49, 
        'xǁValidatorAgentǁ_run_tier1__mutmut_50': xǁValidatorAgentǁ_run_tier1__mutmut_50
    }
    xǁValidatorAgentǁ_run_tier1__mutmut_orig.__name__ = 'xǁValidatorAgentǁ_run_tier1'
    
    def _check_syntax(self, report: ValidationReport, file_path: str, code: str):
        args = [report, file_path, code]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁValidatorAgentǁ_check_syntax__mutmut_orig'), object.__getattribute__(self, 'xǁValidatorAgentǁ_check_syntax__mutmut_mutants'), args, kwargs, self)
    
    def xǁValidatorAgentǁ_check_syntax__mutmut_orig(self, report: ValidationReport, file_path: str, code: str):
        """Check if migrated code is valid Python."""
        try:
            ast.parse(code)
        except SyntaxError as e:
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="syntax",
                message=f"Syntax error: {e.msg}",
                file_path=file_path,
                line_number=e.lineno,
                fix_hint=f"Fix syntax error at line {e.lineno}: {e.text.strip() if e.text else ''}",
                auto_fixable=False,
            ))
    
    def xǁValidatorAgentǁ_check_syntax__mutmut_1(self, report: ValidationReport, file_path: str, code: str):
        """Check if migrated code is valid Python."""
        try:
            ast.parse(None)
        except SyntaxError as e:
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="syntax",
                message=f"Syntax error: {e.msg}",
                file_path=file_path,
                line_number=e.lineno,
                fix_hint=f"Fix syntax error at line {e.lineno}: {e.text.strip() if e.text else ''}",
                auto_fixable=False,
            ))
    
    def xǁValidatorAgentǁ_check_syntax__mutmut_2(self, report: ValidationReport, file_path: str, code: str):
        """Check if migrated code is valid Python."""
        try:
            ast.parse(code)
        except SyntaxError as e:
            report.add_issue(None)
    
    def xǁValidatorAgentǁ_check_syntax__mutmut_3(self, report: ValidationReport, file_path: str, code: str):
        """Check if migrated code is valid Python."""
        try:
            ast.parse(code)
        except SyntaxError as e:
            report.add_issue(ValidationIssue(
                tier=None,
                severity="error",
                category="syntax",
                message=f"Syntax error: {e.msg}",
                file_path=file_path,
                line_number=e.lineno,
                fix_hint=f"Fix syntax error at line {e.lineno}: {e.text.strip() if e.text else ''}",
                auto_fixable=False,
            ))
    
    def xǁValidatorAgentǁ_check_syntax__mutmut_4(self, report: ValidationReport, file_path: str, code: str):
        """Check if migrated code is valid Python."""
        try:
            ast.parse(code)
        except SyntaxError as e:
            report.add_issue(ValidationIssue(
                tier=1,
                severity=None,
                category="syntax",
                message=f"Syntax error: {e.msg}",
                file_path=file_path,
                line_number=e.lineno,
                fix_hint=f"Fix syntax error at line {e.lineno}: {e.text.strip() if e.text else ''}",
                auto_fixable=False,
            ))
    
    def xǁValidatorAgentǁ_check_syntax__mutmut_5(self, report: ValidationReport, file_path: str, code: str):
        """Check if migrated code is valid Python."""
        try:
            ast.parse(code)
        except SyntaxError as e:
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category=None,
                message=f"Syntax error: {e.msg}",
                file_path=file_path,
                line_number=e.lineno,
                fix_hint=f"Fix syntax error at line {e.lineno}: {e.text.strip() if e.text else ''}",
                auto_fixable=False,
            ))
    
    def xǁValidatorAgentǁ_check_syntax__mutmut_6(self, report: ValidationReport, file_path: str, code: str):
        """Check if migrated code is valid Python."""
        try:
            ast.parse(code)
        except SyntaxError as e:
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="syntax",
                message=None,
                file_path=file_path,
                line_number=e.lineno,
                fix_hint=f"Fix syntax error at line {e.lineno}: {e.text.strip() if e.text else ''}",
                auto_fixable=False,
            ))
    
    def xǁValidatorAgentǁ_check_syntax__mutmut_7(self, report: ValidationReport, file_path: str, code: str):
        """Check if migrated code is valid Python."""
        try:
            ast.parse(code)
        except SyntaxError as e:
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="syntax",
                message=f"Syntax error: {e.msg}",
                file_path=None,
                line_number=e.lineno,
                fix_hint=f"Fix syntax error at line {e.lineno}: {e.text.strip() if e.text else ''}",
                auto_fixable=False,
            ))
    
    def xǁValidatorAgentǁ_check_syntax__mutmut_8(self, report: ValidationReport, file_path: str, code: str):
        """Check if migrated code is valid Python."""
        try:
            ast.parse(code)
        except SyntaxError as e:
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="syntax",
                message=f"Syntax error: {e.msg}",
                file_path=file_path,
                line_number=None,
                fix_hint=f"Fix syntax error at line {e.lineno}: {e.text.strip() if e.text else ''}",
                auto_fixable=False,
            ))
    
    def xǁValidatorAgentǁ_check_syntax__mutmut_9(self, report: ValidationReport, file_path: str, code: str):
        """Check if migrated code is valid Python."""
        try:
            ast.parse(code)
        except SyntaxError as e:
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="syntax",
                message=f"Syntax error: {e.msg}",
                file_path=file_path,
                line_number=e.lineno,
                fix_hint=None,
                auto_fixable=False,
            ))
    
    def xǁValidatorAgentǁ_check_syntax__mutmut_10(self, report: ValidationReport, file_path: str, code: str):
        """Check if migrated code is valid Python."""
        try:
            ast.parse(code)
        except SyntaxError as e:
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="syntax",
                message=f"Syntax error: {e.msg}",
                file_path=file_path,
                line_number=e.lineno,
                fix_hint=f"Fix syntax error at line {e.lineno}: {e.text.strip() if e.text else ''}",
                auto_fixable=None,
            ))
    
    def xǁValidatorAgentǁ_check_syntax__mutmut_11(self, report: ValidationReport, file_path: str, code: str):
        """Check if migrated code is valid Python."""
        try:
            ast.parse(code)
        except SyntaxError as e:
            report.add_issue(ValidationIssue(
                severity="error",
                category="syntax",
                message=f"Syntax error: {e.msg}",
                file_path=file_path,
                line_number=e.lineno,
                fix_hint=f"Fix syntax error at line {e.lineno}: {e.text.strip() if e.text else ''}",
                auto_fixable=False,
            ))
    
    def xǁValidatorAgentǁ_check_syntax__mutmut_12(self, report: ValidationReport, file_path: str, code: str):
        """Check if migrated code is valid Python."""
        try:
            ast.parse(code)
        except SyntaxError as e:
            report.add_issue(ValidationIssue(
                tier=1,
                category="syntax",
                message=f"Syntax error: {e.msg}",
                file_path=file_path,
                line_number=e.lineno,
                fix_hint=f"Fix syntax error at line {e.lineno}: {e.text.strip() if e.text else ''}",
                auto_fixable=False,
            ))
    
    def xǁValidatorAgentǁ_check_syntax__mutmut_13(self, report: ValidationReport, file_path: str, code: str):
        """Check if migrated code is valid Python."""
        try:
            ast.parse(code)
        except SyntaxError as e:
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                message=f"Syntax error: {e.msg}",
                file_path=file_path,
                line_number=e.lineno,
                fix_hint=f"Fix syntax error at line {e.lineno}: {e.text.strip() if e.text else ''}",
                auto_fixable=False,
            ))
    
    def xǁValidatorAgentǁ_check_syntax__mutmut_14(self, report: ValidationReport, file_path: str, code: str):
        """Check if migrated code is valid Python."""
        try:
            ast.parse(code)
        except SyntaxError as e:
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="syntax",
                file_path=file_path,
                line_number=e.lineno,
                fix_hint=f"Fix syntax error at line {e.lineno}: {e.text.strip() if e.text else ''}",
                auto_fixable=False,
            ))
    
    def xǁValidatorAgentǁ_check_syntax__mutmut_15(self, report: ValidationReport, file_path: str, code: str):
        """Check if migrated code is valid Python."""
        try:
            ast.parse(code)
        except SyntaxError as e:
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="syntax",
                message=f"Syntax error: {e.msg}",
                line_number=e.lineno,
                fix_hint=f"Fix syntax error at line {e.lineno}: {e.text.strip() if e.text else ''}",
                auto_fixable=False,
            ))
    
    def xǁValidatorAgentǁ_check_syntax__mutmut_16(self, report: ValidationReport, file_path: str, code: str):
        """Check if migrated code is valid Python."""
        try:
            ast.parse(code)
        except SyntaxError as e:
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="syntax",
                message=f"Syntax error: {e.msg}",
                file_path=file_path,
                fix_hint=f"Fix syntax error at line {e.lineno}: {e.text.strip() if e.text else ''}",
                auto_fixable=False,
            ))
    
    def xǁValidatorAgentǁ_check_syntax__mutmut_17(self, report: ValidationReport, file_path: str, code: str):
        """Check if migrated code is valid Python."""
        try:
            ast.parse(code)
        except SyntaxError as e:
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="syntax",
                message=f"Syntax error: {e.msg}",
                file_path=file_path,
                line_number=e.lineno,
                auto_fixable=False,
            ))
    
    def xǁValidatorAgentǁ_check_syntax__mutmut_18(self, report: ValidationReport, file_path: str, code: str):
        """Check if migrated code is valid Python."""
        try:
            ast.parse(code)
        except SyntaxError as e:
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="syntax",
                message=f"Syntax error: {e.msg}",
                file_path=file_path,
                line_number=e.lineno,
                fix_hint=f"Fix syntax error at line {e.lineno}: {e.text.strip() if e.text else ''}",
                ))
    
    def xǁValidatorAgentǁ_check_syntax__mutmut_19(self, report: ValidationReport, file_path: str, code: str):
        """Check if migrated code is valid Python."""
        try:
            ast.parse(code)
        except SyntaxError as e:
            report.add_issue(ValidationIssue(
                tier=2,
                severity="error",
                category="syntax",
                message=f"Syntax error: {e.msg}",
                file_path=file_path,
                line_number=e.lineno,
                fix_hint=f"Fix syntax error at line {e.lineno}: {e.text.strip() if e.text else ''}",
                auto_fixable=False,
            ))
    
    def xǁValidatorAgentǁ_check_syntax__mutmut_20(self, report: ValidationReport, file_path: str, code: str):
        """Check if migrated code is valid Python."""
        try:
            ast.parse(code)
        except SyntaxError as e:
            report.add_issue(ValidationIssue(
                tier=1,
                severity="XXerrorXX",
                category="syntax",
                message=f"Syntax error: {e.msg}",
                file_path=file_path,
                line_number=e.lineno,
                fix_hint=f"Fix syntax error at line {e.lineno}: {e.text.strip() if e.text else ''}",
                auto_fixable=False,
            ))
    
    def xǁValidatorAgentǁ_check_syntax__mutmut_21(self, report: ValidationReport, file_path: str, code: str):
        """Check if migrated code is valid Python."""
        try:
            ast.parse(code)
        except SyntaxError as e:
            report.add_issue(ValidationIssue(
                tier=1,
                severity="ERROR",
                category="syntax",
                message=f"Syntax error: {e.msg}",
                file_path=file_path,
                line_number=e.lineno,
                fix_hint=f"Fix syntax error at line {e.lineno}: {e.text.strip() if e.text else ''}",
                auto_fixable=False,
            ))
    
    def xǁValidatorAgentǁ_check_syntax__mutmut_22(self, report: ValidationReport, file_path: str, code: str):
        """Check if migrated code is valid Python."""
        try:
            ast.parse(code)
        except SyntaxError as e:
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="XXsyntaxXX",
                message=f"Syntax error: {e.msg}",
                file_path=file_path,
                line_number=e.lineno,
                fix_hint=f"Fix syntax error at line {e.lineno}: {e.text.strip() if e.text else ''}",
                auto_fixable=False,
            ))
    
    def xǁValidatorAgentǁ_check_syntax__mutmut_23(self, report: ValidationReport, file_path: str, code: str):
        """Check if migrated code is valid Python."""
        try:
            ast.parse(code)
        except SyntaxError as e:
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="SYNTAX",
                message=f"Syntax error: {e.msg}",
                file_path=file_path,
                line_number=e.lineno,
                fix_hint=f"Fix syntax error at line {e.lineno}: {e.text.strip() if e.text else ''}",
                auto_fixable=False,
            ))
    
    def xǁValidatorAgentǁ_check_syntax__mutmut_24(self, report: ValidationReport, file_path: str, code: str):
        """Check if migrated code is valid Python."""
        try:
            ast.parse(code)
        except SyntaxError as e:
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="syntax",
                message=f"Syntax error: {e.msg}",
                file_path=file_path,
                line_number=e.lineno,
                fix_hint=f"Fix syntax error at line {e.lineno}: {e.text.strip() if e.text else 'XXXX'}",
                auto_fixable=False,
            ))
    
    def xǁValidatorAgentǁ_check_syntax__mutmut_25(self, report: ValidationReport, file_path: str, code: str):
        """Check if migrated code is valid Python."""
        try:
            ast.parse(code)
        except SyntaxError as e:
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="syntax",
                message=f"Syntax error: {e.msg}",
                file_path=file_path,
                line_number=e.lineno,
                fix_hint=f"Fix syntax error at line {e.lineno}: {e.text.strip() if e.text else ''}",
                auto_fixable=True,
            ))
    
    xǁValidatorAgentǁ_check_syntax__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁValidatorAgentǁ_check_syntax__mutmut_1': xǁValidatorAgentǁ_check_syntax__mutmut_1, 
        'xǁValidatorAgentǁ_check_syntax__mutmut_2': xǁValidatorAgentǁ_check_syntax__mutmut_2, 
        'xǁValidatorAgentǁ_check_syntax__mutmut_3': xǁValidatorAgentǁ_check_syntax__mutmut_3, 
        'xǁValidatorAgentǁ_check_syntax__mutmut_4': xǁValidatorAgentǁ_check_syntax__mutmut_4, 
        'xǁValidatorAgentǁ_check_syntax__mutmut_5': xǁValidatorAgentǁ_check_syntax__mutmut_5, 
        'xǁValidatorAgentǁ_check_syntax__mutmut_6': xǁValidatorAgentǁ_check_syntax__mutmut_6, 
        'xǁValidatorAgentǁ_check_syntax__mutmut_7': xǁValidatorAgentǁ_check_syntax__mutmut_7, 
        'xǁValidatorAgentǁ_check_syntax__mutmut_8': xǁValidatorAgentǁ_check_syntax__mutmut_8, 
        'xǁValidatorAgentǁ_check_syntax__mutmut_9': xǁValidatorAgentǁ_check_syntax__mutmut_9, 
        'xǁValidatorAgentǁ_check_syntax__mutmut_10': xǁValidatorAgentǁ_check_syntax__mutmut_10, 
        'xǁValidatorAgentǁ_check_syntax__mutmut_11': xǁValidatorAgentǁ_check_syntax__mutmut_11, 
        'xǁValidatorAgentǁ_check_syntax__mutmut_12': xǁValidatorAgentǁ_check_syntax__mutmut_12, 
        'xǁValidatorAgentǁ_check_syntax__mutmut_13': xǁValidatorAgentǁ_check_syntax__mutmut_13, 
        'xǁValidatorAgentǁ_check_syntax__mutmut_14': xǁValidatorAgentǁ_check_syntax__mutmut_14, 
        'xǁValidatorAgentǁ_check_syntax__mutmut_15': xǁValidatorAgentǁ_check_syntax__mutmut_15, 
        'xǁValidatorAgentǁ_check_syntax__mutmut_16': xǁValidatorAgentǁ_check_syntax__mutmut_16, 
        'xǁValidatorAgentǁ_check_syntax__mutmut_17': xǁValidatorAgentǁ_check_syntax__mutmut_17, 
        'xǁValidatorAgentǁ_check_syntax__mutmut_18': xǁValidatorAgentǁ_check_syntax__mutmut_18, 
        'xǁValidatorAgentǁ_check_syntax__mutmut_19': xǁValidatorAgentǁ_check_syntax__mutmut_19, 
        'xǁValidatorAgentǁ_check_syntax__mutmut_20': xǁValidatorAgentǁ_check_syntax__mutmut_20, 
        'xǁValidatorAgentǁ_check_syntax__mutmut_21': xǁValidatorAgentǁ_check_syntax__mutmut_21, 
        'xǁValidatorAgentǁ_check_syntax__mutmut_22': xǁValidatorAgentǁ_check_syntax__mutmut_22, 
        'xǁValidatorAgentǁ_check_syntax__mutmut_23': xǁValidatorAgentǁ_check_syntax__mutmut_23, 
        'xǁValidatorAgentǁ_check_syntax__mutmut_24': xǁValidatorAgentǁ_check_syntax__mutmut_24, 
        'xǁValidatorAgentǁ_check_syntax__mutmut_25': xǁValidatorAgentǁ_check_syntax__mutmut_25
    }
    xǁValidatorAgentǁ_check_syntax__mutmut_orig.__name__ = 'xǁValidatorAgentǁ_check_syntax'
    
    def _check_deprecated_imports(self, report: ValidationReport, file_path: str, code: str):
        args = [report, file_path, code]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_orig'), object.__getattribute__(self, 'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_mutants'), args, kwargs, self)
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_orig(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="import",
                            message=f"Deprecated import still present: from {node.module} import {alias.name}",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=fix,
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_1(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = None
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="import",
                            message=f"Deprecated import still present: from {node.module} import {alias.name}",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=fix,
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_2(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(None)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="import",
                            message=f"Deprecated import still present: from {node.module} import {alias.name}",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=fix,
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_3(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(None):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="import",
                            message=f"Deprecated import still present: from {node.module} import {alias.name}",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=fix,
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_4(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) or node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="import",
                            message=f"Deprecated import still present: from {node.module} import {alias.name}",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=fix,
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_5(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = None
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="import",
                            message=f"Deprecated import still present: from {node.module} import {alias.name}",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=fix,
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_6(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key not in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="import",
                            message=f"Deprecated import still present: from {node.module} import {alias.name}",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=fix,
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_7(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = None
                        if new_module is None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="import",
                            message=f"Deprecated import still present: from {node.module} import {alias.name}",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=fix,
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_8(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is not None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="import",
                            message=f"Deprecated import still present: from {node.module} import {alias.name}",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=fix,
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_9(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is None:
                            fix = None
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="import",
                            message=f"Deprecated import still present: from {node.module} import {alias.name}",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=fix,
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_10(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = None
                        
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="import",
                            message=f"Deprecated import still present: from {node.module} import {alias.name}",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=fix,
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_11(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(None)
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_12(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(ValidationIssue(
                            tier=None,
                            severity="error",
                            category="import",
                            message=f"Deprecated import still present: from {node.module} import {alias.name}",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=fix,
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_13(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity=None,
                            category="import",
                            message=f"Deprecated import still present: from {node.module} import {alias.name}",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=fix,
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_14(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category=None,
                            message=f"Deprecated import still present: from {node.module} import {alias.name}",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=fix,
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_15(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="import",
                            message=None,
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=fix,
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_16(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="import",
                            message=f"Deprecated import still present: from {node.module} import {alias.name}",
                            file_path=None,
                            line_number=node.lineno,
                            fix_hint=fix,
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_17(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="import",
                            message=f"Deprecated import still present: from {node.module} import {alias.name}",
                            file_path=file_path,
                            line_number=None,
                            fix_hint=fix,
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_18(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="import",
                            message=f"Deprecated import still present: from {node.module} import {alias.name}",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=None,
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_19(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="import",
                            message=f"Deprecated import still present: from {node.module} import {alias.name}",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=fix,
                            auto_fixable=None,
                        ))
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_20(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(ValidationIssue(
                            severity="error",
                            category="import",
                            message=f"Deprecated import still present: from {node.module} import {alias.name}",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=fix,
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_21(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(ValidationIssue(
                            tier=1,
                            category="import",
                            message=f"Deprecated import still present: from {node.module} import {alias.name}",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=fix,
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_22(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            message=f"Deprecated import still present: from {node.module} import {alias.name}",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=fix,
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_23(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="import",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=fix,
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_24(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="import",
                            message=f"Deprecated import still present: from {node.module} import {alias.name}",
                            line_number=node.lineno,
                            fix_hint=fix,
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_25(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="import",
                            message=f"Deprecated import still present: from {node.module} import {alias.name}",
                            file_path=file_path,
                            fix_hint=fix,
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_26(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="import",
                            message=f"Deprecated import still present: from {node.module} import {alias.name}",
                            file_path=file_path,
                            line_number=node.lineno,
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_27(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="import",
                            message=f"Deprecated import still present: from {node.module} import {alias.name}",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=fix,
                            ))
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_28(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(ValidationIssue(
                            tier=2,
                            severity="error",
                            category="import",
                            message=f"Deprecated import still present: from {node.module} import {alias.name}",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=fix,
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_29(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="XXerrorXX",
                            category="import",
                            message=f"Deprecated import still present: from {node.module} import {alias.name}",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=fix,
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_30(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="ERROR",
                            category="import",
                            message=f"Deprecated import still present: from {node.module} import {alias.name}",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=fix,
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_31(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="XXimportXX",
                            message=f"Deprecated import still present: from {node.module} import {alias.name}",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=fix,
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_32(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="IMPORT",
                            message=f"Deprecated import still present: from {node.module} import {alias.name}",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=fix,
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_deprecated_imports__mutmut_33(self, report: ValidationReport, file_path: str, code: str):
        """Check for Django 2.x deprecated imports that should have been migrated."""
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return  # Already caught in syntax check
        
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                for alias in node.names:
                    key = (node.module, alias.name)
                    if key in DEPRECATED_IMPORTS:
                        new_module, new_name = DEPRECATED_IMPORTS[key]
                        if new_module is None:
                            fix = f"Remove import — '{alias.name}' was removed in Django 3+"
                        else:
                            fix = f"Change to: from {new_module} import {new_name}"
                        
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="import",
                            message=f"Deprecated import still present: from {node.module} import {alias.name}",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=fix,
                            auto_fixable=False,
                        ))
    
    xǁValidatorAgentǁ_check_deprecated_imports__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_1': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_1, 
        'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_2': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_2, 
        'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_3': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_3, 
        'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_4': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_4, 
        'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_5': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_5, 
        'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_6': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_6, 
        'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_7': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_7, 
        'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_8': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_8, 
        'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_9': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_9, 
        'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_10': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_10, 
        'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_11': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_11, 
        'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_12': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_12, 
        'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_13': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_13, 
        'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_14': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_14, 
        'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_15': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_15, 
        'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_16': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_16, 
        'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_17': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_17, 
        'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_18': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_18, 
        'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_19': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_19, 
        'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_20': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_20, 
        'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_21': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_21, 
        'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_22': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_22, 
        'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_23': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_23, 
        'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_24': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_24, 
        'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_25': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_25, 
        'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_26': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_26, 
        'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_27': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_27, 
        'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_28': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_28, 
        'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_29': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_29, 
        'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_30': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_30, 
        'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_31': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_31, 
        'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_32': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_32, 
        'xǁValidatorAgentǁ_check_deprecated_imports__mutmut_33': xǁValidatorAgentǁ_check_deprecated_imports__mutmut_33
    }
    xǁValidatorAgentǁ_check_deprecated_imports__mutmut_orig.__name__ = 'xǁValidatorAgentǁ_check_deprecated_imports'
    
    def _check_removed_apis(self, report: ValidationReport, file_path: str, code: str):
        args = [report, file_path, code]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁValidatorAgentǁ_check_removed_apis__mutmut_orig'), object.__getattribute__(self, 'xǁValidatorAgentǁ_check_removed_apis__mutmut_mutants'), args, kwargs, self)
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_orig(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(pattern, code):
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed function still used: {func_name}()",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {func_name}() with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_1(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = None
            for match in re.finditer(pattern, code):
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed function still used: {func_name}()",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {func_name}() with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_2(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(None, code):
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed function still used: {func_name}()",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {func_name}() with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_3(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(pattern, None):
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed function still used: {func_name}()",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {func_name}() with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_4(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(code):
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed function still used: {func_name}()",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {func_name}() with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_5(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(pattern, ):
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed function still used: {func_name}()",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {func_name}() with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_6(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(pattern, code):
                line_num = None
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed function still used: {func_name}()",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {func_name}() with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_7(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(pattern, code):
                line_num = code[:match.start()].count('\n') - 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed function still used: {func_name}()",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {func_name}() with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_8(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(pattern, code):
                line_num = code[:match.start()].count(None) + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed function still used: {func_name}()",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {func_name}() with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_9(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(pattern, code):
                line_num = code[:match.start()].count('XX\nXX') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed function still used: {func_name}()",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {func_name}() with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_10(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(pattern, code):
                line_num = code[:match.start()].count('\n') + 2
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed function still used: {func_name}()",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {func_name}() with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_11(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(pattern, code):
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(None)
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_12(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(pattern, code):
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=None,
                    severity="error",
                    category="api_compat",
                    message=f"Removed function still used: {func_name}()",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {func_name}() with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_13(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(pattern, code):
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity=None,
                    category="api_compat",
                    message=f"Removed function still used: {func_name}()",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {func_name}() with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_14(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(pattern, code):
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category=None,
                    message=f"Removed function still used: {func_name}()",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {func_name}() with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_15(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(pattern, code):
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=None,
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {func_name}() with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_16(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(pattern, code):
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed function still used: {func_name}()",
                    file_path=None,
                    line_number=line_num,
                    fix_hint=f"Replace {func_name}() with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_17(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(pattern, code):
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed function still used: {func_name}()",
                    file_path=file_path,
                    line_number=None,
                    fix_hint=f"Replace {func_name}() with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_18(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(pattern, code):
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed function still used: {func_name}()",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=None,
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_19(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(pattern, code):
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed function still used: {func_name}()",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {func_name}() with its Django 5.x equivalent",
                    auto_fixable=None,
                ))
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_20(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(pattern, code):
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    severity="error",
                    category="api_compat",
                    message=f"Removed function still used: {func_name}()",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {func_name}() with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_21(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(pattern, code):
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    category="api_compat",
                    message=f"Removed function still used: {func_name}()",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {func_name}() with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_22(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(pattern, code):
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    message=f"Removed function still used: {func_name}()",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {func_name}() with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_23(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(pattern, code):
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {func_name}() with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_24(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(pattern, code):
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed function still used: {func_name}()",
                    line_number=line_num,
                    fix_hint=f"Replace {func_name}() with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_25(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(pattern, code):
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed function still used: {func_name}()",
                    file_path=file_path,
                    fix_hint=f"Replace {func_name}() with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_26(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(pattern, code):
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed function still used: {func_name}()",
                    file_path=file_path,
                    line_number=line_num,
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_27(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(pattern, code):
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed function still used: {func_name}()",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {func_name}() with its Django 5.x equivalent",
                    ))
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_28(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(pattern, code):
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=2,
                    severity="error",
                    category="api_compat",
                    message=f"Removed function still used: {func_name}()",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {func_name}() with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_29(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(pattern, code):
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="XXerrorXX",
                    category="api_compat",
                    message=f"Removed function still used: {func_name}()",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {func_name}() with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_30(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(pattern, code):
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="ERROR",
                    category="api_compat",
                    message=f"Removed function still used: {func_name}()",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {func_name}() with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_31(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(pattern, code):
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="XXapi_compatXX",
                    message=f"Removed function still used: {func_name}()",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {func_name}() with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_32(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(pattern, code):
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="API_COMPAT",
                    message=f"Removed function still used: {func_name}()",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {func_name}() with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_apis__mutmut_33(self, report: ValidationReport, file_path: str, code: str):
        """Check for removed Django function calls."""
        for func_name in DJANGO5_REMOVED_FUNCTIONS:
            pattern = rf'\b{func_name}\s*\('
            for match in re.finditer(pattern, code):
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed function still used: {func_name}()",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {func_name}() with its Django 5.x equivalent",
                    auto_fixable=False,
                ))
    
    xǁValidatorAgentǁ_check_removed_apis__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁValidatorAgentǁ_check_removed_apis__mutmut_1': xǁValidatorAgentǁ_check_removed_apis__mutmut_1, 
        'xǁValidatorAgentǁ_check_removed_apis__mutmut_2': xǁValidatorAgentǁ_check_removed_apis__mutmut_2, 
        'xǁValidatorAgentǁ_check_removed_apis__mutmut_3': xǁValidatorAgentǁ_check_removed_apis__mutmut_3, 
        'xǁValidatorAgentǁ_check_removed_apis__mutmut_4': xǁValidatorAgentǁ_check_removed_apis__mutmut_4, 
        'xǁValidatorAgentǁ_check_removed_apis__mutmut_5': xǁValidatorAgentǁ_check_removed_apis__mutmut_5, 
        'xǁValidatorAgentǁ_check_removed_apis__mutmut_6': xǁValidatorAgentǁ_check_removed_apis__mutmut_6, 
        'xǁValidatorAgentǁ_check_removed_apis__mutmut_7': xǁValidatorAgentǁ_check_removed_apis__mutmut_7, 
        'xǁValidatorAgentǁ_check_removed_apis__mutmut_8': xǁValidatorAgentǁ_check_removed_apis__mutmut_8, 
        'xǁValidatorAgentǁ_check_removed_apis__mutmut_9': xǁValidatorAgentǁ_check_removed_apis__mutmut_9, 
        'xǁValidatorAgentǁ_check_removed_apis__mutmut_10': xǁValidatorAgentǁ_check_removed_apis__mutmut_10, 
        'xǁValidatorAgentǁ_check_removed_apis__mutmut_11': xǁValidatorAgentǁ_check_removed_apis__mutmut_11, 
        'xǁValidatorAgentǁ_check_removed_apis__mutmut_12': xǁValidatorAgentǁ_check_removed_apis__mutmut_12, 
        'xǁValidatorAgentǁ_check_removed_apis__mutmut_13': xǁValidatorAgentǁ_check_removed_apis__mutmut_13, 
        'xǁValidatorAgentǁ_check_removed_apis__mutmut_14': xǁValidatorAgentǁ_check_removed_apis__mutmut_14, 
        'xǁValidatorAgentǁ_check_removed_apis__mutmut_15': xǁValidatorAgentǁ_check_removed_apis__mutmut_15, 
        'xǁValidatorAgentǁ_check_removed_apis__mutmut_16': xǁValidatorAgentǁ_check_removed_apis__mutmut_16, 
        'xǁValidatorAgentǁ_check_removed_apis__mutmut_17': xǁValidatorAgentǁ_check_removed_apis__mutmut_17, 
        'xǁValidatorAgentǁ_check_removed_apis__mutmut_18': xǁValidatorAgentǁ_check_removed_apis__mutmut_18, 
        'xǁValidatorAgentǁ_check_removed_apis__mutmut_19': xǁValidatorAgentǁ_check_removed_apis__mutmut_19, 
        'xǁValidatorAgentǁ_check_removed_apis__mutmut_20': xǁValidatorAgentǁ_check_removed_apis__mutmut_20, 
        'xǁValidatorAgentǁ_check_removed_apis__mutmut_21': xǁValidatorAgentǁ_check_removed_apis__mutmut_21, 
        'xǁValidatorAgentǁ_check_removed_apis__mutmut_22': xǁValidatorAgentǁ_check_removed_apis__mutmut_22, 
        'xǁValidatorAgentǁ_check_removed_apis__mutmut_23': xǁValidatorAgentǁ_check_removed_apis__mutmut_23, 
        'xǁValidatorAgentǁ_check_removed_apis__mutmut_24': xǁValidatorAgentǁ_check_removed_apis__mutmut_24, 
        'xǁValidatorAgentǁ_check_removed_apis__mutmut_25': xǁValidatorAgentǁ_check_removed_apis__mutmut_25, 
        'xǁValidatorAgentǁ_check_removed_apis__mutmut_26': xǁValidatorAgentǁ_check_removed_apis__mutmut_26, 
        'xǁValidatorAgentǁ_check_removed_apis__mutmut_27': xǁValidatorAgentǁ_check_removed_apis__mutmut_27, 
        'xǁValidatorAgentǁ_check_removed_apis__mutmut_28': xǁValidatorAgentǁ_check_removed_apis__mutmut_28, 
        'xǁValidatorAgentǁ_check_removed_apis__mutmut_29': xǁValidatorAgentǁ_check_removed_apis__mutmut_29, 
        'xǁValidatorAgentǁ_check_removed_apis__mutmut_30': xǁValidatorAgentǁ_check_removed_apis__mutmut_30, 
        'xǁValidatorAgentǁ_check_removed_apis__mutmut_31': xǁValidatorAgentǁ_check_removed_apis__mutmut_31, 
        'xǁValidatorAgentǁ_check_removed_apis__mutmut_32': xǁValidatorAgentǁ_check_removed_apis__mutmut_32, 
        'xǁValidatorAgentǁ_check_removed_apis__mutmut_33': xǁValidatorAgentǁ_check_removed_apis__mutmut_33
    }
    xǁValidatorAgentǁ_check_removed_apis__mutmut_orig.__name__ = 'xǁValidatorAgentǁ_check_removed_apis'
    
    def _check_foreignkey_on_delete(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        args = [report, file_path, code, file_type]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_orig'), object.__getattribute__(self, 'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_mutants'), args, kwargs, self)
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_orig(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_1(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_2(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("XXmodelXX", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_3(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("MODEL", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_4(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "XXmodelsXX", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_5(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "MODELS", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_6(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "XXunknownXX"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_7(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "UNKNOWN"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_8(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = None
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_9(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(None)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_10(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(None):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_11(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = None
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_12(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = "XXXX"
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_13(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = None
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_14(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = None
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_15(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name not in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_16(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = None
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_17(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        None
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_18(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg != "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_19(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "XXon_deleteXX" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_20(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "ON_DELETE" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_21(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete or len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_22(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_23(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) <= 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_24(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 3:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_25(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(None)
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_26(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=None,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_27(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity=None,
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_28(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category=None,
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_29(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=None,
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_30(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=None,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_31(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=None,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_32(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=None,
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_33(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=None,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_34(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_35(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_36(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_37(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_38(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_39(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_40(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_41(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_42(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=2,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_43(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="XXerrorXX",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_44(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="ERROR",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_45(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="XXapi_compatXX",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_46(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="API_COMPAT",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=True,
                        ))
    
    def xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_47(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check that ForeignKey/OneToOneField has on_delete parameter."""
        if file_type not in ("model", "models", "unknown"):
            return
        
        try:
            tree = ast.parse(code)
        except SyntaxError:
            return
        
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                func_name = ""
                if isinstance(node.func, ast.Attribute):
                    func_name = node.func.attr
                elif isinstance(node.func, ast.Name):
                    func_name = node.func.id
                
                if func_name in FOREIGNKEY_FIELDS:
                    has_on_delete = any(
                        kw.arg == "on_delete" for kw in node.keywords
                    )
                    # Also check positional args (on_delete can be 2nd positional)
                    if not has_on_delete and len(node.args) < 2:
                        report.add_issue(ValidationIssue(
                            tier=1,
                            severity="error",
                            category="api_compat",
                            message=f"{func_name} missing required 'on_delete' parameter",
                            file_path=file_path,
                            line_number=node.lineno,
                            fix_hint=f"Add on_delete=models.CASCADE (or appropriate action) to {func_name}",
                            auto_fixable=False,
                        ))
    
    xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_1': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_1, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_2': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_2, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_3': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_3, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_4': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_4, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_5': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_5, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_6': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_6, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_7': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_7, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_8': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_8, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_9': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_9, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_10': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_10, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_11': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_11, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_12': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_12, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_13': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_13, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_14': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_14, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_15': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_15, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_16': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_16, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_17': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_17, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_18': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_18, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_19': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_19, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_20': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_20, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_21': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_21, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_22': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_22, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_23': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_23, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_24': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_24, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_25': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_25, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_26': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_26, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_27': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_27, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_28': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_28, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_29': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_29, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_30': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_30, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_31': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_31, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_32': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_32, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_33': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_33, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_34': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_34, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_35': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_35, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_36': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_36, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_37': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_37, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_38': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_38, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_39': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_39, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_40': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_40, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_41': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_41, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_42': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_42, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_43': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_43, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_44': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_44, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_45': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_45, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_46': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_46, 
        'xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_47': xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_47
    }
    xǁValidatorAgentǁ_check_foreignkey_on_delete__mutmut_orig.__name__ = 'xǁValidatorAgentǁ_check_foreignkey_on_delete'
    
    def _check_middleware_pattern(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        args = [report, file_path, code, file_type]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_orig'), object.__getattribute__(self, 'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_mutants'), args, kwargs, self)
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_orig(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_1(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_2(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("XXmiddlewareXX", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_3(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("MIDDLEWARE", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_4(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "XXunknownXX"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_5(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "UNKNOWN"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_6(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code and "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_7(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "XXprocess_requestXX" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_8(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "PROCESS_REQUEST" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_9(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" not in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_10(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "XXprocess_responseXX" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_11(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "PROCESS_RESPONSE" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_12(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" not in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_13(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code or "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_14(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "XXget_responseXX" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_15(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "GET_RESPONSE" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_16(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_17(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "XX__call__XX" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_18(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__CALL__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_19(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_20(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(None)
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_21(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=None,
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_22(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity=None,
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_23(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category=None,
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_24(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=None,
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_25(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=None,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_26(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint=None,
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_27(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=None,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_28(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_29(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_30(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_31(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_32(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_33(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_34(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_35(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=2,
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_36(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="XXerrorXX",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_37(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="ERROR",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_38(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="XXapi_compatXX",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_39(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="API_COMPAT",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_40(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="XXOld-style middleware still uses process_request/process_response patternXX",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_41(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_42(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="OLD-STYLE MIDDLEWARE STILL USES PROCESS_REQUEST/PROCESS_RESPONSE PATTERN",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_43(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="XXConvert to new-style middleware with __init__(get_response) and __call__(request)XX",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_44(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_45(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="CONVERT TO NEW-STYLE MIDDLEWARE WITH __INIT__(GET_RESPONSE) AND __CALL__(REQUEST)",
                    auto_fixable=False,
                ))
    
    def xǁValidatorAgentǁ_check_middleware_pattern__mutmut_46(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style middleware pattern."""
        if file_type not in ("middleware", "unknown"):
            return
        
        # Old pattern: class with process_request/process_response methods
        if "process_request" in code or "process_response" in code:
            if "get_response" not in code and "__call__" not in code:
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message="Old-style middleware still uses process_request/process_response pattern",
                    file_path=file_path,
                    fix_hint="Convert to new-style middleware with __init__(get_response) and __call__(request)",
                    auto_fixable=True,
                ))
    
    xǁValidatorAgentǁ_check_middleware_pattern__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_1': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_1, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_2': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_2, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_3': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_3, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_4': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_4, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_5': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_5, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_6': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_6, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_7': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_7, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_8': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_8, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_9': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_9, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_10': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_10, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_11': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_11, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_12': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_12, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_13': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_13, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_14': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_14, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_15': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_15, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_16': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_16, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_17': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_17, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_18': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_18, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_19': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_19, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_20': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_20, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_21': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_21, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_22': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_22, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_23': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_23, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_24': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_24, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_25': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_25, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_26': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_26, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_27': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_27, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_28': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_28, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_29': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_29, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_30': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_30, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_31': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_31, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_32': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_32, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_33': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_33, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_34': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_34, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_35': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_35, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_36': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_36, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_37': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_37, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_38': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_38, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_39': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_39, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_40': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_40, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_41': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_41, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_42': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_42, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_43': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_43, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_44': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_44, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_45': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_45, 
        'xǁValidatorAgentǁ_check_middleware_pattern__mutmut_46': xǁValidatorAgentǁ_check_middleware_pattern__mutmut_46
    }
    xǁValidatorAgentǁ_check_middleware_pattern__mutmut_orig.__name__ = 'xǁValidatorAgentǁ_check_middleware_pattern'
    
    def _check_removed_settings(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        args = [report, file_path, code, file_type]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁValidatorAgentǁ_check_removed_settings__mutmut_orig'), object.__getattribute__(self, 'xǁValidatorAgentǁ_check_removed_settings__mutmut_mutants'), args, kwargs, self)
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_orig(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_1(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") or not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_2(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_3(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("XXsettingsXX", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_4(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("SETTINGS", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_5(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "XXunknownXX", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_6(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "UNKNOWN", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_7(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "XXotherXX") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_8(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "OTHER") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_9(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_10(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith(None):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_11(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("XXsettings.pyXX"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_12(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("SETTINGS.PY"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_13(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = None
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_14(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = None
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_15(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(None, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_16(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, None)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_17(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_18(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, )
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_19(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = None
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_20(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') - 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_21(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count(None) + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_22(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('XX\nXX') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_23(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 2
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_24(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(None)
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_25(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=None,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_26(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity=None,
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_27(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category=None,
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_28(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=None,
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_29(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=None,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_30(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=None,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_31(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=None,
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_32(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=None,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_33(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_34(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_35(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_36(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_37(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_38(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_39(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_40(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_41(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=2,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_42(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="XXerrorXX",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_43(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="ERROR",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_44(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="XXapi_compatXX",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_45(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="API_COMPAT",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=True,
                ))
    
    def xǁValidatorAgentǁ_check_removed_settings__mutmut_46(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for removed Django settings."""
        if file_type not in ("settings", "unknown", "other") and not file_path.endswith("settings.py"):
            return
        
        for setting in REMOVED_SETTINGS:
            pattern = rf'\b{setting}\b\s*='
            match = re.search(pattern, code)
            if match:
                line_num = code[:match.start()].count('\n') + 1
                report.add_issue(ValidationIssue(
                    tier=1,
                    severity="error",
                    category="api_compat",
                    message=f"Removed setting still present: {setting}",
                    file_path=file_path,
                    line_number=line_num,
                    fix_hint=f"Replace {setting} with its Django 5.x equivalent",
                    auto_fixable=False,
                ))
    
    xǁValidatorAgentǁ_check_removed_settings__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁValidatorAgentǁ_check_removed_settings__mutmut_1': xǁValidatorAgentǁ_check_removed_settings__mutmut_1, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_2': xǁValidatorAgentǁ_check_removed_settings__mutmut_2, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_3': xǁValidatorAgentǁ_check_removed_settings__mutmut_3, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_4': xǁValidatorAgentǁ_check_removed_settings__mutmut_4, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_5': xǁValidatorAgentǁ_check_removed_settings__mutmut_5, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_6': xǁValidatorAgentǁ_check_removed_settings__mutmut_6, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_7': xǁValidatorAgentǁ_check_removed_settings__mutmut_7, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_8': xǁValidatorAgentǁ_check_removed_settings__mutmut_8, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_9': xǁValidatorAgentǁ_check_removed_settings__mutmut_9, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_10': xǁValidatorAgentǁ_check_removed_settings__mutmut_10, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_11': xǁValidatorAgentǁ_check_removed_settings__mutmut_11, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_12': xǁValidatorAgentǁ_check_removed_settings__mutmut_12, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_13': xǁValidatorAgentǁ_check_removed_settings__mutmut_13, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_14': xǁValidatorAgentǁ_check_removed_settings__mutmut_14, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_15': xǁValidatorAgentǁ_check_removed_settings__mutmut_15, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_16': xǁValidatorAgentǁ_check_removed_settings__mutmut_16, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_17': xǁValidatorAgentǁ_check_removed_settings__mutmut_17, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_18': xǁValidatorAgentǁ_check_removed_settings__mutmut_18, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_19': xǁValidatorAgentǁ_check_removed_settings__mutmut_19, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_20': xǁValidatorAgentǁ_check_removed_settings__mutmut_20, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_21': xǁValidatorAgentǁ_check_removed_settings__mutmut_21, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_22': xǁValidatorAgentǁ_check_removed_settings__mutmut_22, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_23': xǁValidatorAgentǁ_check_removed_settings__mutmut_23, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_24': xǁValidatorAgentǁ_check_removed_settings__mutmut_24, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_25': xǁValidatorAgentǁ_check_removed_settings__mutmut_25, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_26': xǁValidatorAgentǁ_check_removed_settings__mutmut_26, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_27': xǁValidatorAgentǁ_check_removed_settings__mutmut_27, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_28': xǁValidatorAgentǁ_check_removed_settings__mutmut_28, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_29': xǁValidatorAgentǁ_check_removed_settings__mutmut_29, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_30': xǁValidatorAgentǁ_check_removed_settings__mutmut_30, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_31': xǁValidatorAgentǁ_check_removed_settings__mutmut_31, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_32': xǁValidatorAgentǁ_check_removed_settings__mutmut_32, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_33': xǁValidatorAgentǁ_check_removed_settings__mutmut_33, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_34': xǁValidatorAgentǁ_check_removed_settings__mutmut_34, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_35': xǁValidatorAgentǁ_check_removed_settings__mutmut_35, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_36': xǁValidatorAgentǁ_check_removed_settings__mutmut_36, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_37': xǁValidatorAgentǁ_check_removed_settings__mutmut_37, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_38': xǁValidatorAgentǁ_check_removed_settings__mutmut_38, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_39': xǁValidatorAgentǁ_check_removed_settings__mutmut_39, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_40': xǁValidatorAgentǁ_check_removed_settings__mutmut_40, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_41': xǁValidatorAgentǁ_check_removed_settings__mutmut_41, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_42': xǁValidatorAgentǁ_check_removed_settings__mutmut_42, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_43': xǁValidatorAgentǁ_check_removed_settings__mutmut_43, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_44': xǁValidatorAgentǁ_check_removed_settings__mutmut_44, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_45': xǁValidatorAgentǁ_check_removed_settings__mutmut_45, 
        'xǁValidatorAgentǁ_check_removed_settings__mutmut_46': xǁValidatorAgentǁ_check_removed_settings__mutmut_46
    }
    xǁValidatorAgentǁ_check_removed_settings__mutmut_orig.__name__ = 'xǁValidatorAgentǁ_check_removed_settings'
    
    def _check_url_patterns(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        args = [report, file_path, code, file_type]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁValidatorAgentǁ_check_url_patterns__mutmut_orig'), object.__getattribute__(self, 'xǁValidatorAgentǁ_check_url_patterns__mutmut_mutants'), args, kwargs, self)
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_orig(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_1(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_2(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("XXurlsXX", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_3(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("URLS", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_4(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "XXurlXX", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_5(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "URL", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_6(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "XXunknownXX"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_7(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "UNKNOWN"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_8(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = None
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_9(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'XX\burl\s*\(\s*r[\'"]XX'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_10(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\bURL\s*\(\s*R[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_11(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(None, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_12(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, None):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_13(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_14(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, ):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_15(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = None
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_16(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') - 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_17(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count(None) + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_18(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('XX\nXX') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_19(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 2
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_20(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(None)
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_21(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=None,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_22(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity=None,
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_23(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category=None,
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_24(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message=None,
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_25(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=None,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_26(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=None,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_27(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint=None,
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_28(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=None,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_29(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_30(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_31(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_32(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_33(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_34(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_35(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_36(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_37(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=2,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_38(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="XXerrorXX",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_39(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="ERROR",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_40(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="XXapi_compatXX",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_41(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="API_COMPAT",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_42(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="XXOld-style url() with regex still present — should use path() or re_path()XX",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_43(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_44(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="OLD-STYLE URL() WITH REGEX STILL PRESENT — SHOULD USE PATH() OR RE_PATH()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_45(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="XXReplace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)XX",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_46(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_47(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="REPLACE URL(R'...', ...) WITH PATH('...', ...) OR RE_PATH(R'...', ...)",
                auto_fixable=True,
            ))
    
    def xǁValidatorAgentǁ_check_url_patterns__mutmut_48(
        self, report: ValidationReport, file_path: str, code: str, file_type: str
    ):
        """Check for old-style url() patterns that should be path()/re_path()."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Check for url() function calls (not re_path)
        old_url_pattern = r'\burl\s*\(\s*r[\'"]'
        for match in re.finditer(old_url_pattern, code):
            line_num = code[:match.start()].count('\n') + 1
            report.add_issue(ValidationIssue(
                tier=1,
                severity="error",
                category="api_compat",
                message="Old-style url() with regex still present — should use path() or re_path()",
                file_path=file_path,
                line_number=line_num,
                fix_hint="Replace url(r'...', ...) with path('...', ...) or re_path(r'...', ...)",
                auto_fixable=False,
            ))
    
    xǁValidatorAgentǁ_check_url_patterns__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁValidatorAgentǁ_check_url_patterns__mutmut_1': xǁValidatorAgentǁ_check_url_patterns__mutmut_1, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_2': xǁValidatorAgentǁ_check_url_patterns__mutmut_2, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_3': xǁValidatorAgentǁ_check_url_patterns__mutmut_3, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_4': xǁValidatorAgentǁ_check_url_patterns__mutmut_4, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_5': xǁValidatorAgentǁ_check_url_patterns__mutmut_5, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_6': xǁValidatorAgentǁ_check_url_patterns__mutmut_6, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_7': xǁValidatorAgentǁ_check_url_patterns__mutmut_7, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_8': xǁValidatorAgentǁ_check_url_patterns__mutmut_8, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_9': xǁValidatorAgentǁ_check_url_patterns__mutmut_9, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_10': xǁValidatorAgentǁ_check_url_patterns__mutmut_10, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_11': xǁValidatorAgentǁ_check_url_patterns__mutmut_11, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_12': xǁValidatorAgentǁ_check_url_patterns__mutmut_12, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_13': xǁValidatorAgentǁ_check_url_patterns__mutmut_13, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_14': xǁValidatorAgentǁ_check_url_patterns__mutmut_14, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_15': xǁValidatorAgentǁ_check_url_patterns__mutmut_15, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_16': xǁValidatorAgentǁ_check_url_patterns__mutmut_16, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_17': xǁValidatorAgentǁ_check_url_patterns__mutmut_17, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_18': xǁValidatorAgentǁ_check_url_patterns__mutmut_18, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_19': xǁValidatorAgentǁ_check_url_patterns__mutmut_19, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_20': xǁValidatorAgentǁ_check_url_patterns__mutmut_20, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_21': xǁValidatorAgentǁ_check_url_patterns__mutmut_21, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_22': xǁValidatorAgentǁ_check_url_patterns__mutmut_22, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_23': xǁValidatorAgentǁ_check_url_patterns__mutmut_23, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_24': xǁValidatorAgentǁ_check_url_patterns__mutmut_24, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_25': xǁValidatorAgentǁ_check_url_patterns__mutmut_25, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_26': xǁValidatorAgentǁ_check_url_patterns__mutmut_26, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_27': xǁValidatorAgentǁ_check_url_patterns__mutmut_27, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_28': xǁValidatorAgentǁ_check_url_patterns__mutmut_28, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_29': xǁValidatorAgentǁ_check_url_patterns__mutmut_29, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_30': xǁValidatorAgentǁ_check_url_patterns__mutmut_30, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_31': xǁValidatorAgentǁ_check_url_patterns__mutmut_31, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_32': xǁValidatorAgentǁ_check_url_patterns__mutmut_32, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_33': xǁValidatorAgentǁ_check_url_patterns__mutmut_33, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_34': xǁValidatorAgentǁ_check_url_patterns__mutmut_34, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_35': xǁValidatorAgentǁ_check_url_patterns__mutmut_35, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_36': xǁValidatorAgentǁ_check_url_patterns__mutmut_36, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_37': xǁValidatorAgentǁ_check_url_patterns__mutmut_37, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_38': xǁValidatorAgentǁ_check_url_patterns__mutmut_38, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_39': xǁValidatorAgentǁ_check_url_patterns__mutmut_39, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_40': xǁValidatorAgentǁ_check_url_patterns__mutmut_40, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_41': xǁValidatorAgentǁ_check_url_patterns__mutmut_41, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_42': xǁValidatorAgentǁ_check_url_patterns__mutmut_42, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_43': xǁValidatorAgentǁ_check_url_patterns__mutmut_43, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_44': xǁValidatorAgentǁ_check_url_patterns__mutmut_44, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_45': xǁValidatorAgentǁ_check_url_patterns__mutmut_45, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_46': xǁValidatorAgentǁ_check_url_patterns__mutmut_46, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_47': xǁValidatorAgentǁ_check_url_patterns__mutmut_47, 
        'xǁValidatorAgentǁ_check_url_patterns__mutmut_48': xǁValidatorAgentǁ_check_url_patterns__mutmut_48
    }
    xǁValidatorAgentǁ_check_url_patterns__mutmut_orig.__name__ = 'xǁValidatorAgentǁ_check_url_patterns'
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def _run_tier2(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        args = [report, file_path, original_code, migrated_code, file_type, related_files, dependency_graph]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁValidatorAgentǁ_run_tier2__mutmut_orig'), object.__getattribute__(self, 'xǁValidatorAgentǁ_run_tier2__mutmut_mutants'), args, kwargs, self)
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_orig(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            report, file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, file_path, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_1(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            None, file_path, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            report, file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, file_path, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_2(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, None, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            report, file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, file_path, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_3(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, None, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            report, file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, file_path, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_4(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, None, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            report, file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, file_path, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_5(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, file_type, None
        )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            report, file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, file_path, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_6(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            file_path, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            report, file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, file_path, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_7(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            report, file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, file_path, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_8(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            report, file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, file_path, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_9(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            report, file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, file_path, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_10(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, file_type, )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            report, file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, file_path, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_11(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            None, file_path, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            report, file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, file_path, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_12(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, None, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            report, file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, file_path, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_13(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, None, file_type, related_files
        )
        self._check_import_chains(
            report, file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, file_path, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_14(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, None, related_files
        )
        self._check_import_chains(
            report, file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, file_path, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_15(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, file_type, None
        )
        self._check_import_chains(
            report, file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, file_path, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_16(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            file_path, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            report, file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, file_path, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_17(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            report, file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, file_path, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_18(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, file_type, related_files
        )
        self._check_import_chains(
            report, file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, file_path, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_19(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, related_files
        )
        self._check_import_chains(
            report, file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, file_path, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_20(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, file_type, )
        self._check_import_chains(
            report, file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, file_path, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_21(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            None, file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, file_path, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_22(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            report, None, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, file_path, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_23(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            report, file_path, None, related_files
        )
        self._check_url_view_binding(
            report, file_path, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_24(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            report, file_path, migrated_code, None
        )
        self._check_url_view_binding(
            report, file_path, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_25(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, file_path, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_26(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            report, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, file_path, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_27(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            report, file_path, related_files
        )
        self._check_url_view_binding(
            report, file_path, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_28(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            report, file_path, migrated_code, )
        self._check_url_view_binding(
            report, file_path, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_29(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            report, file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            None, file_path, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_30(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            report, file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, None, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_31(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            report, file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, file_path, None, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_32(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            report, file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, file_path, migrated_code, None, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_33(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            report, file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, file_path, migrated_code, file_type, None
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_34(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            report, file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            file_path, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_35(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            report, file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, migrated_code, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_36(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            report, file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, file_path, file_type, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_37(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            report, file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, file_path, migrated_code, related_files
        )
    
    # ─── TIER 2: Cross-File Consistency ────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier2__mutmut_38(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
        dependency_graph: Optional[Dict] = None,
    ):
        """Run cross-file consistency checks."""
        self._check_model_view_consistency(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_model_serializer_sync(
            report, file_path, migrated_code, file_type, related_files
        )
        self._check_import_chains(
            report, file_path, migrated_code, related_files
        )
        self._check_url_view_binding(
            report, file_path, migrated_code, file_type, )
    
    xǁValidatorAgentǁ_run_tier2__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁValidatorAgentǁ_run_tier2__mutmut_1': xǁValidatorAgentǁ_run_tier2__mutmut_1, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_2': xǁValidatorAgentǁ_run_tier2__mutmut_2, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_3': xǁValidatorAgentǁ_run_tier2__mutmut_3, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_4': xǁValidatorAgentǁ_run_tier2__mutmut_4, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_5': xǁValidatorAgentǁ_run_tier2__mutmut_5, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_6': xǁValidatorAgentǁ_run_tier2__mutmut_6, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_7': xǁValidatorAgentǁ_run_tier2__mutmut_7, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_8': xǁValidatorAgentǁ_run_tier2__mutmut_8, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_9': xǁValidatorAgentǁ_run_tier2__mutmut_9, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_10': xǁValidatorAgentǁ_run_tier2__mutmut_10, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_11': xǁValidatorAgentǁ_run_tier2__mutmut_11, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_12': xǁValidatorAgentǁ_run_tier2__mutmut_12, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_13': xǁValidatorAgentǁ_run_tier2__mutmut_13, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_14': xǁValidatorAgentǁ_run_tier2__mutmut_14, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_15': xǁValidatorAgentǁ_run_tier2__mutmut_15, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_16': xǁValidatorAgentǁ_run_tier2__mutmut_16, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_17': xǁValidatorAgentǁ_run_tier2__mutmut_17, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_18': xǁValidatorAgentǁ_run_tier2__mutmut_18, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_19': xǁValidatorAgentǁ_run_tier2__mutmut_19, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_20': xǁValidatorAgentǁ_run_tier2__mutmut_20, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_21': xǁValidatorAgentǁ_run_tier2__mutmut_21, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_22': xǁValidatorAgentǁ_run_tier2__mutmut_22, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_23': xǁValidatorAgentǁ_run_tier2__mutmut_23, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_24': xǁValidatorAgentǁ_run_tier2__mutmut_24, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_25': xǁValidatorAgentǁ_run_tier2__mutmut_25, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_26': xǁValidatorAgentǁ_run_tier2__mutmut_26, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_27': xǁValidatorAgentǁ_run_tier2__mutmut_27, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_28': xǁValidatorAgentǁ_run_tier2__mutmut_28, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_29': xǁValidatorAgentǁ_run_tier2__mutmut_29, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_30': xǁValidatorAgentǁ_run_tier2__mutmut_30, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_31': xǁValidatorAgentǁ_run_tier2__mutmut_31, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_32': xǁValidatorAgentǁ_run_tier2__mutmut_32, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_33': xǁValidatorAgentǁ_run_tier2__mutmut_33, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_34': xǁValidatorAgentǁ_run_tier2__mutmut_34, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_35': xǁValidatorAgentǁ_run_tier2__mutmut_35, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_36': xǁValidatorAgentǁ_run_tier2__mutmut_36, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_37': xǁValidatorAgentǁ_run_tier2__mutmut_37, 
        'xǁValidatorAgentǁ_run_tier2__mutmut_38': xǁValidatorAgentǁ_run_tier2__mutmut_38
    }
    xǁValidatorAgentǁ_run_tier2__mutmut_orig.__name__ = 'xǁValidatorAgentǁ_run_tier2'
    
    def _check_model_view_consistency(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        args = [report, file_path, migrated_code, file_type, related_files]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_orig'), object.__getattribute__(self, 'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_mutants'), args, kwargs, self)
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_orig(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_1(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_2(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("XXviewXX", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_3(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("VIEW", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_4(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "XXviewsXX", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_5(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "VIEWS", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_6(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "XXunknownXX"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_7(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "UNKNOWN"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_8(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = None
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_9(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() and "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_10(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "XXmodelsXX" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_11(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "MODELS" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_12(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" not in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_13(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.upper() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_14(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "XXmodelXX" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_15(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "MODEL" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_16(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" not in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_17(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.upper()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_18(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_19(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = None
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_20(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = None
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_21(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(None)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_22(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(None):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_23(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = None
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_24(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = "XXXX"
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_25(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = None
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_26(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = None
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_27(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name not in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_28(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("XXModelXX", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_29(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_30(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("MODEL", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_31(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "XXAbstractUserXX", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_32(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "abstractuser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_33(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "ABSTRACTUSER", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_34(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "XXAbstractBaseUserXX"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_35(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "abstractbaseuser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_36(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "ABSTRACTBASEUSER"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_37(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(None)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_38(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                break
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_39(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_40(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = None
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_41(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(None)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_42(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(None):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_43(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes or node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_44(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id in model_classes and node.attr == "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_45(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr != "objects":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_46(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "XXobjectsXX":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_view_consistency__mutmut_47(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that views reference model classes/fields that exist in migrated models."""
        if file_type not in ("view", "views", "unknown"):
            return
        
        # Find model files in related files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower() or "model" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model class names from model files
        model_classes = set()
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        # Check if it inherits from models.Model
                        for base in node.bases:
                            base_name = ""
                            if isinstance(base, ast.Attribute):
                                base_name = base.attr
                            elif isinstance(base, ast.Name):
                                base_name = base.id
                            if base_name in ("Model", "AbstractUser", "AbstractBaseUser"):
                                model_classes.add(node.name)
            except SyntaxError:
                continue
        
        if not model_classes:
            return
        
        # Check if the view references any model that doesn't exist
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Attribute):
                    if isinstance(node.value, ast.Name):
                        # Check for Model.objects.xxx patterns
                        if node.value.id not in model_classes and node.attr == "OBJECTS":
                            # Could be a model that we should recognize
                            # Only flag if it looks like a model access pattern
                            pass  # Don't over-flag — this is a heuristic
        except SyntaxError:
            pass
    
    xǁValidatorAgentǁ_check_model_view_consistency__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_1': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_1, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_2': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_2, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_3': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_3, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_4': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_4, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_5': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_5, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_6': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_6, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_7': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_7, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_8': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_8, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_9': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_9, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_10': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_10, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_11': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_11, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_12': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_12, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_13': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_13, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_14': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_14, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_15': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_15, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_16': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_16, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_17': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_17, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_18': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_18, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_19': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_19, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_20': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_20, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_21': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_21, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_22': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_22, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_23': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_23, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_24': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_24, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_25': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_25, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_26': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_26, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_27': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_27, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_28': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_28, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_29': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_29, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_30': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_30, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_31': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_31, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_32': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_32, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_33': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_33, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_34': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_34, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_35': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_35, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_36': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_36, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_37': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_37, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_38': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_38, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_39': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_39, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_40': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_40, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_41': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_41, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_42': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_42, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_43': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_43, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_44': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_44, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_45': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_45, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_46': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_46, 
        'xǁValidatorAgentǁ_check_model_view_consistency__mutmut_47': xǁValidatorAgentǁ_check_model_view_consistency__mutmut_47
    }
    xǁValidatorAgentǁ_check_model_view_consistency__mutmut_orig.__name__ = 'xǁValidatorAgentǁ_check_model_view_consistency'
    
    def _check_model_serializer_sync(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        args = [report, file_path, migrated_code, file_type, related_files]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_orig'), object.__getattribute__(self, 'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_mutants'), args, kwargs, self)
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_orig(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_1(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_2(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("XXserializerXX", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_3(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("SERIALIZER", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_4(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "XXserializersXX", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_5(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "SERIALIZERS", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_6(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "XXunknownXX"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_7(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "UNKNOWN"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_8(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = None
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_9(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "XXmodelsXX" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_10(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "MODELS" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_11(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" not in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_12(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.upper()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_13(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_14(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = None  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_15(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = None
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_16(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(None)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_17(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(None):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_18(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = None
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_19(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(None)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_20(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = None
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_21(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                break
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_22(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_23(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = None
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_24(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(None)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_25(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(None):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_26(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) or node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_27(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name != "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_28(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "XXMetaXX":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_29(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_30(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "META":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_31(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) or target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_32(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id != "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_33(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "XXmodelXX":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_34(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "MODEL":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_35(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = None
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_36(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name not in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_37(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                None, file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_38(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, None, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_39(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, None,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_40(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                None, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_41(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, None
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_42(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                file_path, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_43(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, node,
                                                model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_44(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, model_name, model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_45(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_fields[model_name]
                                            )
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_46(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that serializers reference fields that exist in migrated models."""
        if file_type not in ("serializer", "serializers", "unknown"):
            return
        
        # Find model files
        model_files = {
            path: content for path, content in related_files.items()
            if "models" in path.lower()
        }
        
        if not model_files:
            return
        
        # Extract model field names
        model_fields = {}  # model_name -> set of field names
        for model_path, model_code in model_files.items():
            try:
                tree = ast.parse(model_code)
                for node in ast.walk(tree):
                    if isinstance(node, ast.ClassDef):
                        fields = set()
                        for item in node.body:
                            if isinstance(item, ast.Assign):
                                for target in item.targets:
                                    if isinstance(target, ast.Name):
                                        fields.add(target.id)
                        if fields:
                            model_fields[node.name] = fields
            except SyntaxError:
                continue
        
        if not model_fields:
            return
        
        # Check serializer Meta.fields against model fields
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.ClassDef) and node.name == "Meta":
                    for item in node.body:
                        if isinstance(item, ast.Assign):
                            for target in item.targets:
                                if isinstance(target, ast.Name) and target.id == "model":
                                    # Get model name from the assignment
                                    if isinstance(item.value, ast.Name):
                                        model_name = item.value.id
                                        if model_name in model_fields:
                                            # Now check fields list
                                            self._verify_serializer_fields(
                                                report, file_path, node,
                                                model_name, )
        except SyntaxError:
            pass
    
    xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_1': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_1, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_2': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_2, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_3': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_3, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_4': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_4, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_5': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_5, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_6': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_6, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_7': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_7, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_8': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_8, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_9': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_9, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_10': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_10, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_11': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_11, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_12': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_12, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_13': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_13, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_14': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_14, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_15': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_15, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_16': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_16, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_17': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_17, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_18': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_18, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_19': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_19, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_20': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_20, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_21': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_21, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_22': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_22, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_23': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_23, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_24': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_24, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_25': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_25, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_26': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_26, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_27': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_27, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_28': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_28, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_29': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_29, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_30': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_30, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_31': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_31, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_32': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_32, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_33': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_33, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_34': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_34, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_35': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_35, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_36': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_36, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_37': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_37, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_38': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_38, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_39': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_39, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_40': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_40, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_41': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_41, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_42': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_42, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_43': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_43, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_44': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_44, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_45': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_45, 
        'xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_46': xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_46
    }
    xǁValidatorAgentǁ_check_model_serializer_sync__mutmut_orig.__name__ = 'xǁValidatorAgentǁ_check_model_serializer_sync'
    
    def _verify_serializer_fields(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        args = [report, file_path, meta_node, model_name, model_fields]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_orig'), object.__getattribute__(self, 'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_mutants'), args, kwargs, self)
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_orig(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="warning",
                                            category="cross_file",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_1(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) or target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="warning",
                                            category="cross_file",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_2(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id != "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="warning",
                                            category="cross_file",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_3(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "XXfieldsXX":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="warning",
                                            category="cross_file",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_4(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "FIELDS":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="warning",
                                            category="cross_file",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_5(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) or isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="warning",
                                            category="cross_file",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_6(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = None
                                    if field_name not in model_fields and field_name not in ("id", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="warning",
                                            category="cross_file",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_7(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields or field_name not in ("id", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="warning",
                                            category="cross_file",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_8(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name in model_fields and field_name not in ("id", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="warning",
                                            category="cross_file",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_9(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name in ("id", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="warning",
                                            category="cross_file",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_10(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("XXidXX", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="warning",
                                            category="cross_file",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_11(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("ID", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="warning",
                                            category="cross_file",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_12(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "XXpkXX", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="warning",
                                            category="cross_file",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_13(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "PK", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="warning",
                                            category="cross_file",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_14(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "pk", "XX__all__XX"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="warning",
                                            category="cross_file",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_15(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "pk", "__ALL__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="warning",
                                            category="cross_file",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_16(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "pk", "__all__"):
                                        report.add_issue(None)
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_17(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=None,
                                            severity="warning",
                                            category="cross_file",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_18(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity=None,
                                            category="cross_file",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_19(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="warning",
                                            category=None,
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_20(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="warning",
                                            category="cross_file",
                                            message=None,
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_21(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="warning",
                                            category="cross_file",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=None,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_22(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="warning",
                                            category="cross_file",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=None,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_23(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="warning",
                                            category="cross_file",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=None,
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_24(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="warning",
                                            category="cross_file",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=None,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_25(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            severity="warning",
                                            category="cross_file",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_26(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            category="cross_file",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_27(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="warning",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_28(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="warning",
                                            category="cross_file",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_29(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="warning",
                                            category="cross_file",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_30(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="warning",
                                            category="cross_file",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_31(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="warning",
                                            category="cross_file",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_32(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="warning",
                                            category="cross_file",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_33(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=3,
                                            severity="warning",
                                            category="cross_file",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_34(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="XXwarningXX",
                                            category="cross_file",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_35(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="WARNING",
                                            category="cross_file",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_36(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="warning",
                                            category="XXcross_fileXX",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_37(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="warning",
                                            category="CROSS_FILE",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=False,
                                        ))
    
    def xǁValidatorAgentǁ_verify_serializer_fields__mutmut_38(
        self,
        report: ValidationReport,
        file_path: str,
        meta_node: ast.ClassDef,
        model_name: str,
        model_fields: set,
    ):
        """Verify serializer fields match model fields."""
        for item in meta_node.body:
            if isinstance(item, ast.Assign):
                for target in item.targets:
                    if isinstance(target, ast.Name) and target.id == "fields":
                        if isinstance(item.value, (ast.List, ast.Tuple)):
                            for elt in item.value.elts:
                                if isinstance(elt, ast.Constant) and isinstance(elt.value, str):
                                    field_name = elt.value
                                    if field_name not in model_fields and field_name not in ("id", "pk", "__all__"):
                                        report.add_issue(ValidationIssue(
                                            tier=2,
                                            severity="warning",
                                            category="cross_file",
                                            message=f"Serializer field '{field_name}' not found in {model_name} model",
                                            file_path=file_path,
                                            line_number=elt.lineno,
                                            fix_hint=f"Verify '{field_name}' exists in migrated {model_name} model",
                                            auto_fixable=True,
                                        ))
    
    xǁValidatorAgentǁ_verify_serializer_fields__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_1': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_1, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_2': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_2, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_3': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_3, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_4': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_4, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_5': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_5, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_6': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_6, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_7': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_7, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_8': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_8, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_9': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_9, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_10': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_10, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_11': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_11, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_12': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_12, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_13': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_13, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_14': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_14, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_15': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_15, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_16': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_16, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_17': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_17, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_18': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_18, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_19': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_19, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_20': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_20, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_21': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_21, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_22': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_22, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_23': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_23, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_24': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_24, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_25': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_25, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_26': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_26, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_27': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_27, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_28': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_28, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_29': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_29, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_30': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_30, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_31': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_31, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_32': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_32, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_33': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_33, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_34': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_34, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_35': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_35, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_36': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_36, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_37': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_37, 
        'xǁValidatorAgentǁ_verify_serializer_fields__mutmut_38': xǁValidatorAgentǁ_verify_serializer_fields__mutmut_38
    }
    xǁValidatorAgentǁ_verify_serializer_fields__mutmut_orig.__name__ = 'xǁValidatorAgentǁ_verify_serializer_fields'
    
    def _check_import_chains(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        args = [report, file_path, migrated_code, related_files]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁValidatorAgentǁ_check_import_chains__mutmut_orig'), object.__getattribute__(self, 'xǁValidatorAgentǁ_check_import_chains__mutmut_mutants'), args, kwargs, self)
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_orig(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_1(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = None
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_2(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(None)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_3(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = None  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_4(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = None
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_5(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(None)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_6(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = None
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_7(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(None):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_8(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(None)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_9(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(None)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_10(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(None)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_11(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = None
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_12(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                break
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_13(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(None):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_14(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) or node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_15(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = None
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_16(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(None, "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_17(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", None)
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_18(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace("/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_19(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", )
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_20(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace("XX.XX", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_21(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "XX/XX")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_22(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = None
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_23(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(None, "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_24(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", None)
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_25(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace("")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_26(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", )
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_27(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace(None, "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_28(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", None).replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_29(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_30(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", ).replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_31(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("XX\\XX", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_32(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "XX/XX").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_33(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace("XX.pyXX", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_34(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".PY", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_35(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "XXXX")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_36(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(None):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_37(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split(None)[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_38(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("XX/XX")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_39(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[+1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_40(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-2]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_41(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names or alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_42(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_43(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name == "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_44(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "XX*XX":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_45(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(None)
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_46(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=None,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_47(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity=None,
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_48(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category=None,
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_49(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=None,
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_50(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=None,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_51(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=None,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_52(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=None,
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_53(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=None,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_54(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_55(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_56(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_57(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_58(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_59(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_60(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_61(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_62(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=3,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_63(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="XXerrorXX",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_64(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="ERROR",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_65(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="XXcross_fileXX",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_66(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="CROSS_FILE",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=False,
                                ))
    
    def xǁValidatorAgentǁ_check_import_chains__mutmut_67(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        related_files: Dict[str, str],
    ):
        """Check that imports from related files reference classes/functions that exist."""
        try:
            tree = ast.parse(migrated_code)
        except SyntaxError:
            return
        
        # Get exports from related files
        exports = {}  # module_path -> set of exported names
        for path, content in related_files.items():
            try:
                related_tree = ast.parse(content)
                names = set()
                for node in ast.walk(related_tree):
                    if isinstance(node, ast.ClassDef):
                        names.add(node.name)
                    elif isinstance(node, ast.FunctionDef):
                        names.add(node.name)
                    elif isinstance(node, ast.Assign):
                        for target in node.targets:
                            if isinstance(target, ast.Name):
                                names.add(target.id)
                exports[path] = names
            except SyntaxError:
                continue
        
        # Check imports in migrated code against exports
        for node in ast.walk(tree):
            if isinstance(node, ast.ImportFrom) and node.module:
                # Convert module path to file path for matching
                module_as_path = node.module.replace(".", "/")
                
                for path, names in exports.items():
                    # Fuzzy match: check if the import module matches a related file
                    path_normalized = path.replace("\\", "/").replace(".py", "")
                    if module_as_path.endswith(path_normalized.split("/")[-1]):
                        for alias in node.names:
                            if alias.name not in names and alias.name != "*":
                                report.add_issue(ValidationIssue(
                                    tier=2,
                                    severity="error",
                                    category="cross_file",
                                    message=f"Import '{alias.name}' from '{node.module}' — name not found in migrated '{path}'",
                                    file_path=file_path,
                                    line_number=node.lineno,
                                    fix_hint=f"Check if '{alias.name}' was renamed or removed in the migrated version of '{path}'",
                                    auto_fixable=True,
                                ))
    
    xǁValidatorAgentǁ_check_import_chains__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁValidatorAgentǁ_check_import_chains__mutmut_1': xǁValidatorAgentǁ_check_import_chains__mutmut_1, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_2': xǁValidatorAgentǁ_check_import_chains__mutmut_2, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_3': xǁValidatorAgentǁ_check_import_chains__mutmut_3, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_4': xǁValidatorAgentǁ_check_import_chains__mutmut_4, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_5': xǁValidatorAgentǁ_check_import_chains__mutmut_5, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_6': xǁValidatorAgentǁ_check_import_chains__mutmut_6, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_7': xǁValidatorAgentǁ_check_import_chains__mutmut_7, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_8': xǁValidatorAgentǁ_check_import_chains__mutmut_8, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_9': xǁValidatorAgentǁ_check_import_chains__mutmut_9, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_10': xǁValidatorAgentǁ_check_import_chains__mutmut_10, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_11': xǁValidatorAgentǁ_check_import_chains__mutmut_11, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_12': xǁValidatorAgentǁ_check_import_chains__mutmut_12, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_13': xǁValidatorAgentǁ_check_import_chains__mutmut_13, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_14': xǁValidatorAgentǁ_check_import_chains__mutmut_14, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_15': xǁValidatorAgentǁ_check_import_chains__mutmut_15, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_16': xǁValidatorAgentǁ_check_import_chains__mutmut_16, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_17': xǁValidatorAgentǁ_check_import_chains__mutmut_17, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_18': xǁValidatorAgentǁ_check_import_chains__mutmut_18, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_19': xǁValidatorAgentǁ_check_import_chains__mutmut_19, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_20': xǁValidatorAgentǁ_check_import_chains__mutmut_20, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_21': xǁValidatorAgentǁ_check_import_chains__mutmut_21, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_22': xǁValidatorAgentǁ_check_import_chains__mutmut_22, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_23': xǁValidatorAgentǁ_check_import_chains__mutmut_23, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_24': xǁValidatorAgentǁ_check_import_chains__mutmut_24, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_25': xǁValidatorAgentǁ_check_import_chains__mutmut_25, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_26': xǁValidatorAgentǁ_check_import_chains__mutmut_26, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_27': xǁValidatorAgentǁ_check_import_chains__mutmut_27, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_28': xǁValidatorAgentǁ_check_import_chains__mutmut_28, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_29': xǁValidatorAgentǁ_check_import_chains__mutmut_29, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_30': xǁValidatorAgentǁ_check_import_chains__mutmut_30, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_31': xǁValidatorAgentǁ_check_import_chains__mutmut_31, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_32': xǁValidatorAgentǁ_check_import_chains__mutmut_32, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_33': xǁValidatorAgentǁ_check_import_chains__mutmut_33, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_34': xǁValidatorAgentǁ_check_import_chains__mutmut_34, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_35': xǁValidatorAgentǁ_check_import_chains__mutmut_35, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_36': xǁValidatorAgentǁ_check_import_chains__mutmut_36, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_37': xǁValidatorAgentǁ_check_import_chains__mutmut_37, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_38': xǁValidatorAgentǁ_check_import_chains__mutmut_38, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_39': xǁValidatorAgentǁ_check_import_chains__mutmut_39, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_40': xǁValidatorAgentǁ_check_import_chains__mutmut_40, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_41': xǁValidatorAgentǁ_check_import_chains__mutmut_41, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_42': xǁValidatorAgentǁ_check_import_chains__mutmut_42, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_43': xǁValidatorAgentǁ_check_import_chains__mutmut_43, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_44': xǁValidatorAgentǁ_check_import_chains__mutmut_44, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_45': xǁValidatorAgentǁ_check_import_chains__mutmut_45, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_46': xǁValidatorAgentǁ_check_import_chains__mutmut_46, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_47': xǁValidatorAgentǁ_check_import_chains__mutmut_47, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_48': xǁValidatorAgentǁ_check_import_chains__mutmut_48, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_49': xǁValidatorAgentǁ_check_import_chains__mutmut_49, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_50': xǁValidatorAgentǁ_check_import_chains__mutmut_50, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_51': xǁValidatorAgentǁ_check_import_chains__mutmut_51, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_52': xǁValidatorAgentǁ_check_import_chains__mutmut_52, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_53': xǁValidatorAgentǁ_check_import_chains__mutmut_53, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_54': xǁValidatorAgentǁ_check_import_chains__mutmut_54, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_55': xǁValidatorAgentǁ_check_import_chains__mutmut_55, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_56': xǁValidatorAgentǁ_check_import_chains__mutmut_56, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_57': xǁValidatorAgentǁ_check_import_chains__mutmut_57, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_58': xǁValidatorAgentǁ_check_import_chains__mutmut_58, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_59': xǁValidatorAgentǁ_check_import_chains__mutmut_59, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_60': xǁValidatorAgentǁ_check_import_chains__mutmut_60, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_61': xǁValidatorAgentǁ_check_import_chains__mutmut_61, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_62': xǁValidatorAgentǁ_check_import_chains__mutmut_62, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_63': xǁValidatorAgentǁ_check_import_chains__mutmut_63, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_64': xǁValidatorAgentǁ_check_import_chains__mutmut_64, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_65': xǁValidatorAgentǁ_check_import_chains__mutmut_65, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_66': xǁValidatorAgentǁ_check_import_chains__mutmut_66, 
        'xǁValidatorAgentǁ_check_import_chains__mutmut_67': xǁValidatorAgentǁ_check_import_chains__mutmut_67
    }
    xǁValidatorAgentǁ_check_import_chains__mutmut_orig.__name__ = 'xǁValidatorAgentǁ_check_import_chains'
    
    def _check_url_view_binding(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        args = [report, file_path, migrated_code, file_type, related_files]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁValidatorAgentǁ_check_url_view_binding__mutmut_orig'), object.__getattribute__(self, 'xǁValidatorAgentǁ_check_url_view_binding__mutmut_mutants'), args, kwargs, self)
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_orig(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_1(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_2(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("XXurlsXX", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_3(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("URLS", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_4(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "XXurlXX", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_5(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "URL", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_6(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "XXunknownXX"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_7(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "UNKNOWN"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_8(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = None
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_9(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() and "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_10(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "XXviewsXX" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_11(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "VIEWS" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_12(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" not in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_13(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.upper() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_14(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "XXviewXX" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_15(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "VIEW" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_16(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" not in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_17(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.upper()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_18(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_19(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = None
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_20(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = None
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_21(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(None)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_22(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(None):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_23(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(None)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_24(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(None)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_25(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                break
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_26(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_27(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = None
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_28(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(None)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_29(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(None):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_30(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = None
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_31(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = "XXXX"
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_32(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = None
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_33(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = None
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_34(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name not in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_35(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("XXpathXX", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_36(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("PATH", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_37(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "XXre_pathXX", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_38(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "RE_PATH", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_39(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "XXurlXX"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_40(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "URL"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_41(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) > 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_42(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 3:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_43(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = None
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_44(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[2]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_45(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = None
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_46(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_47(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(None)
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_48(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=None,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_49(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity=None,
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_50(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category=None,
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_51(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=None,
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_52(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=None,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_53(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=None,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_54(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=None,
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_55(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=None,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_56(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_57(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_58(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_59(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_60(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_61(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_62(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_63(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_64(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=3,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_65(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="XXwarningXX",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_66(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="WARNING",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_67(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="XXcross_fileXX",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_68(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="CROSS_FILE",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=False,
                                    ))
        except SyntaxError:
            pass
    
    def xǁValidatorAgentǁ_check_url_view_binding__mutmut_69(
        self,
        report: ValidationReport,
        file_path: str,
        migrated_code: str,
        file_type: str,
        related_files: Dict[str, str],
    ):
        """Check that URL patterns reference views that exist."""
        if file_type not in ("urls", "url", "unknown"):
            return
        
        # Look for view files in related files
        view_files = {
            path: content for path, content in related_files.items()
            if "views" in path.lower() or "view" in path.lower()
        }
        
        if not view_files:
            return
        
        # Extract view function/class names from view files
        view_names = set()
        for view_path, view_code in view_files.items():
            try:
                tree = ast.parse(view_code)
                for node in ast.walk(tree):
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
                        view_names.add(node.name)
                    elif isinstance(node, ast.ClassDef):
                        view_names.add(node.name)
            except SyntaxError:
                continue
        
        if not view_names:
            return
        
        # Check URL patterns reference existing views
        try:
            tree = ast.parse(migrated_code)
            for node in ast.walk(tree):
                if isinstance(node, ast.Call):
                    func_name = ""
                    if isinstance(node.func, ast.Name):
                        func_name = node.func.id
                    elif isinstance(node.func, ast.Attribute):
                        func_name = node.func.attr
                    
                    if func_name in ("path", "re_path", "url"):
                        # Check 2nd argument (view reference)
                        if len(node.args) >= 2:
                            view_ref = node.args[1]
                            if isinstance(view_ref, ast.Attribute):
                                # views.some_view pattern
                                ref_name = view_ref.attr
                                if ref_name not in view_names:
                                    report.add_issue(ValidationIssue(
                                        tier=2,
                                        severity="warning",
                                        category="cross_file",
                                        message=f"URL pattern references view '{ref_name}' — not found in migrated view files",
                                        file_path=file_path,
                                        line_number=node.lineno,
                                        fix_hint=f"Verify '{ref_name}' exists in the migrated views module",
                                        auto_fixable=True,
                                    ))
        except SyntaxError:
            pass
    
    xǁValidatorAgentǁ_check_url_view_binding__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁValidatorAgentǁ_check_url_view_binding__mutmut_1': xǁValidatorAgentǁ_check_url_view_binding__mutmut_1, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_2': xǁValidatorAgentǁ_check_url_view_binding__mutmut_2, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_3': xǁValidatorAgentǁ_check_url_view_binding__mutmut_3, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_4': xǁValidatorAgentǁ_check_url_view_binding__mutmut_4, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_5': xǁValidatorAgentǁ_check_url_view_binding__mutmut_5, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_6': xǁValidatorAgentǁ_check_url_view_binding__mutmut_6, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_7': xǁValidatorAgentǁ_check_url_view_binding__mutmut_7, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_8': xǁValidatorAgentǁ_check_url_view_binding__mutmut_8, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_9': xǁValidatorAgentǁ_check_url_view_binding__mutmut_9, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_10': xǁValidatorAgentǁ_check_url_view_binding__mutmut_10, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_11': xǁValidatorAgentǁ_check_url_view_binding__mutmut_11, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_12': xǁValidatorAgentǁ_check_url_view_binding__mutmut_12, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_13': xǁValidatorAgentǁ_check_url_view_binding__mutmut_13, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_14': xǁValidatorAgentǁ_check_url_view_binding__mutmut_14, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_15': xǁValidatorAgentǁ_check_url_view_binding__mutmut_15, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_16': xǁValidatorAgentǁ_check_url_view_binding__mutmut_16, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_17': xǁValidatorAgentǁ_check_url_view_binding__mutmut_17, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_18': xǁValidatorAgentǁ_check_url_view_binding__mutmut_18, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_19': xǁValidatorAgentǁ_check_url_view_binding__mutmut_19, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_20': xǁValidatorAgentǁ_check_url_view_binding__mutmut_20, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_21': xǁValidatorAgentǁ_check_url_view_binding__mutmut_21, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_22': xǁValidatorAgentǁ_check_url_view_binding__mutmut_22, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_23': xǁValidatorAgentǁ_check_url_view_binding__mutmut_23, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_24': xǁValidatorAgentǁ_check_url_view_binding__mutmut_24, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_25': xǁValidatorAgentǁ_check_url_view_binding__mutmut_25, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_26': xǁValidatorAgentǁ_check_url_view_binding__mutmut_26, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_27': xǁValidatorAgentǁ_check_url_view_binding__mutmut_27, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_28': xǁValidatorAgentǁ_check_url_view_binding__mutmut_28, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_29': xǁValidatorAgentǁ_check_url_view_binding__mutmut_29, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_30': xǁValidatorAgentǁ_check_url_view_binding__mutmut_30, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_31': xǁValidatorAgentǁ_check_url_view_binding__mutmut_31, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_32': xǁValidatorAgentǁ_check_url_view_binding__mutmut_32, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_33': xǁValidatorAgentǁ_check_url_view_binding__mutmut_33, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_34': xǁValidatorAgentǁ_check_url_view_binding__mutmut_34, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_35': xǁValidatorAgentǁ_check_url_view_binding__mutmut_35, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_36': xǁValidatorAgentǁ_check_url_view_binding__mutmut_36, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_37': xǁValidatorAgentǁ_check_url_view_binding__mutmut_37, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_38': xǁValidatorAgentǁ_check_url_view_binding__mutmut_38, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_39': xǁValidatorAgentǁ_check_url_view_binding__mutmut_39, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_40': xǁValidatorAgentǁ_check_url_view_binding__mutmut_40, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_41': xǁValidatorAgentǁ_check_url_view_binding__mutmut_41, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_42': xǁValidatorAgentǁ_check_url_view_binding__mutmut_42, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_43': xǁValidatorAgentǁ_check_url_view_binding__mutmut_43, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_44': xǁValidatorAgentǁ_check_url_view_binding__mutmut_44, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_45': xǁValidatorAgentǁ_check_url_view_binding__mutmut_45, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_46': xǁValidatorAgentǁ_check_url_view_binding__mutmut_46, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_47': xǁValidatorAgentǁ_check_url_view_binding__mutmut_47, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_48': xǁValidatorAgentǁ_check_url_view_binding__mutmut_48, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_49': xǁValidatorAgentǁ_check_url_view_binding__mutmut_49, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_50': xǁValidatorAgentǁ_check_url_view_binding__mutmut_50, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_51': xǁValidatorAgentǁ_check_url_view_binding__mutmut_51, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_52': xǁValidatorAgentǁ_check_url_view_binding__mutmut_52, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_53': xǁValidatorAgentǁ_check_url_view_binding__mutmut_53, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_54': xǁValidatorAgentǁ_check_url_view_binding__mutmut_54, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_55': xǁValidatorAgentǁ_check_url_view_binding__mutmut_55, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_56': xǁValidatorAgentǁ_check_url_view_binding__mutmut_56, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_57': xǁValidatorAgentǁ_check_url_view_binding__mutmut_57, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_58': xǁValidatorAgentǁ_check_url_view_binding__mutmut_58, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_59': xǁValidatorAgentǁ_check_url_view_binding__mutmut_59, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_60': xǁValidatorAgentǁ_check_url_view_binding__mutmut_60, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_61': xǁValidatorAgentǁ_check_url_view_binding__mutmut_61, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_62': xǁValidatorAgentǁ_check_url_view_binding__mutmut_62, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_63': xǁValidatorAgentǁ_check_url_view_binding__mutmut_63, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_64': xǁValidatorAgentǁ_check_url_view_binding__mutmut_64, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_65': xǁValidatorAgentǁ_check_url_view_binding__mutmut_65, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_66': xǁValidatorAgentǁ_check_url_view_binding__mutmut_66, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_67': xǁValidatorAgentǁ_check_url_view_binding__mutmut_67, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_68': xǁValidatorAgentǁ_check_url_view_binding__mutmut_68, 
        'xǁValidatorAgentǁ_check_url_view_binding__mutmut_69': xǁValidatorAgentǁ_check_url_view_binding__mutmut_69
    }
    xǁValidatorAgentǁ_check_url_view_binding__mutmut_orig.__name__ = 'xǁValidatorAgentǁ_check_url_view_binding'
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def _run_tier3(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        args = [report, file_path, original_code, migrated_code, file_type, related_files]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁValidatorAgentǁ_run_tier3__mutmut_orig'), object.__getattribute__(self, 'xǁValidatorAgentǁ_run_tier3__mutmut_mutants'), args, kwargs, self)
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_orig(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_1(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_2(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = None
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_3(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            None, original_code, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_4(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, None, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_5(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, None, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_6(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, None, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_7(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, file_type, None
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_8(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            original_code, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_9(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_10(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_11(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_12(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, file_type, )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_13(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = None
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_14(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(None)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_15(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = None
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_16(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(None, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_17(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, None)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_18(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_19(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, )
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_20(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(None)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_21(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(None)
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_22(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=None,
                severity="warning",
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_23(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity=None,
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_24(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                category=None,
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_25(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                category="semantic",
                message=None,
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_26(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=None,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_27(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=None,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_28(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                severity="warning",
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_29(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_30(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_31(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                category="semantic",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_32(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_33(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_34(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=4,
                severity="warning",
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_35(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="XXwarningXX",
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_36(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="WARNING",
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_37(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                category="XXsemanticXX",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_38(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                category="SEMANTIC",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_39(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                category="semantic",
                message=f"Tier 3 review failed: {str(None)}",
                file_path=file_path,
                auto_fixable=False,
            ))
    
    # ─── TIER 3: LLM Semantic Review ──────────────────────────────────────────
    
    def xǁValidatorAgentǁ_run_tier3__mutmut_40(
        self,
        report: ValidationReport,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ):
        """Run LLM-powered semantic review."""
        if not self.llm_client:
            return
        
        prompt = self._build_tier3_prompt(
            file_path, original_code, migrated_code, file_type, related_files
        )
        
        try:
            # Use the LLM to review
            response = self._call_llm(prompt)
            issues = self._parse_tier3_response(response, file_path)
            
            for issue in issues:
                report.add_issue(issue)
                
        except Exception as e:
            report.add_issue(ValidationIssue(
                tier=3,
                severity="warning",
                category="semantic",
                message=f"Tier 3 review failed: {str(e)}",
                file_path=file_path,
                auto_fixable=True,
            ))
    
    xǁValidatorAgentǁ_run_tier3__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁValidatorAgentǁ_run_tier3__mutmut_1': xǁValidatorAgentǁ_run_tier3__mutmut_1, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_2': xǁValidatorAgentǁ_run_tier3__mutmut_2, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_3': xǁValidatorAgentǁ_run_tier3__mutmut_3, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_4': xǁValidatorAgentǁ_run_tier3__mutmut_4, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_5': xǁValidatorAgentǁ_run_tier3__mutmut_5, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_6': xǁValidatorAgentǁ_run_tier3__mutmut_6, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_7': xǁValidatorAgentǁ_run_tier3__mutmut_7, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_8': xǁValidatorAgentǁ_run_tier3__mutmut_8, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_9': xǁValidatorAgentǁ_run_tier3__mutmut_9, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_10': xǁValidatorAgentǁ_run_tier3__mutmut_10, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_11': xǁValidatorAgentǁ_run_tier3__mutmut_11, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_12': xǁValidatorAgentǁ_run_tier3__mutmut_12, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_13': xǁValidatorAgentǁ_run_tier3__mutmut_13, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_14': xǁValidatorAgentǁ_run_tier3__mutmut_14, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_15': xǁValidatorAgentǁ_run_tier3__mutmut_15, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_16': xǁValidatorAgentǁ_run_tier3__mutmut_16, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_17': xǁValidatorAgentǁ_run_tier3__mutmut_17, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_18': xǁValidatorAgentǁ_run_tier3__mutmut_18, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_19': xǁValidatorAgentǁ_run_tier3__mutmut_19, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_20': xǁValidatorAgentǁ_run_tier3__mutmut_20, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_21': xǁValidatorAgentǁ_run_tier3__mutmut_21, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_22': xǁValidatorAgentǁ_run_tier3__mutmut_22, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_23': xǁValidatorAgentǁ_run_tier3__mutmut_23, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_24': xǁValidatorAgentǁ_run_tier3__mutmut_24, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_25': xǁValidatorAgentǁ_run_tier3__mutmut_25, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_26': xǁValidatorAgentǁ_run_tier3__mutmut_26, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_27': xǁValidatorAgentǁ_run_tier3__mutmut_27, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_28': xǁValidatorAgentǁ_run_tier3__mutmut_28, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_29': xǁValidatorAgentǁ_run_tier3__mutmut_29, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_30': xǁValidatorAgentǁ_run_tier3__mutmut_30, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_31': xǁValidatorAgentǁ_run_tier3__mutmut_31, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_32': xǁValidatorAgentǁ_run_tier3__mutmut_32, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_33': xǁValidatorAgentǁ_run_tier3__mutmut_33, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_34': xǁValidatorAgentǁ_run_tier3__mutmut_34, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_35': xǁValidatorAgentǁ_run_tier3__mutmut_35, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_36': xǁValidatorAgentǁ_run_tier3__mutmut_36, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_37': xǁValidatorAgentǁ_run_tier3__mutmut_37, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_38': xǁValidatorAgentǁ_run_tier3__mutmut_38, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_39': xǁValidatorAgentǁ_run_tier3__mutmut_39, 
        'xǁValidatorAgentǁ_run_tier3__mutmut_40': xǁValidatorAgentǁ_run_tier3__mutmut_40
    }
    xǁValidatorAgentǁ_run_tier3__mutmut_orig.__name__ = 'xǁValidatorAgentǁ_run_tier3'
    
    def _build_tier3_prompt(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ) -> str:
        args = [file_path, original_code, migrated_code, file_type, related_files]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁValidatorAgentǁ_build_tier3_prompt__mutmut_orig'), object.__getattribute__(self, 'xǁValidatorAgentǁ_build_tier3_prompt__mutmut_mutants'), args, kwargs, self)
    
    def xǁValidatorAgentǁ_build_tier3_prompt__mutmut_orig(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ) -> str:
        """Build prompt for LLM semantic review."""
        related_context = ""
        if related_files:
            for path, content in list(related_files.items())[:3]:  # Limit context
                related_context += f"\n### Related File: {path}\n```python\n{content[:2000]}\n```\n"
        
        return f"""You are a Django migration validator. Review the following migration from Django 2.x to 5.x.

## Original Code ({file_path}, type: {file_type}):
```python
{original_code}
```

## Migrated Code:
```python
{migrated_code}
```

{f"## Related Files (already migrated):{related_context}" if related_context else ""}

## Check for these issues:
1. **Logic changes**: Was any business logic accidentally altered?
2. **Missing migrations**: Are there deprecated patterns that weren't updated?
3. **Wrong patterns**: Were migration rules applied incorrectly?
4. **Cross-file breaks**: Does this code reference things in related files that may have changed?

## Output Format:
For each issue found, output EXACTLY one line in this format:
ISSUE|severity|message|line_number|fix_hint

Where severity is: error, warning, or info
If no issues found, output: NO_ISSUES

Only report REAL issues. Do not report false positives.
"""
    
    def xǁValidatorAgentǁ_build_tier3_prompt__mutmut_1(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ) -> str:
        """Build prompt for LLM semantic review."""
        related_context = None
        if related_files:
            for path, content in list(related_files.items())[:3]:  # Limit context
                related_context += f"\n### Related File: {path}\n```python\n{content[:2000]}\n```\n"
        
        return f"""You are a Django migration validator. Review the following migration from Django 2.x to 5.x.

## Original Code ({file_path}, type: {file_type}):
```python
{original_code}
```

## Migrated Code:
```python
{migrated_code}
```

{f"## Related Files (already migrated):{related_context}" if related_context else ""}

## Check for these issues:
1. **Logic changes**: Was any business logic accidentally altered?
2. **Missing migrations**: Are there deprecated patterns that weren't updated?
3. **Wrong patterns**: Were migration rules applied incorrectly?
4. **Cross-file breaks**: Does this code reference things in related files that may have changed?

## Output Format:
For each issue found, output EXACTLY one line in this format:
ISSUE|severity|message|line_number|fix_hint

Where severity is: error, warning, or info
If no issues found, output: NO_ISSUES

Only report REAL issues. Do not report false positives.
"""
    
    def xǁValidatorAgentǁ_build_tier3_prompt__mutmut_2(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ) -> str:
        """Build prompt for LLM semantic review."""
        related_context = "XXXX"
        if related_files:
            for path, content in list(related_files.items())[:3]:  # Limit context
                related_context += f"\n### Related File: {path}\n```python\n{content[:2000]}\n```\n"
        
        return f"""You are a Django migration validator. Review the following migration from Django 2.x to 5.x.

## Original Code ({file_path}, type: {file_type}):
```python
{original_code}
```

## Migrated Code:
```python
{migrated_code}
```

{f"## Related Files (already migrated):{related_context}" if related_context else ""}

## Check for these issues:
1. **Logic changes**: Was any business logic accidentally altered?
2. **Missing migrations**: Are there deprecated patterns that weren't updated?
3. **Wrong patterns**: Were migration rules applied incorrectly?
4. **Cross-file breaks**: Does this code reference things in related files that may have changed?

## Output Format:
For each issue found, output EXACTLY one line in this format:
ISSUE|severity|message|line_number|fix_hint

Where severity is: error, warning, or info
If no issues found, output: NO_ISSUES

Only report REAL issues. Do not report false positives.
"""
    
    def xǁValidatorAgentǁ_build_tier3_prompt__mutmut_3(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ) -> str:
        """Build prompt for LLM semantic review."""
        related_context = ""
        if related_files:
            for path, content in list(None)[:3]:  # Limit context
                related_context += f"\n### Related File: {path}\n```python\n{content[:2000]}\n```\n"
        
        return f"""You are a Django migration validator. Review the following migration from Django 2.x to 5.x.

## Original Code ({file_path}, type: {file_type}):
```python
{original_code}
```

## Migrated Code:
```python
{migrated_code}
```

{f"## Related Files (already migrated):{related_context}" if related_context else ""}

## Check for these issues:
1. **Logic changes**: Was any business logic accidentally altered?
2. **Missing migrations**: Are there deprecated patterns that weren't updated?
3. **Wrong patterns**: Were migration rules applied incorrectly?
4. **Cross-file breaks**: Does this code reference things in related files that may have changed?

## Output Format:
For each issue found, output EXACTLY one line in this format:
ISSUE|severity|message|line_number|fix_hint

Where severity is: error, warning, or info
If no issues found, output: NO_ISSUES

Only report REAL issues. Do not report false positives.
"""
    
    def xǁValidatorAgentǁ_build_tier3_prompt__mutmut_4(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ) -> str:
        """Build prompt for LLM semantic review."""
        related_context = ""
        if related_files:
            for path, content in list(related_files.items())[:4]:  # Limit context
                related_context += f"\n### Related File: {path}\n```python\n{content[:2000]}\n```\n"
        
        return f"""You are a Django migration validator. Review the following migration from Django 2.x to 5.x.

## Original Code ({file_path}, type: {file_type}):
```python
{original_code}
```

## Migrated Code:
```python
{migrated_code}
```

{f"## Related Files (already migrated):{related_context}" if related_context else ""}

## Check for these issues:
1. **Logic changes**: Was any business logic accidentally altered?
2. **Missing migrations**: Are there deprecated patterns that weren't updated?
3. **Wrong patterns**: Were migration rules applied incorrectly?
4. **Cross-file breaks**: Does this code reference things in related files that may have changed?

## Output Format:
For each issue found, output EXACTLY one line in this format:
ISSUE|severity|message|line_number|fix_hint

Where severity is: error, warning, or info
If no issues found, output: NO_ISSUES

Only report REAL issues. Do not report false positives.
"""
    
    def xǁValidatorAgentǁ_build_tier3_prompt__mutmut_5(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ) -> str:
        """Build prompt for LLM semantic review."""
        related_context = ""
        if related_files:
            for path, content in list(related_files.items())[:3]:  # Limit context
                related_context = f"\n### Related File: {path}\n```python\n{content[:2000]}\n```\n"
        
        return f"""You are a Django migration validator. Review the following migration from Django 2.x to 5.x.

## Original Code ({file_path}, type: {file_type}):
```python
{original_code}
```

## Migrated Code:
```python
{migrated_code}
```

{f"## Related Files (already migrated):{related_context}" if related_context else ""}

## Check for these issues:
1. **Logic changes**: Was any business logic accidentally altered?
2. **Missing migrations**: Are there deprecated patterns that weren't updated?
3. **Wrong patterns**: Were migration rules applied incorrectly?
4. **Cross-file breaks**: Does this code reference things in related files that may have changed?

## Output Format:
For each issue found, output EXACTLY one line in this format:
ISSUE|severity|message|line_number|fix_hint

Where severity is: error, warning, or info
If no issues found, output: NO_ISSUES

Only report REAL issues. Do not report false positives.
"""
    
    def xǁValidatorAgentǁ_build_tier3_prompt__mutmut_6(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ) -> str:
        """Build prompt for LLM semantic review."""
        related_context = ""
        if related_files:
            for path, content in list(related_files.items())[:3]:  # Limit context
                related_context -= f"\n### Related File: {path}\n```python\n{content[:2000]}\n```\n"
        
        return f"""You are a Django migration validator. Review the following migration from Django 2.x to 5.x.

## Original Code ({file_path}, type: {file_type}):
```python
{original_code}
```

## Migrated Code:
```python
{migrated_code}
```

{f"## Related Files (already migrated):{related_context}" if related_context else ""}

## Check for these issues:
1. **Logic changes**: Was any business logic accidentally altered?
2. **Missing migrations**: Are there deprecated patterns that weren't updated?
3. **Wrong patterns**: Were migration rules applied incorrectly?
4. **Cross-file breaks**: Does this code reference things in related files that may have changed?

## Output Format:
For each issue found, output EXACTLY one line in this format:
ISSUE|severity|message|line_number|fix_hint

Where severity is: error, warning, or info
If no issues found, output: NO_ISSUES

Only report REAL issues. Do not report false positives.
"""
    
    def xǁValidatorAgentǁ_build_tier3_prompt__mutmut_7(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ) -> str:
        """Build prompt for LLM semantic review."""
        related_context = ""
        if related_files:
            for path, content in list(related_files.items())[:3]:  # Limit context
                related_context += f"\n### Related File: {path}\n```python\n{content[:2001]}\n```\n"
        
        return f"""You are a Django migration validator. Review the following migration from Django 2.x to 5.x.

## Original Code ({file_path}, type: {file_type}):
```python
{original_code}
```

## Migrated Code:
```python
{migrated_code}
```

{f"## Related Files (already migrated):{related_context}" if related_context else ""}

## Check for these issues:
1. **Logic changes**: Was any business logic accidentally altered?
2. **Missing migrations**: Are there deprecated patterns that weren't updated?
3. **Wrong patterns**: Were migration rules applied incorrectly?
4. **Cross-file breaks**: Does this code reference things in related files that may have changed?

## Output Format:
For each issue found, output EXACTLY one line in this format:
ISSUE|severity|message|line_number|fix_hint

Where severity is: error, warning, or info
If no issues found, output: NO_ISSUES

Only report REAL issues. Do not report false positives.
"""
    
    def xǁValidatorAgentǁ_build_tier3_prompt__mutmut_8(
        self,
        file_path: str,
        original_code: str,
        migrated_code: str,
        file_type: str,
        related_files: Optional[Dict[str, str]] = None,
    ) -> str:
        """Build prompt for LLM semantic review."""
        related_context = ""
        if related_files:
            for path, content in list(related_files.items())[:3]:  # Limit context
                related_context += f"\n### Related File: {path}\n```python\n{content[:2000]}\n```\n"
        
        return f"""You are a Django migration validator. Review the following migration from Django 2.x to 5.x.

## Original Code ({file_path}, type: {file_type}):
```python
{original_code}
```

## Migrated Code:
```python
{migrated_code}
```

{f"## Related Files (already migrated):{related_context}" if related_context else "XXXX"}

## Check for these issues:
1. **Logic changes**: Was any business logic accidentally altered?
2. **Missing migrations**: Are there deprecated patterns that weren't updated?
3. **Wrong patterns**: Were migration rules applied incorrectly?
4. **Cross-file breaks**: Does this code reference things in related files that may have changed?

## Output Format:
For each issue found, output EXACTLY one line in this format:
ISSUE|severity|message|line_number|fix_hint

Where severity is: error, warning, or info
If no issues found, output: NO_ISSUES

Only report REAL issues. Do not report false positives.
"""
    
    xǁValidatorAgentǁ_build_tier3_prompt__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁValidatorAgentǁ_build_tier3_prompt__mutmut_1': xǁValidatorAgentǁ_build_tier3_prompt__mutmut_1, 
        'xǁValidatorAgentǁ_build_tier3_prompt__mutmut_2': xǁValidatorAgentǁ_build_tier3_prompt__mutmut_2, 
        'xǁValidatorAgentǁ_build_tier3_prompt__mutmut_3': xǁValidatorAgentǁ_build_tier3_prompt__mutmut_3, 
        'xǁValidatorAgentǁ_build_tier3_prompt__mutmut_4': xǁValidatorAgentǁ_build_tier3_prompt__mutmut_4, 
        'xǁValidatorAgentǁ_build_tier3_prompt__mutmut_5': xǁValidatorAgentǁ_build_tier3_prompt__mutmut_5, 
        'xǁValidatorAgentǁ_build_tier3_prompt__mutmut_6': xǁValidatorAgentǁ_build_tier3_prompt__mutmut_6, 
        'xǁValidatorAgentǁ_build_tier3_prompt__mutmut_7': xǁValidatorAgentǁ_build_tier3_prompt__mutmut_7, 
        'xǁValidatorAgentǁ_build_tier3_prompt__mutmut_8': xǁValidatorAgentǁ_build_tier3_prompt__mutmut_8
    }
    xǁValidatorAgentǁ_build_tier3_prompt__mutmut_orig.__name__ = 'xǁValidatorAgentǁ_build_tier3_prompt'
    
    def _call_llm(self, prompt: str) -> str:
        args = [prompt]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁValidatorAgentǁ_call_llm__mutmut_orig'), object.__getattribute__(self, 'xǁValidatorAgentǁ_call_llm__mutmut_mutants'), args, kwargs, self)
    
    def xǁValidatorAgentǁ_call_llm__mutmut_orig(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"role": "user", "content": prompt}],
                temperature=0.1,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_1(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(None, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"role": "user", "content": prompt}],
                temperature=0.1,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_2(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, None):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"role": "user", "content": prompt}],
                temperature=0.1,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_3(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr('chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"role": "user", "content": prompt}],
                temperature=0.1,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_4(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, ):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"role": "user", "content": prompt}],
                temperature=0.1,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_5(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'XXchatXX'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"role": "user", "content": prompt}],
                temperature=0.1,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_6(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'CHAT'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"role": "user", "content": prompt}],
                temperature=0.1,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_7(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = None
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_8(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model=None,
                messages=[{"role": "user", "content": prompt}],
                temperature=0.1,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_9(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=None,
                temperature=0.1,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_10(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"role": "user", "content": prompt}],
                temperature=None,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_11(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"role": "user", "content": prompt}],
                temperature=0.1,
                max_tokens=None,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_12(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                messages=[{"role": "user", "content": prompt}],
                temperature=0.1,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_13(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                temperature=0.1,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_14(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"role": "user", "content": prompt}],
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_15(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"role": "user", "content": prompt}],
                temperature=0.1,
                )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_16(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="XXllama-3.3-70b-versatileXX",
                messages=[{"role": "user", "content": prompt}],
                temperature=0.1,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_17(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="LLAMA-3.3-70B-VERSATILE",
                messages=[{"role": "user", "content": prompt}],
                temperature=0.1,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_18(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"XXroleXX": "user", "content": prompt}],
                temperature=0.1,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_19(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"ROLE": "user", "content": prompt}],
                temperature=0.1,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_20(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"role": "XXuserXX", "content": prompt}],
                temperature=0.1,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_21(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"role": "USER", "content": prompt}],
                temperature=0.1,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_22(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"role": "user", "XXcontentXX": prompt}],
                temperature=0.1,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_23(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"role": "user", "CONTENT": prompt}],
                temperature=0.1,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_24(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"role": "user", "content": prompt}],
                temperature=1.1,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_25(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"role": "user", "content": prompt}],
                temperature=0.1,
                max_tokens=2001,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_26(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"role": "user", "content": prompt}],
                temperature=0.1,
                max_tokens=2000,
            )
            return response.choices[1].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_27(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"role": "user", "content": prompt}],
                temperature=0.1,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(None, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_28(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"role": "user", "content": prompt}],
                temperature=0.1,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, None):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_29(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"role": "user", "content": prompt}],
                temperature=0.1,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr('generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_30(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"role": "user", "content": prompt}],
                temperature=0.1,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, ):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_31(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"role": "user", "content": prompt}],
                temperature=0.1,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'XXgenerate_contentXX'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_32(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"role": "user", "content": prompt}],
                temperature=0.1,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'GENERATE_CONTENT'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_33(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"role": "user", "content": prompt}],
                temperature=0.1,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = None
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_34(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"role": "user", "content": prompt}],
                temperature=0.1,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(None)
            return response.text
        else:
            raise ValueError("Unknown LLM client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_35(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"role": "user", "content": prompt}],
                temperature=0.1,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError(None)
    
    def xǁValidatorAgentǁ_call_llm__mutmut_36(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"role": "user", "content": prompt}],
                temperature=0.1,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("XXUnknown LLM client typeXX")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_37(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"role": "user", "content": prompt}],
                temperature=0.1,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("unknown llm client type")
    
    def xǁValidatorAgentǁ_call_llm__mutmut_38(self, prompt: str) -> str:
        """Call the LLM backend."""
        if hasattr(self.llm_client, 'chat'):
            # Groq client
            response = self.llm_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"role": "user", "content": prompt}],
                temperature=0.1,
                max_tokens=2000,
            )
            return response.choices[0].message.content
        elif hasattr(self.llm_client, 'generate_content'):
            # Gemini client
            response = self.llm_client.generate_content(prompt)
            return response.text
        else:
            raise ValueError("UNKNOWN LLM CLIENT TYPE")
    
    xǁValidatorAgentǁ_call_llm__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁValidatorAgentǁ_call_llm__mutmut_1': xǁValidatorAgentǁ_call_llm__mutmut_1, 
        'xǁValidatorAgentǁ_call_llm__mutmut_2': xǁValidatorAgentǁ_call_llm__mutmut_2, 
        'xǁValidatorAgentǁ_call_llm__mutmut_3': xǁValidatorAgentǁ_call_llm__mutmut_3, 
        'xǁValidatorAgentǁ_call_llm__mutmut_4': xǁValidatorAgentǁ_call_llm__mutmut_4, 
        'xǁValidatorAgentǁ_call_llm__mutmut_5': xǁValidatorAgentǁ_call_llm__mutmut_5, 
        'xǁValidatorAgentǁ_call_llm__mutmut_6': xǁValidatorAgentǁ_call_llm__mutmut_6, 
        'xǁValidatorAgentǁ_call_llm__mutmut_7': xǁValidatorAgentǁ_call_llm__mutmut_7, 
        'xǁValidatorAgentǁ_call_llm__mutmut_8': xǁValidatorAgentǁ_call_llm__mutmut_8, 
        'xǁValidatorAgentǁ_call_llm__mutmut_9': xǁValidatorAgentǁ_call_llm__mutmut_9, 
        'xǁValidatorAgentǁ_call_llm__mutmut_10': xǁValidatorAgentǁ_call_llm__mutmut_10, 
        'xǁValidatorAgentǁ_call_llm__mutmut_11': xǁValidatorAgentǁ_call_llm__mutmut_11, 
        'xǁValidatorAgentǁ_call_llm__mutmut_12': xǁValidatorAgentǁ_call_llm__mutmut_12, 
        'xǁValidatorAgentǁ_call_llm__mutmut_13': xǁValidatorAgentǁ_call_llm__mutmut_13, 
        'xǁValidatorAgentǁ_call_llm__mutmut_14': xǁValidatorAgentǁ_call_llm__mutmut_14, 
        'xǁValidatorAgentǁ_call_llm__mutmut_15': xǁValidatorAgentǁ_call_llm__mutmut_15, 
        'xǁValidatorAgentǁ_call_llm__mutmut_16': xǁValidatorAgentǁ_call_llm__mutmut_16, 
        'xǁValidatorAgentǁ_call_llm__mutmut_17': xǁValidatorAgentǁ_call_llm__mutmut_17, 
        'xǁValidatorAgentǁ_call_llm__mutmut_18': xǁValidatorAgentǁ_call_llm__mutmut_18, 
        'xǁValidatorAgentǁ_call_llm__mutmut_19': xǁValidatorAgentǁ_call_llm__mutmut_19, 
        'xǁValidatorAgentǁ_call_llm__mutmut_20': xǁValidatorAgentǁ_call_llm__mutmut_20, 
        'xǁValidatorAgentǁ_call_llm__mutmut_21': xǁValidatorAgentǁ_call_llm__mutmut_21, 
        'xǁValidatorAgentǁ_call_llm__mutmut_22': xǁValidatorAgentǁ_call_llm__mutmut_22, 
        'xǁValidatorAgentǁ_call_llm__mutmut_23': xǁValidatorAgentǁ_call_llm__mutmut_23, 
        'xǁValidatorAgentǁ_call_llm__mutmut_24': xǁValidatorAgentǁ_call_llm__mutmut_24, 
        'xǁValidatorAgentǁ_call_llm__mutmut_25': xǁValidatorAgentǁ_call_llm__mutmut_25, 
        'xǁValidatorAgentǁ_call_llm__mutmut_26': xǁValidatorAgentǁ_call_llm__mutmut_26, 
        'xǁValidatorAgentǁ_call_llm__mutmut_27': xǁValidatorAgentǁ_call_llm__mutmut_27, 
        'xǁValidatorAgentǁ_call_llm__mutmut_28': xǁValidatorAgentǁ_call_llm__mutmut_28, 
        'xǁValidatorAgentǁ_call_llm__mutmut_29': xǁValidatorAgentǁ_call_llm__mutmut_29, 
        'xǁValidatorAgentǁ_call_llm__mutmut_30': xǁValidatorAgentǁ_call_llm__mutmut_30, 
        'xǁValidatorAgentǁ_call_llm__mutmut_31': xǁValidatorAgentǁ_call_llm__mutmut_31, 
        'xǁValidatorAgentǁ_call_llm__mutmut_32': xǁValidatorAgentǁ_call_llm__mutmut_32, 
        'xǁValidatorAgentǁ_call_llm__mutmut_33': xǁValidatorAgentǁ_call_llm__mutmut_33, 
        'xǁValidatorAgentǁ_call_llm__mutmut_34': xǁValidatorAgentǁ_call_llm__mutmut_34, 
        'xǁValidatorAgentǁ_call_llm__mutmut_35': xǁValidatorAgentǁ_call_llm__mutmut_35, 
        'xǁValidatorAgentǁ_call_llm__mutmut_36': xǁValidatorAgentǁ_call_llm__mutmut_36, 
        'xǁValidatorAgentǁ_call_llm__mutmut_37': xǁValidatorAgentǁ_call_llm__mutmut_37, 
        'xǁValidatorAgentǁ_call_llm__mutmut_38': xǁValidatorAgentǁ_call_llm__mutmut_38
    }
    xǁValidatorAgentǁ_call_llm__mutmut_orig.__name__ = 'xǁValidatorAgentǁ_call_llm'
    
    def _parse_tier3_response(self, response: str, file_path: str) -> List[ValidationIssue]:
        args = [response, file_path]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁValidatorAgentǁ_parse_tier3_response__mutmut_orig'), object.__getattribute__(self, 'xǁValidatorAgentǁ_parse_tier3_response__mutmut_mutants'), args, kwargs, self)
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_orig(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_1(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = None
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_2(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response and "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_3(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_4(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "XXNO_ISSUESXX" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_5(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "no_issues" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_6(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" not in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_7(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split(None):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_8(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("XX\nXX"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_9(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = None
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_10(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith(None):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_11(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("XXISSUE|XX"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_12(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("issue|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_13(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = None
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_14(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split(None)
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_15(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("XX|XX")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_16(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) > 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_17(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 5:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_18(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = None
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_19(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().upper()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_20(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[2].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_21(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = None
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_22(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[3].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_23(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = ""
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_24(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = ""
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_25(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) > 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_26(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 5:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_27(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = None
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_28(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(None)
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_29(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[4].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_30(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) > 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_31(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 6:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_32(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = None
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_33(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[5].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_34(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity not in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_35(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("XXerrorXX", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_36(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("ERROR", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_37(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "XXwarningXX", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_38(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "WARNING", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_39(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "XXinfoXX"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_40(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "INFO"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_41(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(None)
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_42(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=None,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_43(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=None,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_44(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category=None,
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_45(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=None,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_46(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=None,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_47(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=None,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_48(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=None,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_49(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=None,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_50(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_51(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_52(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_53(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_54(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_55(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_56(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_57(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_58(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=4,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_59(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="XXsemanticXX",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_60(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="SEMANTIC",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=False,
                        ))
        
        return issues
    
    def xǁValidatorAgentǁ_parse_tier3_response__mutmut_61(self, response: str, file_path: str) -> List[ValidationIssue]:
        """Parse LLM response into ValidationIssue objects."""
        issues = []
        
        if not response or "NO_ISSUES" in response:
            return issues
        
        for line in response.strip().split("\n"):
            line = line.strip()
            if line.startswith("ISSUE|"):
                parts = line.split("|")
                if len(parts) >= 4:
                    severity = parts[1].strip().lower()
                    message = parts[2].strip()
                    line_num = None
                    fix_hint = None
                    
                    if len(parts) >= 4:
                        try:
                            line_num = int(parts[3].strip())
                        except (ValueError, IndexError):
                            pass
                    if len(parts) >= 5:
                        fix_hint = parts[4].strip()
                    
                    if severity in ("error", "warning", "info"):
                        issues.append(ValidationIssue(
                            tier=3,
                            severity=severity,
                            category="semantic",
                            message=message,
                            file_path=file_path,
                            line_number=line_num,
                            fix_hint=fix_hint,
                            auto_fixable=True,
                        ))
        
        return issues
    
    xǁValidatorAgentǁ_parse_tier3_response__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁValidatorAgentǁ_parse_tier3_response__mutmut_1': xǁValidatorAgentǁ_parse_tier3_response__mutmut_1, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_2': xǁValidatorAgentǁ_parse_tier3_response__mutmut_2, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_3': xǁValidatorAgentǁ_parse_tier3_response__mutmut_3, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_4': xǁValidatorAgentǁ_parse_tier3_response__mutmut_4, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_5': xǁValidatorAgentǁ_parse_tier3_response__mutmut_5, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_6': xǁValidatorAgentǁ_parse_tier3_response__mutmut_6, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_7': xǁValidatorAgentǁ_parse_tier3_response__mutmut_7, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_8': xǁValidatorAgentǁ_parse_tier3_response__mutmut_8, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_9': xǁValidatorAgentǁ_parse_tier3_response__mutmut_9, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_10': xǁValidatorAgentǁ_parse_tier3_response__mutmut_10, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_11': xǁValidatorAgentǁ_parse_tier3_response__mutmut_11, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_12': xǁValidatorAgentǁ_parse_tier3_response__mutmut_12, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_13': xǁValidatorAgentǁ_parse_tier3_response__mutmut_13, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_14': xǁValidatorAgentǁ_parse_tier3_response__mutmut_14, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_15': xǁValidatorAgentǁ_parse_tier3_response__mutmut_15, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_16': xǁValidatorAgentǁ_parse_tier3_response__mutmut_16, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_17': xǁValidatorAgentǁ_parse_tier3_response__mutmut_17, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_18': xǁValidatorAgentǁ_parse_tier3_response__mutmut_18, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_19': xǁValidatorAgentǁ_parse_tier3_response__mutmut_19, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_20': xǁValidatorAgentǁ_parse_tier3_response__mutmut_20, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_21': xǁValidatorAgentǁ_parse_tier3_response__mutmut_21, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_22': xǁValidatorAgentǁ_parse_tier3_response__mutmut_22, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_23': xǁValidatorAgentǁ_parse_tier3_response__mutmut_23, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_24': xǁValidatorAgentǁ_parse_tier3_response__mutmut_24, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_25': xǁValidatorAgentǁ_parse_tier3_response__mutmut_25, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_26': xǁValidatorAgentǁ_parse_tier3_response__mutmut_26, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_27': xǁValidatorAgentǁ_parse_tier3_response__mutmut_27, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_28': xǁValidatorAgentǁ_parse_tier3_response__mutmut_28, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_29': xǁValidatorAgentǁ_parse_tier3_response__mutmut_29, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_30': xǁValidatorAgentǁ_parse_tier3_response__mutmut_30, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_31': xǁValidatorAgentǁ_parse_tier3_response__mutmut_31, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_32': xǁValidatorAgentǁ_parse_tier3_response__mutmut_32, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_33': xǁValidatorAgentǁ_parse_tier3_response__mutmut_33, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_34': xǁValidatorAgentǁ_parse_tier3_response__mutmut_34, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_35': xǁValidatorAgentǁ_parse_tier3_response__mutmut_35, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_36': xǁValidatorAgentǁ_parse_tier3_response__mutmut_36, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_37': xǁValidatorAgentǁ_parse_tier3_response__mutmut_37, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_38': xǁValidatorAgentǁ_parse_tier3_response__mutmut_38, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_39': xǁValidatorAgentǁ_parse_tier3_response__mutmut_39, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_40': xǁValidatorAgentǁ_parse_tier3_response__mutmut_40, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_41': xǁValidatorAgentǁ_parse_tier3_response__mutmut_41, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_42': xǁValidatorAgentǁ_parse_tier3_response__mutmut_42, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_43': xǁValidatorAgentǁ_parse_tier3_response__mutmut_43, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_44': xǁValidatorAgentǁ_parse_tier3_response__mutmut_44, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_45': xǁValidatorAgentǁ_parse_tier3_response__mutmut_45, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_46': xǁValidatorAgentǁ_parse_tier3_response__mutmut_46, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_47': xǁValidatorAgentǁ_parse_tier3_response__mutmut_47, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_48': xǁValidatorAgentǁ_parse_tier3_response__mutmut_48, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_49': xǁValidatorAgentǁ_parse_tier3_response__mutmut_49, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_50': xǁValidatorAgentǁ_parse_tier3_response__mutmut_50, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_51': xǁValidatorAgentǁ_parse_tier3_response__mutmut_51, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_52': xǁValidatorAgentǁ_parse_tier3_response__mutmut_52, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_53': xǁValidatorAgentǁ_parse_tier3_response__mutmut_53, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_54': xǁValidatorAgentǁ_parse_tier3_response__mutmut_54, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_55': xǁValidatorAgentǁ_parse_tier3_response__mutmut_55, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_56': xǁValidatorAgentǁ_parse_tier3_response__mutmut_56, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_57': xǁValidatorAgentǁ_parse_tier3_response__mutmut_57, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_58': xǁValidatorAgentǁ_parse_tier3_response__mutmut_58, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_59': xǁValidatorAgentǁ_parse_tier3_response__mutmut_59, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_60': xǁValidatorAgentǁ_parse_tier3_response__mutmut_60, 
        'xǁValidatorAgentǁ_parse_tier3_response__mutmut_61': xǁValidatorAgentǁ_parse_tier3_response__mutmut_61
    }
    xǁValidatorAgentǁ_parse_tier3_response__mutmut_orig.__name__ = 'xǁValidatorAgentǁ_parse_tier3_response'

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def _run_tier4_docker(self, migration_context) -> List[ValidationIssue]:
        args = [migration_context]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁValidatorAgentǁ_run_tier4_docker__mutmut_orig'), object.__getattribute__(self, 'xǁValidatorAgentǁ_run_tier4_docker__mutmut_mutants'), args, kwargs, self)

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_orig(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_1(self, migration_context) -> List[ValidationIssue]:
        issues = None
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_2(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = None
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_3(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(None, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_4(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, None)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_5(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_6(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, )
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_7(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(None, exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_8(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=None)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_9(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_10(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), )
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_11(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(None), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_12(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=False)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_13(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(None, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_14(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, None, encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_15(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding=None) as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_16(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open("w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_17(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_18(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", ) as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_19(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "XXwXX", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_20(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "W", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_21(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="XXutf-8XX") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_22(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="UTF-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_23(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(None)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_24(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = None
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_25(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(None, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_26(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, None)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_27(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_28(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, )
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_29(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(None, exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_30(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=None)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_31(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_32(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), )
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_33(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(None), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_34(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=False)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_35(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(None, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_36(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, None, encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_37(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding=None) as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_38(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open("w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_39(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_40(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", ) as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_41(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "XXwXX", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_42(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "W", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_43(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="XXutf-8XX") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_44(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="UTF-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_45(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(None)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_46(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = ""
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_47(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith(None):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_48(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("XXmanage.pyXX"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_49(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("MANAGE.PY"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_50(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = None
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_51(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        return
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_52(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_53(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(None)
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_54(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=None, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_55(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity=None, category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_56(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category=None,
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_57(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message=None,
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_58(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=None
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_59(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_60(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_61(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_62(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_63(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_64(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=5, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_65(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="XXwarningXX", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_66(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="WARNING", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_67(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="XXruntimeXX",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_68(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="RUNTIME",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_69(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="XXmanage.py not found in contextual files. Skipping Django container tests.XX",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_70(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. skipping django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_71(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="MANAGE.PY NOT FOUND IN CONTEXTUAL FILES. SKIPPING DJANGO CONTAINER TESTS.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_72(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path="XXXX"
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_73(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = None
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_74(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(None)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_75(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = None
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_76(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip(None)
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_77(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".lstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_78(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('XX/XX')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_79(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = None
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_80(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(None, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_81(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, None)
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_82(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join("requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_83(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, )
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_84(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "XXrequirements.txtXX")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_85(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "REQUIREMENTS.TXT")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_86(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = None
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_87(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "XXpip install django==5.1.0XX"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_88(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "PIP INSTALL DJANGO==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_89(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(None):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_90(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = None
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_91(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "XXpip install -r requirements.txt && pip install django==5.1.0 --upgradeXX"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_92(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "PIP INSTALL -R REQUIREMENTS.TXT && PIP INSTALL DJANGO==5.1.0 --UPGRADE"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_93(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = None
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_94(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = None
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_95(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image=None,
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_96(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=None,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_97(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes=None,
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_98(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=None,
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_99(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=None,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_100(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=None,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_101(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=None
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_102(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_103(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_104(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_105(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_106(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_107(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_108(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_109(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="XXpython:3.12-slimXX",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_110(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="PYTHON:3.12-SLIM",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_111(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'XXbindXX': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_112(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'BIND': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_113(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': 'XX/appXX', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_114(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/APP', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_115(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'XXmodeXX': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_116(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'MODE': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_117(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'XXrwXX'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_118(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'RW'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_119(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir and "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_120(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "XX/appXX",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_121(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/APP",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_122(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=True,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_123(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=False,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_124(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=91
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_125(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(None)
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_126(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = None
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_127(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode(None) if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_128(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('XXutf-8XX') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_129(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('UTF-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_130(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else "XXXX"
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_131(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = None
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_132(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode(None) if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_133(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('XXutf-8XX') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_134(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('UTF-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_135(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else "XXXX"
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_136(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = None
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_137(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr and stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_138(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(None)
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_139(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = None
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_140(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = "XXXX"
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_141(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path not in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_142(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = None
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_143(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        return
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_144(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(None)
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_145(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=None, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_146(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity=None, category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_147(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category=None,
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_148(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=None,
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_149(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=None
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_150(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_151(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_152(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_153(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_154(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_155(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=5, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_156(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="XXerrorXX", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_157(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="ERROR", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_158(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="XXruntimeXX",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_159(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="RUNTIME",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_160(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(None)
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_161(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=None, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_162(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity=None, category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_163(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category=None,
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_164(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=None,
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_165(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=None
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_166(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_167(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_168(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_169(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_170(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_171(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=5, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_172(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="XXerrorXX", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_173(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="ERROR", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_174(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="XXruntimeXX",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_175(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="RUNTIME",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_176(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(None)}",
                    file_path=""
                ))
        return issues

    # --- TIER 4: Containerized Runtime Testing ----------------------------------------

    def xǁValidatorAgentǁ_run_tier4_docker__mutmut_177(self, migration_context) -> List[ValidationIssue]:
        issues = []
        with tempfile.TemporaryDirectory() as temp_dir:
            try:
                # 1. Write the full contextual tree
                for file_path, content in migration_context.original_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)
                
                # 2. Overlay migrated files
                for file_path, content in migration_context.migrated_files.items():
                    full_path = os.path.join(temp_dir, file_path)
                    os.makedirs(os.path.dirname(full_path), exist_ok=True)
                    with open(full_path, "w", encoding="utf-8") as f:
                        f.write(content)

                # Find manage.py in the root or close to root
                manage_py_path = None
                for file_path in migration_context.original_files:
                    if file_path.endswith("manage.py"):
                        manage_py_path = file_path
                        break
                
                if not manage_py_path:
                    issues.append(ValidationIssue(
                        tier=4, severity="warning", category="runtime",
                        message="manage.py not found in contextual files. Skipping Django container tests.",
                        file_path=""
                    ))
                    return issues

                manage_py_dir = os.path.dirname(manage_py_path)
                container_run_dir = f"/app/{manage_py_dir}".rstrip('/')
                
                # Check for requirements.txt to install dependencies
                req_path = os.path.join(temp_dir, "requirements.txt")
                install_cmd = "pip install django==5.1.0"
                if os.path.exists(req_path):
                    install_cmd = "pip install -r requirements.txt && pip install django==5.1.0 --upgrade"
                
                # First run system checks, then run tests if the check passes
                # If they don't have tests, manage.py test will just run 0 tests and succeed.
                cmd = f"sh -c '{install_cmd} && python manage.py check && python manage.py test'"
                
                container = self.docker_client.containers.run(
                    image="python:3.12-slim",
                    command=cmd,
                    volumes={temp_dir: {'bind': '/app', 'mode': 'rw'}},
                    working_dir=container_run_dir or "/app",
                    detach=False,
                    remove=True,
                    timeout=90
                )
                migration_context.add_log(f"[VALIDATOR] Docker container check succeeded.")
                
            except docker.errors.ContainerError as e:
                stderr = e.stderr.decode('utf-8') if e.stderr else ""
                stdout = e.stdout.decode('utf-8') if e.stdout else ""
                output = stderr or stdout
                
                migration_context.add_log(f"[VALIDATOR] Docker container runtime failed:\n{output}")
                
                # Try to extract a file name from traceback
                guessed_file = ""
                for file_path in migration_context.migrated_files:
                    if file_path in output:
                        guessed_file = file_path
                        break
                
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Runtime error during `manage.py check` or `manage.py test`:\n\n{output}",
                    file_path=guessed_file
                ))
            except Exception as e:
                issues.append(ValidationIssue(
                    tier=4, severity="error", category="runtime",
                    message=f"Docker orchestration failed: {str(e)}",
                    file_path="XXXX"
                ))
        return issues
    
    xǁValidatorAgentǁ_run_tier4_docker__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁValidatorAgentǁ_run_tier4_docker__mutmut_1': xǁValidatorAgentǁ_run_tier4_docker__mutmut_1, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_2': xǁValidatorAgentǁ_run_tier4_docker__mutmut_2, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_3': xǁValidatorAgentǁ_run_tier4_docker__mutmut_3, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_4': xǁValidatorAgentǁ_run_tier4_docker__mutmut_4, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_5': xǁValidatorAgentǁ_run_tier4_docker__mutmut_5, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_6': xǁValidatorAgentǁ_run_tier4_docker__mutmut_6, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_7': xǁValidatorAgentǁ_run_tier4_docker__mutmut_7, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_8': xǁValidatorAgentǁ_run_tier4_docker__mutmut_8, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_9': xǁValidatorAgentǁ_run_tier4_docker__mutmut_9, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_10': xǁValidatorAgentǁ_run_tier4_docker__mutmut_10, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_11': xǁValidatorAgentǁ_run_tier4_docker__mutmut_11, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_12': xǁValidatorAgentǁ_run_tier4_docker__mutmut_12, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_13': xǁValidatorAgentǁ_run_tier4_docker__mutmut_13, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_14': xǁValidatorAgentǁ_run_tier4_docker__mutmut_14, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_15': xǁValidatorAgentǁ_run_tier4_docker__mutmut_15, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_16': xǁValidatorAgentǁ_run_tier4_docker__mutmut_16, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_17': xǁValidatorAgentǁ_run_tier4_docker__mutmut_17, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_18': xǁValidatorAgentǁ_run_tier4_docker__mutmut_18, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_19': xǁValidatorAgentǁ_run_tier4_docker__mutmut_19, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_20': xǁValidatorAgentǁ_run_tier4_docker__mutmut_20, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_21': xǁValidatorAgentǁ_run_tier4_docker__mutmut_21, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_22': xǁValidatorAgentǁ_run_tier4_docker__mutmut_22, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_23': xǁValidatorAgentǁ_run_tier4_docker__mutmut_23, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_24': xǁValidatorAgentǁ_run_tier4_docker__mutmut_24, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_25': xǁValidatorAgentǁ_run_tier4_docker__mutmut_25, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_26': xǁValidatorAgentǁ_run_tier4_docker__mutmut_26, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_27': xǁValidatorAgentǁ_run_tier4_docker__mutmut_27, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_28': xǁValidatorAgentǁ_run_tier4_docker__mutmut_28, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_29': xǁValidatorAgentǁ_run_tier4_docker__mutmut_29, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_30': xǁValidatorAgentǁ_run_tier4_docker__mutmut_30, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_31': xǁValidatorAgentǁ_run_tier4_docker__mutmut_31, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_32': xǁValidatorAgentǁ_run_tier4_docker__mutmut_32, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_33': xǁValidatorAgentǁ_run_tier4_docker__mutmut_33, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_34': xǁValidatorAgentǁ_run_tier4_docker__mutmut_34, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_35': xǁValidatorAgentǁ_run_tier4_docker__mutmut_35, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_36': xǁValidatorAgentǁ_run_tier4_docker__mutmut_36, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_37': xǁValidatorAgentǁ_run_tier4_docker__mutmut_37, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_38': xǁValidatorAgentǁ_run_tier4_docker__mutmut_38, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_39': xǁValidatorAgentǁ_run_tier4_docker__mutmut_39, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_40': xǁValidatorAgentǁ_run_tier4_docker__mutmut_40, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_41': xǁValidatorAgentǁ_run_tier4_docker__mutmut_41, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_42': xǁValidatorAgentǁ_run_tier4_docker__mutmut_42, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_43': xǁValidatorAgentǁ_run_tier4_docker__mutmut_43, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_44': xǁValidatorAgentǁ_run_tier4_docker__mutmut_44, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_45': xǁValidatorAgentǁ_run_tier4_docker__mutmut_45, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_46': xǁValidatorAgentǁ_run_tier4_docker__mutmut_46, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_47': xǁValidatorAgentǁ_run_tier4_docker__mutmut_47, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_48': xǁValidatorAgentǁ_run_tier4_docker__mutmut_48, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_49': xǁValidatorAgentǁ_run_tier4_docker__mutmut_49, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_50': xǁValidatorAgentǁ_run_tier4_docker__mutmut_50, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_51': xǁValidatorAgentǁ_run_tier4_docker__mutmut_51, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_52': xǁValidatorAgentǁ_run_tier4_docker__mutmut_52, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_53': xǁValidatorAgentǁ_run_tier4_docker__mutmut_53, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_54': xǁValidatorAgentǁ_run_tier4_docker__mutmut_54, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_55': xǁValidatorAgentǁ_run_tier4_docker__mutmut_55, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_56': xǁValidatorAgentǁ_run_tier4_docker__mutmut_56, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_57': xǁValidatorAgentǁ_run_tier4_docker__mutmut_57, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_58': xǁValidatorAgentǁ_run_tier4_docker__mutmut_58, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_59': xǁValidatorAgentǁ_run_tier4_docker__mutmut_59, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_60': xǁValidatorAgentǁ_run_tier4_docker__mutmut_60, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_61': xǁValidatorAgentǁ_run_tier4_docker__mutmut_61, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_62': xǁValidatorAgentǁ_run_tier4_docker__mutmut_62, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_63': xǁValidatorAgentǁ_run_tier4_docker__mutmut_63, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_64': xǁValidatorAgentǁ_run_tier4_docker__mutmut_64, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_65': xǁValidatorAgentǁ_run_tier4_docker__mutmut_65, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_66': xǁValidatorAgentǁ_run_tier4_docker__mutmut_66, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_67': xǁValidatorAgentǁ_run_tier4_docker__mutmut_67, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_68': xǁValidatorAgentǁ_run_tier4_docker__mutmut_68, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_69': xǁValidatorAgentǁ_run_tier4_docker__mutmut_69, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_70': xǁValidatorAgentǁ_run_tier4_docker__mutmut_70, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_71': xǁValidatorAgentǁ_run_tier4_docker__mutmut_71, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_72': xǁValidatorAgentǁ_run_tier4_docker__mutmut_72, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_73': xǁValidatorAgentǁ_run_tier4_docker__mutmut_73, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_74': xǁValidatorAgentǁ_run_tier4_docker__mutmut_74, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_75': xǁValidatorAgentǁ_run_tier4_docker__mutmut_75, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_76': xǁValidatorAgentǁ_run_tier4_docker__mutmut_76, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_77': xǁValidatorAgentǁ_run_tier4_docker__mutmut_77, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_78': xǁValidatorAgentǁ_run_tier4_docker__mutmut_78, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_79': xǁValidatorAgentǁ_run_tier4_docker__mutmut_79, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_80': xǁValidatorAgentǁ_run_tier4_docker__mutmut_80, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_81': xǁValidatorAgentǁ_run_tier4_docker__mutmut_81, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_82': xǁValidatorAgentǁ_run_tier4_docker__mutmut_82, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_83': xǁValidatorAgentǁ_run_tier4_docker__mutmut_83, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_84': xǁValidatorAgentǁ_run_tier4_docker__mutmut_84, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_85': xǁValidatorAgentǁ_run_tier4_docker__mutmut_85, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_86': xǁValidatorAgentǁ_run_tier4_docker__mutmut_86, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_87': xǁValidatorAgentǁ_run_tier4_docker__mutmut_87, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_88': xǁValidatorAgentǁ_run_tier4_docker__mutmut_88, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_89': xǁValidatorAgentǁ_run_tier4_docker__mutmut_89, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_90': xǁValidatorAgentǁ_run_tier4_docker__mutmut_90, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_91': xǁValidatorAgentǁ_run_tier4_docker__mutmut_91, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_92': xǁValidatorAgentǁ_run_tier4_docker__mutmut_92, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_93': xǁValidatorAgentǁ_run_tier4_docker__mutmut_93, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_94': xǁValidatorAgentǁ_run_tier4_docker__mutmut_94, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_95': xǁValidatorAgentǁ_run_tier4_docker__mutmut_95, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_96': xǁValidatorAgentǁ_run_tier4_docker__mutmut_96, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_97': xǁValidatorAgentǁ_run_tier4_docker__mutmut_97, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_98': xǁValidatorAgentǁ_run_tier4_docker__mutmut_98, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_99': xǁValidatorAgentǁ_run_tier4_docker__mutmut_99, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_100': xǁValidatorAgentǁ_run_tier4_docker__mutmut_100, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_101': xǁValidatorAgentǁ_run_tier4_docker__mutmut_101, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_102': xǁValidatorAgentǁ_run_tier4_docker__mutmut_102, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_103': xǁValidatorAgentǁ_run_tier4_docker__mutmut_103, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_104': xǁValidatorAgentǁ_run_tier4_docker__mutmut_104, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_105': xǁValidatorAgentǁ_run_tier4_docker__mutmut_105, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_106': xǁValidatorAgentǁ_run_tier4_docker__mutmut_106, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_107': xǁValidatorAgentǁ_run_tier4_docker__mutmut_107, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_108': xǁValidatorAgentǁ_run_tier4_docker__mutmut_108, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_109': xǁValidatorAgentǁ_run_tier4_docker__mutmut_109, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_110': xǁValidatorAgentǁ_run_tier4_docker__mutmut_110, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_111': xǁValidatorAgentǁ_run_tier4_docker__mutmut_111, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_112': xǁValidatorAgentǁ_run_tier4_docker__mutmut_112, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_113': xǁValidatorAgentǁ_run_tier4_docker__mutmut_113, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_114': xǁValidatorAgentǁ_run_tier4_docker__mutmut_114, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_115': xǁValidatorAgentǁ_run_tier4_docker__mutmut_115, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_116': xǁValidatorAgentǁ_run_tier4_docker__mutmut_116, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_117': xǁValidatorAgentǁ_run_tier4_docker__mutmut_117, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_118': xǁValidatorAgentǁ_run_tier4_docker__mutmut_118, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_119': xǁValidatorAgentǁ_run_tier4_docker__mutmut_119, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_120': xǁValidatorAgentǁ_run_tier4_docker__mutmut_120, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_121': xǁValidatorAgentǁ_run_tier4_docker__mutmut_121, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_122': xǁValidatorAgentǁ_run_tier4_docker__mutmut_122, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_123': xǁValidatorAgentǁ_run_tier4_docker__mutmut_123, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_124': xǁValidatorAgentǁ_run_tier4_docker__mutmut_124, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_125': xǁValidatorAgentǁ_run_tier4_docker__mutmut_125, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_126': xǁValidatorAgentǁ_run_tier4_docker__mutmut_126, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_127': xǁValidatorAgentǁ_run_tier4_docker__mutmut_127, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_128': xǁValidatorAgentǁ_run_tier4_docker__mutmut_128, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_129': xǁValidatorAgentǁ_run_tier4_docker__mutmut_129, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_130': xǁValidatorAgentǁ_run_tier4_docker__mutmut_130, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_131': xǁValidatorAgentǁ_run_tier4_docker__mutmut_131, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_132': xǁValidatorAgentǁ_run_tier4_docker__mutmut_132, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_133': xǁValidatorAgentǁ_run_tier4_docker__mutmut_133, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_134': xǁValidatorAgentǁ_run_tier4_docker__mutmut_134, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_135': xǁValidatorAgentǁ_run_tier4_docker__mutmut_135, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_136': xǁValidatorAgentǁ_run_tier4_docker__mutmut_136, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_137': xǁValidatorAgentǁ_run_tier4_docker__mutmut_137, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_138': xǁValidatorAgentǁ_run_tier4_docker__mutmut_138, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_139': xǁValidatorAgentǁ_run_tier4_docker__mutmut_139, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_140': xǁValidatorAgentǁ_run_tier4_docker__mutmut_140, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_141': xǁValidatorAgentǁ_run_tier4_docker__mutmut_141, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_142': xǁValidatorAgentǁ_run_tier4_docker__mutmut_142, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_143': xǁValidatorAgentǁ_run_tier4_docker__mutmut_143, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_144': xǁValidatorAgentǁ_run_tier4_docker__mutmut_144, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_145': xǁValidatorAgentǁ_run_tier4_docker__mutmut_145, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_146': xǁValidatorAgentǁ_run_tier4_docker__mutmut_146, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_147': xǁValidatorAgentǁ_run_tier4_docker__mutmut_147, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_148': xǁValidatorAgentǁ_run_tier4_docker__mutmut_148, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_149': xǁValidatorAgentǁ_run_tier4_docker__mutmut_149, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_150': xǁValidatorAgentǁ_run_tier4_docker__mutmut_150, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_151': xǁValidatorAgentǁ_run_tier4_docker__mutmut_151, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_152': xǁValidatorAgentǁ_run_tier4_docker__mutmut_152, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_153': xǁValidatorAgentǁ_run_tier4_docker__mutmut_153, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_154': xǁValidatorAgentǁ_run_tier4_docker__mutmut_154, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_155': xǁValidatorAgentǁ_run_tier4_docker__mutmut_155, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_156': xǁValidatorAgentǁ_run_tier4_docker__mutmut_156, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_157': xǁValidatorAgentǁ_run_tier4_docker__mutmut_157, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_158': xǁValidatorAgentǁ_run_tier4_docker__mutmut_158, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_159': xǁValidatorAgentǁ_run_tier4_docker__mutmut_159, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_160': xǁValidatorAgentǁ_run_tier4_docker__mutmut_160, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_161': xǁValidatorAgentǁ_run_tier4_docker__mutmut_161, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_162': xǁValidatorAgentǁ_run_tier4_docker__mutmut_162, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_163': xǁValidatorAgentǁ_run_tier4_docker__mutmut_163, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_164': xǁValidatorAgentǁ_run_tier4_docker__mutmut_164, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_165': xǁValidatorAgentǁ_run_tier4_docker__mutmut_165, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_166': xǁValidatorAgentǁ_run_tier4_docker__mutmut_166, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_167': xǁValidatorAgentǁ_run_tier4_docker__mutmut_167, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_168': xǁValidatorAgentǁ_run_tier4_docker__mutmut_168, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_169': xǁValidatorAgentǁ_run_tier4_docker__mutmut_169, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_170': xǁValidatorAgentǁ_run_tier4_docker__mutmut_170, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_171': xǁValidatorAgentǁ_run_tier4_docker__mutmut_171, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_172': xǁValidatorAgentǁ_run_tier4_docker__mutmut_172, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_173': xǁValidatorAgentǁ_run_tier4_docker__mutmut_173, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_174': xǁValidatorAgentǁ_run_tier4_docker__mutmut_174, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_175': xǁValidatorAgentǁ_run_tier4_docker__mutmut_175, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_176': xǁValidatorAgentǁ_run_tier4_docker__mutmut_176, 
        'xǁValidatorAgentǁ_run_tier4_docker__mutmut_177': xǁValidatorAgentǁ_run_tier4_docker__mutmut_177
    }
    xǁValidatorAgentǁ_run_tier4_docker__mutmut_orig.__name__ = 'xǁValidatorAgentǁ_run_tier4_docker'

    @staticmethod
    def format_errors_for_prompt(errors: List[Dict]) -> str:
        """Backward compatibility for formatting error output."""
        return "\n".join([f"- {e.get('severity', 'Error').upper()}: {e.get('message', '')} "
                          f"(Line {e.get('line_number', '?')})" for e in errors])
                          
    def validate(self, code: str) -> Dict[str, Any]:
        args = [code]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁValidatorAgentǁvalidate__mutmut_orig'), object.__getattribute__(self, 'xǁValidatorAgentǁvalidate__mutmut_mutants'), args, kwargs, self)
    def xǁValidatorAgentǁvalidate__mutmut_orig(self, code: str) -> Dict[str, Any]:
        """Backward compatibility wrapper for singular code validation."""
        report = ValidationReport("unknown.py")
        self._run_tier1(report, "unknown.py", code, "unknown")
        
        return {
            "is_valid": report.passed,
            "errors": [i.to_dict() for i in report.issues if i.severity == "error"]
        }
    def xǁValidatorAgentǁvalidate__mutmut_1(self, code: str) -> Dict[str, Any]:
        """Backward compatibility wrapper for singular code validation."""
        report = None
        self._run_tier1(report, "unknown.py", code, "unknown")
        
        return {
            "is_valid": report.passed,
            "errors": [i.to_dict() for i in report.issues if i.severity == "error"]
        }
    def xǁValidatorAgentǁvalidate__mutmut_2(self, code: str) -> Dict[str, Any]:
        """Backward compatibility wrapper for singular code validation."""
        report = ValidationReport(None)
        self._run_tier1(report, "unknown.py", code, "unknown")
        
        return {
            "is_valid": report.passed,
            "errors": [i.to_dict() for i in report.issues if i.severity == "error"]
        }
    def xǁValidatorAgentǁvalidate__mutmut_3(self, code: str) -> Dict[str, Any]:
        """Backward compatibility wrapper for singular code validation."""
        report = ValidationReport("XXunknown.pyXX")
        self._run_tier1(report, "unknown.py", code, "unknown")
        
        return {
            "is_valid": report.passed,
            "errors": [i.to_dict() for i in report.issues if i.severity == "error"]
        }
    def xǁValidatorAgentǁvalidate__mutmut_4(self, code: str) -> Dict[str, Any]:
        """Backward compatibility wrapper for singular code validation."""
        report = ValidationReport("UNKNOWN.PY")
        self._run_tier1(report, "unknown.py", code, "unknown")
        
        return {
            "is_valid": report.passed,
            "errors": [i.to_dict() for i in report.issues if i.severity == "error"]
        }
    def xǁValidatorAgentǁvalidate__mutmut_5(self, code: str) -> Dict[str, Any]:
        """Backward compatibility wrapper for singular code validation."""
        report = ValidationReport("unknown.py")
        self._run_tier1(None, "unknown.py", code, "unknown")
        
        return {
            "is_valid": report.passed,
            "errors": [i.to_dict() for i in report.issues if i.severity == "error"]
        }
    def xǁValidatorAgentǁvalidate__mutmut_6(self, code: str) -> Dict[str, Any]:
        """Backward compatibility wrapper for singular code validation."""
        report = ValidationReport("unknown.py")
        self._run_tier1(report, None, code, "unknown")
        
        return {
            "is_valid": report.passed,
            "errors": [i.to_dict() for i in report.issues if i.severity == "error"]
        }
    def xǁValidatorAgentǁvalidate__mutmut_7(self, code: str) -> Dict[str, Any]:
        """Backward compatibility wrapper for singular code validation."""
        report = ValidationReport("unknown.py")
        self._run_tier1(report, "unknown.py", None, "unknown")
        
        return {
            "is_valid": report.passed,
            "errors": [i.to_dict() for i in report.issues if i.severity == "error"]
        }
    def xǁValidatorAgentǁvalidate__mutmut_8(self, code: str) -> Dict[str, Any]:
        """Backward compatibility wrapper for singular code validation."""
        report = ValidationReport("unknown.py")
        self._run_tier1(report, "unknown.py", code, None)
        
        return {
            "is_valid": report.passed,
            "errors": [i.to_dict() for i in report.issues if i.severity == "error"]
        }
    def xǁValidatorAgentǁvalidate__mutmut_9(self, code: str) -> Dict[str, Any]:
        """Backward compatibility wrapper for singular code validation."""
        report = ValidationReport("unknown.py")
        self._run_tier1("unknown.py", code, "unknown")
        
        return {
            "is_valid": report.passed,
            "errors": [i.to_dict() for i in report.issues if i.severity == "error"]
        }
    def xǁValidatorAgentǁvalidate__mutmut_10(self, code: str) -> Dict[str, Any]:
        """Backward compatibility wrapper for singular code validation."""
        report = ValidationReport("unknown.py")
        self._run_tier1(report, code, "unknown")
        
        return {
            "is_valid": report.passed,
            "errors": [i.to_dict() for i in report.issues if i.severity == "error"]
        }
    def xǁValidatorAgentǁvalidate__mutmut_11(self, code: str) -> Dict[str, Any]:
        """Backward compatibility wrapper for singular code validation."""
        report = ValidationReport("unknown.py")
        self._run_tier1(report, "unknown.py", "unknown")
        
        return {
            "is_valid": report.passed,
            "errors": [i.to_dict() for i in report.issues if i.severity == "error"]
        }
    def xǁValidatorAgentǁvalidate__mutmut_12(self, code: str) -> Dict[str, Any]:
        """Backward compatibility wrapper for singular code validation."""
        report = ValidationReport("unknown.py")
        self._run_tier1(report, "unknown.py", code, )
        
        return {
            "is_valid": report.passed,
            "errors": [i.to_dict() for i in report.issues if i.severity == "error"]
        }
    def xǁValidatorAgentǁvalidate__mutmut_13(self, code: str) -> Dict[str, Any]:
        """Backward compatibility wrapper for singular code validation."""
        report = ValidationReport("unknown.py")
        self._run_tier1(report, "XXunknown.pyXX", code, "unknown")
        
        return {
            "is_valid": report.passed,
            "errors": [i.to_dict() for i in report.issues if i.severity == "error"]
        }
    def xǁValidatorAgentǁvalidate__mutmut_14(self, code: str) -> Dict[str, Any]:
        """Backward compatibility wrapper for singular code validation."""
        report = ValidationReport("unknown.py")
        self._run_tier1(report, "UNKNOWN.PY", code, "unknown")
        
        return {
            "is_valid": report.passed,
            "errors": [i.to_dict() for i in report.issues if i.severity == "error"]
        }
    def xǁValidatorAgentǁvalidate__mutmut_15(self, code: str) -> Dict[str, Any]:
        """Backward compatibility wrapper for singular code validation."""
        report = ValidationReport("unknown.py")
        self._run_tier1(report, "unknown.py", code, "XXunknownXX")
        
        return {
            "is_valid": report.passed,
            "errors": [i.to_dict() for i in report.issues if i.severity == "error"]
        }
    def xǁValidatorAgentǁvalidate__mutmut_16(self, code: str) -> Dict[str, Any]:
        """Backward compatibility wrapper for singular code validation."""
        report = ValidationReport("unknown.py")
        self._run_tier1(report, "unknown.py", code, "UNKNOWN")
        
        return {
            "is_valid": report.passed,
            "errors": [i.to_dict() for i in report.issues if i.severity == "error"]
        }
    def xǁValidatorAgentǁvalidate__mutmut_17(self, code: str) -> Dict[str, Any]:
        """Backward compatibility wrapper for singular code validation."""
        report = ValidationReport("unknown.py")
        self._run_tier1(report, "unknown.py", code, "unknown")
        
        return {
            "XXis_validXX": report.passed,
            "errors": [i.to_dict() for i in report.issues if i.severity == "error"]
        }
    def xǁValidatorAgentǁvalidate__mutmut_18(self, code: str) -> Dict[str, Any]:
        """Backward compatibility wrapper for singular code validation."""
        report = ValidationReport("unknown.py")
        self._run_tier1(report, "unknown.py", code, "unknown")
        
        return {
            "IS_VALID": report.passed,
            "errors": [i.to_dict() for i in report.issues if i.severity == "error"]
        }
    def xǁValidatorAgentǁvalidate__mutmut_19(self, code: str) -> Dict[str, Any]:
        """Backward compatibility wrapper for singular code validation."""
        report = ValidationReport("unknown.py")
        self._run_tier1(report, "unknown.py", code, "unknown")
        
        return {
            "is_valid": report.passed,
            "XXerrorsXX": [i.to_dict() for i in report.issues if i.severity == "error"]
        }
    def xǁValidatorAgentǁvalidate__mutmut_20(self, code: str) -> Dict[str, Any]:
        """Backward compatibility wrapper for singular code validation."""
        report = ValidationReport("unknown.py")
        self._run_tier1(report, "unknown.py", code, "unknown")
        
        return {
            "is_valid": report.passed,
            "ERRORS": [i.to_dict() for i in report.issues if i.severity == "error"]
        }
    def xǁValidatorAgentǁvalidate__mutmut_21(self, code: str) -> Dict[str, Any]:
        """Backward compatibility wrapper for singular code validation."""
        report = ValidationReport("unknown.py")
        self._run_tier1(report, "unknown.py", code, "unknown")
        
        return {
            "is_valid": report.passed,
            "errors": [i.to_dict() for i in report.issues if i.severity != "error"]
        }
    def xǁValidatorAgentǁvalidate__mutmut_22(self, code: str) -> Dict[str, Any]:
        """Backward compatibility wrapper for singular code validation."""
        report = ValidationReport("unknown.py")
        self._run_tier1(report, "unknown.py", code, "unknown")
        
        return {
            "is_valid": report.passed,
            "errors": [i.to_dict() for i in report.issues if i.severity == "XXerrorXX"]
        }
    def xǁValidatorAgentǁvalidate__mutmut_23(self, code: str) -> Dict[str, Any]:
        """Backward compatibility wrapper for singular code validation."""
        report = ValidationReport("unknown.py")
        self._run_tier1(report, "unknown.py", code, "unknown")
        
        return {
            "is_valid": report.passed,
            "errors": [i.to_dict() for i in report.issues if i.severity == "ERROR"]
        }
    
    xǁValidatorAgentǁvalidate__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁValidatorAgentǁvalidate__mutmut_1': xǁValidatorAgentǁvalidate__mutmut_1, 
        'xǁValidatorAgentǁvalidate__mutmut_2': xǁValidatorAgentǁvalidate__mutmut_2, 
        'xǁValidatorAgentǁvalidate__mutmut_3': xǁValidatorAgentǁvalidate__mutmut_3, 
        'xǁValidatorAgentǁvalidate__mutmut_4': xǁValidatorAgentǁvalidate__mutmut_4, 
        'xǁValidatorAgentǁvalidate__mutmut_5': xǁValidatorAgentǁvalidate__mutmut_5, 
        'xǁValidatorAgentǁvalidate__mutmut_6': xǁValidatorAgentǁvalidate__mutmut_6, 
        'xǁValidatorAgentǁvalidate__mutmut_7': xǁValidatorAgentǁvalidate__mutmut_7, 
        'xǁValidatorAgentǁvalidate__mutmut_8': xǁValidatorAgentǁvalidate__mutmut_8, 
        'xǁValidatorAgentǁvalidate__mutmut_9': xǁValidatorAgentǁvalidate__mutmut_9, 
        'xǁValidatorAgentǁvalidate__mutmut_10': xǁValidatorAgentǁvalidate__mutmut_10, 
        'xǁValidatorAgentǁvalidate__mutmut_11': xǁValidatorAgentǁvalidate__mutmut_11, 
        'xǁValidatorAgentǁvalidate__mutmut_12': xǁValidatorAgentǁvalidate__mutmut_12, 
        'xǁValidatorAgentǁvalidate__mutmut_13': xǁValidatorAgentǁvalidate__mutmut_13, 
        'xǁValidatorAgentǁvalidate__mutmut_14': xǁValidatorAgentǁvalidate__mutmut_14, 
        'xǁValidatorAgentǁvalidate__mutmut_15': xǁValidatorAgentǁvalidate__mutmut_15, 
        'xǁValidatorAgentǁvalidate__mutmut_16': xǁValidatorAgentǁvalidate__mutmut_16, 
        'xǁValidatorAgentǁvalidate__mutmut_17': xǁValidatorAgentǁvalidate__mutmut_17, 
        'xǁValidatorAgentǁvalidate__mutmut_18': xǁValidatorAgentǁvalidate__mutmut_18, 
        'xǁValidatorAgentǁvalidate__mutmut_19': xǁValidatorAgentǁvalidate__mutmut_19, 
        'xǁValidatorAgentǁvalidate__mutmut_20': xǁValidatorAgentǁvalidate__mutmut_20, 
        'xǁValidatorAgentǁvalidate__mutmut_21': xǁValidatorAgentǁvalidate__mutmut_21, 
        'xǁValidatorAgentǁvalidate__mutmut_22': xǁValidatorAgentǁvalidate__mutmut_22, 
        'xǁValidatorAgentǁvalidate__mutmut_23': xǁValidatorAgentǁvalidate__mutmut_23
    }
    xǁValidatorAgentǁvalidate__mutmut_orig.__name__ = 'xǁValidatorAgentǁvalidate'
